# 测试与验收指南

> **文档定位**：定义项目最低回归测试、质量验收和专利原型实验记录方式。
> **适用范围**：核心管线、Web 工作台、文档解析、分块算法、QA 与局部修复。
> **最后更新**：2026-07-06
> **维护者**：项目负责人
> **关联文档**：[更新规划](02-更新规划.md)、[开发者文档](01-开发者文档.md)

## 最低自动化测试

第一批测试不依赖真实翻译 API，优先覆盖纯逻辑。状态口径：`已覆盖` 表示已有自动化测试；`基础覆盖` 表示核心路径已有测试但仍需补边界；`待补自动化` 表示功能或验收口径存在，但还缺独立回归测试。

| 编号 | 当前自动化状态 | 测试对象 | 验收点 |
| --- | --- | --- | --- |
| QA-REF-01 | 待补自动化 | `pdf_structure.py` | 能识别 `References`、`Bibliography`、`参考文献` 等标题；无标题时按配置决定是否尾部兜底 |
| QA-CHUNK-01 | 基础覆盖 | `chunking.py` / `chunkers/structure.py` / `qa.chunk_boundary` | `pages_per_chunk` 与 `overlap_pages` 组合正确，非法参数报错；结构分段能保护高风险页面边界、续表跨页边界和相邻结构父子关系，分段边界 QA 能统计保护/切开、续表边界保护率、预算 split reason、预算压力和预算越界情况，并能与固定页分段做同文档对比 |
| QA-MERGE-01 | 待补自动化 | `pipeline_merge.py` | 合并时剥离 chunk YAML front matter，顺序稳定 |
| QA-DEFER-01 | 待补自动化 | `deferral_markers.py` | 段尾顺延标识解析、替换、清理行为稳定 |
| QA-MEM-01 | 基础覆盖 | `memory_store.py` / `pipeline.py` / `qa.translation` | 术语合并按英文去重；术语冲突进入待审；可按待审 key 确认/否决候选译名，单条确认可覆盖候选译名并保留原候选/编辑候选审计字段，写回置信度、章节作用域、审核人和备注；术语注入片段可按页码、章节、结构类型和 block id 选择性过滤；确认后 QA 不再报告已解决冲突，缺失术语证据可保留置信度和作用域；摘要追加不破坏 JSON |
| QA-FILE-01 | 待补自动化 | `export_filename.py` | 中文友好下载名、部分译文名、zip 名生成正确 |
| QA-JOB-01 | 基础覆盖 | Web 任务 | 状态快照 schema、重启恢复、恢复报告、恢复状态计数、恢复重新入队策略、持久化运行参数、后台手动重新入队入口、列表状态合并、缺失状态快照、产物一致性摘要、局部修复补丁审核报告/修复效果对比报告/发布报告/发布稿产物字段、管线 state 诊断、CLI `web-status` 同口径查询、存储漂移识别、路径穿越清理保护、启动审计、取消请求审计、取消终态审计、失败终态审计、启动恢复扫描审计和恢复重新入队审计已有自动化；真实部署观测仍待补 |
| QA-SEC-01 | 基础覆盖 | 生产安全预检、JWT、密钥存储、上传与资料包边界 | `security-preflight-v1` 能报告默认管理员引导密码、CORS 放开、JWT secret 文件兜底、默认/非法 JWT TTL、默认/非法上传上限、默认数据目录和本地 SQLite 密钥明文/密文状态；CLI `security-check` 与管理端预检共用同一报告；管理员设置快照不回传 API Key 明文；设置 `PDF_TRANSLATE_SECRET_KEY` 后新保存的 API Key 会加密落盘，旧明文兼容读取，缺失或错误加密密钥会进入预检问题；JWT 已覆盖 `exp` 签发、有效 token 解析、过期 token 拒绝和缺 `exp` token 拒绝；生产启动门禁已覆盖开发模式不阻断、生产不合格阻断、生产合格放行；上传保存已覆盖流式读取、PDF 魔数拒绝和超限残片清理；zip 资料包已覆盖普通文件保留和 symlink 外链跳过。部署验收仍待补 |
| QA-ERR-01 | 已覆盖 | 结构化错误码 | 缺失 API Key、HTTP 429、HTTP timeout 和 PDF 解析异常能映射为稳定错误码；HTTP retry 事件、Web 任务状态、`run_metrics.json` 与 `experiment_metrics.json` 能记录错误码/错误类别分布；前端提交、取消、下载失败提示和任务卡片能展示结构化下一步建议与错误码 |
| QA-IR-01 | 已覆盖 | DocumentIR | 表格、续表、图注、表格脚注、脚注、公式、图片、人名机构和模型/数据集名具有类型、候选实体或父子关系；图注/表注/脚注支持同页优先和一页跨页回退归位；能输出页面边界碎片风险 |
| QA-TABLE-01 | 已覆盖 | 表格重建证据、确认发布与提示注入 | `table_reconstruction.json` 能输出表格单元格坐标、表头/行头、数字/单位/显著性 token、表注/脚注表级绑定、`footnote_bindings` 脚注单元格级绑定证据、ragged 行/空单元格统计、合并单元格候选证据、候选证据等级、续表关系、`continued_table_groups`、续表链合并/拒绝状态、拒绝/警告原因分布和可重建率；`table_merged_cell_review.json/md` 能把合并候选整理为待人工确认清单，并统计视觉支持、估算证据、缺证据、人工确认和拒绝状态；`table_structure_publish.json/md` 能记录待确认、阻断或已发布状态，`table_reconstruction_confirmed.json` 只在无阻断项且显式确认后生成，阻断时会移除旧确认副本；confirmed 视图中的 `structure_patches` 可进入双语 HTML 和结构化 PDF 译文表格渲染，未确认 source 视图不得改变阅读产物；`structure_hints_manifest.json` 能逐 chunk 记录实际注入翻译请求的结构保护提示、提示长度、命中表格/续表组、脚注绑定、图注/脚注父子关系、跨页关系、实体候选弱提示、锁定 token 和合并候选 breakdowns；当前 chunk 的表格证据、结构关系和实体候选能生成 `v5`「结构保护提示」并进入翻译请求，疑似合并单元格候选会以未确认结构风险进入提示；译后 QA 能定位表格单元格锁定 token 缺失 |
| QA-VISION-01 | 已覆盖 | 图像/OCR 路由、回写与晋级 | 低文本、图片密集、图注相关页面能产出本地 OCR/VLM 路由建议、原因、页面预览、候选区域裁剪证据、OCR 调度任务清单、表格 OCR 结构化任务契约、公式 OCR 结构化任务契约、普通表格 OCR 文本本地结构化推断、OCR 文本合并候选推断、结构化 OCR 结果透传、本地结构门禁、可选本地 OCR 执行摘要、结构化 JSON stdout 适配、OCR 结果载荷、OCR 结果回写报告、OCR 候选 QA、OCR 候选晋级报告、晋级 DocumentIR 副本和表格/公式 canonical 结构晋级 |
| QA-TRANS-01 | 已覆盖 | 译后规则 QA | 能检查漏数字、漏引用、图表编号、Markdown/DocumentIR 表格形状、表格单元格 token 缺失、图注/脚注关系锚点缺失、表格脚注绑定错位、术语一致性、术语冲突、实体候选保留、重复段落和英文残留 |
| QA-REPAIR-01 | 已覆盖 | 局部修复计划、请求、结果、验证、合并、补丁审核、效果对比、发布确认、回滚演练、正式替换与正式回滚 | QA 问题能映射为带优先级、范围、动作、执行器和证据的修复项；表格单元格 token 缺失能进入 `repair_table_cell_tokens`，并携带目标表格/单元格定位、源表格形状不变量和可选 confirmed 结构补丁上下文；修复项能生成包含源文、当前译文、锁定 token、目标合并定位、结构补丁定位依据和后端 payload 的 `repair_requests.json`；修复执行阶段能生成 `repair_results.json`，默认不覆盖正式译文；候选修复片段能生成 `repair_validation.json`，记录锁定 token 与表格形状验证状态；验证通过且可安全定位的候选能进入 `repair_merge.json`、`repaired_full.md`、`repair_merge_qa.json` 和 `repair_effectiveness.json/md`，表格类修复优先按 QA 证据定位目标 Markdown 表格并透传结构补丁上下文；修复效果对比需统计问题减少率、已解决问题、新增问题、持续存在问题、改善 chunk 和回归 chunk；默认生成 `repair_patch_review.json/md` 补丁审核报告、`repair_effectiveness.json/md` 修复效果对比报告、`repair_publish.json/md` 待确认报告、`repair_rollback.json/md` 回滚演练报告、`repair_formal_replace.json/md` 正式替换报告和 `repair_formal_rollback.json/md` 正式回滚报告；人工可通过 Web 列表式审核台写回单条或批量补丁审核决策，批量决策需验证原子性，发布确认会读取既有人工决策，存在 `publish_blocking_count > 0` 时进入 `blocked_patch_review` 且不生成 `published_full.md`；显式回滚演练会生成 `rollback_full.md` 并校验其与原始译文匹配，不覆盖发布稿；显式正式替换会生成 `formal_full.md` 和 `formal_full.before_repair.md`，显式正式回滚会恢复 `formal_full.md` 并保留 `formal_full.repair_applied.md`；Web 任务状态、下载入口、补丁审核台、确认动作、回滚演练、正式替换和正式回滚动作能暴露修复效果对比报告及对应报告与译文副本 |
| QA-RUN-01 | 已覆盖 | 运行性能指标 | `run_log.jsonl` 能按事件记录阶段耗时、每块翻译请求、HTTP 尝试/重试、结构化错误码和合并事件；`run_metrics.json` 能汇总总耗时、阶段耗时、请求字符数、译文字符数、估算 token、请求次数、HTTP 重试次数、`error_code_counts` 与 `error_category_counts`；`cost_estimate.json` 能按配置画像和可计费请求数估算成本 |
| QA-METRICS-01 | 已覆盖 | 实验指标汇总 | `experiment_metrics.json` 能聚合结构 QA、表格重建、合并候选人工确认清单、表格结构确认发布状态、结构提示清单、合并单元格候选、OCR/VLM 路由、OCR 调度任务、表格 OCR 结构契约、公式 OCR 结构契约、结构化 OCR 结果透传、本地结构门禁、OCR 结果载荷、OCR 结果回写、OCR 候选 QA、OCR 候选晋级、表格/公式 canonical 结构晋级、译后 QA、译后结构关系保真 QA、表格脚注绑定 QA、修复计划、修复请求、修复结果、修复验证、修复合并、补丁审核、修复效果对比、修复发布确认、修复回滚演练、正式稿替换、正式稿回滚、结构补丁支撑修复证据、合并后 QA、结构化 PDF 译文报告、PDF 结构关系输出证据、运行性能、错误码分布、续表边界切开/保护、续表拒绝原因分布和成本估算的关键指标 |
| QA-BATCH-01 | 已覆盖 | 批量专利实验入口 | 多 PDF、多策略实验矩阵能逐次复用单篇管线；`experiment-samples` 可按文本量、图片页、表格数字行、公式符号、多栏块分布、图注/脚注线索和实体候选生成样本清单 CSV/JSON/Markdown，并输出申请前样本覆盖度 checklist；样本清单支持 `suggested_*` 与 `confirmed_*` 并存，批量实验优先采用人工确认类型/标签，`--patent-batch-only` 可只运行人工标记纳入的样本；实验入口支持 JSON/CSV/TSV 样本元数据，并生成 `batch_experiment_manifest.json`、`batch_experiment_summary.json`、`batch_experiment_summary.md`、带多维可读性评分列、补丁审核字段、修复效果对比字段、修复发布审核字段、回滚演练字段、正式替换字段和正式回滚字段的 `batch_experiment_review.csv`、策略均值、相对基线差值，以及评分表回读后的 `batch_experiment_evidence.json` / `batch_experiment_evidence.md`；证据摘要会汇总 OCR 结构门禁、补丁审核统计、修复效果对比、修复发布状态、回滚演练状态、正式替换/正式回滚状态、开放合并问题和报告路径 |
| QA-OUT-01 | 基础覆盖 | 输出导出 | 结构化 Markdown、双语 HTML、结构化 PDF 译文原型不会丢失表格、QA 标记和图注关系；当前自动化覆盖双语 HTML 与结构化 PDF 的 confirmed 表格结构补丁渲染、PDF 文件生成、中文文本可抽取、PDF 报告、PDF 正文 `Structure context` 结构证据框、caption/footnote 与表格脚注绑定字段、实验指标、资料包映射和下载产物字段，前端构建覆盖用户端/管理端译文 PDF 入口，复杂图表原位重排、跨页合并单元格分页联动和页码关系仍需人工样本验收 |

QA-BATCH-01 追加验收口径：确认样本过滤后，`batch_experiment_manifest.json`、`batch_experiment_summary.json/md` 和评分后 `batch_experiment_evidence.json/md` 都必须保留 `selected_coverage` / `patent_batch_coverage`，能够说明已选样本在普通、表格密集、公式密集、多栏复杂、扫描/低文本、注释/实体密集 6 类上的数量和缺口。

QA-MEM-01 追加验收口径：术语冲突确认后，活动术语库应只保留确认译名，译后 QA 不再报告已解决冲突；单条确认允许人工覆盖候选译名，覆盖后 `pending_review.json` 与 `glossary.json` 应同时保留确认译名、原候选和编辑候选审计字段；空候选译名、重复处理和批量统一改写候选译名应被拒绝；术语冲突否决后，原术语仍可注入，候选译名不得污染术语库；已确认术语的置信度和章节作用域应能进入缺失术语 QA 证据；带 `section_scope` 的术语在提供章节上下文且不匹配时不得注入，带 `structure:` 或 `block:` 作用域的术语应只进入匹配结构类型或 block 的 chunk，无上下文时保持旧的兼容注入行为。

当前已落地的基础测试位于 `tests/test_structure_ir.py`、`tests/test_experiments.py`、`tests/test_backend_registry.py`、`tests/test_job_status.py`、`tests/test_security_preflight.py`、`tests/test_secret_store.py`、`tests/test_auth_deps.py`、`tests/test_web_upload_security.py` 与 `tests/test_zip_bundle_security.py`。前者覆盖块分类、表格行列元数据、表格单元格重建证据、表格脚注单元格绑定证据、合并单元格候选证据、合并单元格候选证据分级、合并单元格候选人工确认清单、合并单元格候选结构保护提示注入、确认表格结构补丁的双语 HTML 渲染、确认表格结构补丁的结构化 PDF 渲染、确认表格结构补丁进入单元格级修复定位依据、结构提示清单生成、续表链兼容性合并/拒绝、续表拒绝/警告原因分布聚合、表格结构保护提示注入、表格单元格锁定 token QA、表格单元格修复的源表格形状门禁、按证据定位目标 Markdown 表格合并、局部修复补丁审核产物、局部修复效果对比、局部修复发布确认副本、局部修复回滚演练副本、局部修复正式稿替换、局部修复正式稿回滚、续表风险、实体候选提取与译后保留 QA、DocumentIR 表格形状 QA、公式符号/公式编号/变量/统计表达不变量 QA、图注/表注/表格脚注/脚注同页与一页跨页父子归属、结构 chunk provenance、术语作用域注入上下文推断、预算驱动结构分段、结构关系保护、预算指标聚合、跨页关系指标聚合、页面边界碎片 QA、边界保护式结构分段、分段边界 QA、分段策略对比、低文本图片页 OCR 路由、OCR 路由页预览渲染、OCR 候选区域裁剪、OCR 调度任务清单、本地 OCR 执行器、OCR 结果载荷、OCR 结果回写报告、OCR 候选文本 QA、OCR 候选文本晋级、HTTP 重试捕获、结构化错误码分类、译后规则 QA、术语一致性 QA、术语冲突待审、术语审核确认、术语单条编辑确认、术语章节/结构/block 作用域过滤、局部修复计划、局部修复请求、局部修复结果、局部修复验证、局部修复合并、局部修复补丁审核、局部修复发布确认、局部修复回滚演练、局部修复正式替换、局部修复正式回滚、合并后 QA、运行性能指标、成本估算、实验指标汇总、双语 HTML、结构化 PDF 译文、`structure_qa.json` / `structure_chunks_manifest.json` / `table_reconstruction.json` / `table_merged_cell_review.json` / `table_merged_cell_review.md` / `structure_hints_manifest.json` / `chunk_boundary_qa.json` / `chunk_strategy_comparison.json` / `vision_route.json` / `vision_pages/` / `vision_crops/` / `ocr_tasks.json` / `ocr_results.json` / `ocr_writeback.json` / `ocr_candidate_qa.json` / `ocr_candidate_promotion.json` / `document_ir_ocr.json` / `document_ir_promoted.json` / `qa_report.json` / `repair_plan.json` / `repair_requests.json` / `repair_results.json` / `repair_validation.json` / `repair_merge.json` / `repair_patch_review.json` / `repair_patch_review.md` / `repair_effectiveness.json` / `repair_effectiveness.md` / `repair_publish.json` / `repair_publish.md` / `published_full.md` / `repair_rollback.json` / `repair_rollback.md` / `rollback_full.md` / `repair_formal_replace.json` / `repair_formal_replace.md` / `formal_full.md` / `formal_full.before_repair.md` / `repair_formal_rollback.json` / `repair_formal_rollback.md` / `formal_full.repair_applied.md` / `repair_merge_qa.json` / `run_log.jsonl` / `run_metrics.json` / `cost_estimate.json` / `experiment_metrics.json` / `bilingual.html` / `translated_full.pdf` / `translated_pdf_report.json` 与 echo 后端最小管线；`tests/test_experiments.py` 覆盖批量实验策略解析、page/structure 实验矩阵、结构提示指标、合并单元格候选策略均值、续表拒绝类别汇总、按证据定位的修复补丁统计、补丁审核汇总、回滚演练汇总、正式替换/正式回滚汇总、样本元数据匹配、批量 JSON/Markdown 汇总、人工评分 CSV、多维可读性评分列、结构化 PDF 路径和相对基线差值；`tests/test_backend_registry.py` 覆盖后端注册表一致性；`tests/test_job_status.py` 覆盖 Web 任务状态快照、恢复、恢复报告、路径边界、列表合并、产物一致性摘要、补丁审核报告、回滚报告、正式替换/正式回滚报告与 PDF 译文产物字段、管线 state 诊断、CLI 同口径查询、存储漂移识别和路径穿越清理保护；`tests/test_auth_deps.py`、`tests/test_secret_store.py` 与 `tests/test_security_preflight.py` 覆盖 JWT 过期、JWT TTL 配置、生产启动门禁、管理端密钥加密落盘、旧明文兼容和缺失/错误加密密钥预警。可用以下命令运行：

本轮新增验收点：`test_ocr_task_manifest_schedules_region_crops_for_writeback` 需断言表格 OCR 任务包含 `table_context`、`structure_contract` 与 `writeback.subtarget`；`test_table_ocr_writeback_preserves_structure_context` 需断言 OCR 回写候选、候选 QA 和晋级记录保留 `structured_cells`、`cell_bboxes`、`merged_cell_candidates`、`table_footnotes`，完整结构化表格候选通过本地门禁，并在 `document_ir_promoted.json` 中形成 canonical `block.meta.table`，结构分段可把该表格渲染为 Markdown table；`test_table_ocr_plain_text_can_be_locally_structured` 需断言只有普通表格文本的 OCR 结果也能在回写阶段本地推断 `structured_cells`、估算 `cell_bboxes`，经候选 QA 通过结构门禁并晋级为 canonical `block.meta.table`；`test_table_ocr_plain_text_infers_merged_cell_candidates` 需断言普通表格 OCR 文本保留原始短行证据并生成 `merged_cell_candidates`，该候选经过回写、QA 和晋级后仍进入 canonical `block.meta.table`；`test_formula_ocr_writeback_preserves_structure_context` 需断言公式 OCR 任务包含 `formula_context`、公式结构契约与公式回写子目标，OCR 回写、候选 QA 和晋级记录保留 `formula_latex`、`formula_tokens`、`equation_labels` 与 `formula_confidence`，完整结构化公式候选通过本地门禁，并在晋级 IR 中形成 canonical `block.meta.formula`；`test_local_ocr_executor_allows_task_recommended_engines` 需断言 `local_ocr` / `local_table_ocr` / `local_formula_ocr` 这类任务推荐本地引擎可进入执行器摘要而不是被当作不支持引擎；`test_local_ocr_executor_accepts_structured_json_stdout` 需断言本地命令 stdout 返回 `ocr-results-v1`、结果列表或单条 JSON 结果时，执行器能按 `task_id` 选取结果、标记 `output_format=json` 并保留表格/公式结构字段，普通文本 stdout 仍按旧路径标记 `output_format=text`；`test_structured_table_ocr_gate_requires_locked_tokens_and_bboxes` 需断言缺 locked token 或 bbox 的结构化表格 OCR 只能进入复核；`test_experiment_metrics_aggregates_quality_evidence` 需断言 `ocr_structured_contract_task_count`、`ocr_table_context_*`、`ocr_formula_context_*`、`ocr_structured_result_*`、`ocr_structured_table_gate_*`、`ocr_structured_formula_gate_*`、`ocr_canonical_structure_promotion_count`、`ocr_structured_table_promotion_count`、`ocr_structured_formula_promotion_count`、`repair_patch_review_*`、`repair_publish_*`、`repair_rollback_*`、`repair_formal_replace_*`、`repair_formal_rollback_*` 与 `ocr_task_structure_target_counts` 进入实验指标；`test_run_batch_experiment_writes_patent_evidence_summary` 需断言 OCR 结构门禁、结构晋级指标、`repair_patch_review_*` 补丁审核字段、`repair_publish_*` 修复发布字段、`repair_rollback_*` 回滚演练字段、`repair_formal_replace_*` 正式替换字段和 `repair_formal_rollback_*` 正式回滚字段进入 `batch_experiment_summary.json`、`batch_experiment_summary.md`、相对基线差值、`batch_experiment_review.csv`，并在人工填写入选证据后生成包含 `repair_patch_review_summary`、`repair_publish_summary`、`repair_rollback_summary`、`repair_formal_replace_summary` 与 `repair_formal_rollback_summary` 的 `batch_experiment_evidence.json` 与 `batch_experiment_evidence.md`；`test_write_sample_manifest_classifies_pdf_samples` 需断言 `experiment-samples` 使用的样本清单生成器能把表格密集样本、低文本扫描型样本、注释/实体密集样本写入 CSV/JSON/Markdown，保留 `annotation_marker_count`、`entity_candidate_count`、`summary.coverage` 和人工确认字段，并可被 `load_sample_metadata()` 回读；`test_run_batch_experiment_writes_patent_evidence_summary` 还需断言确认后的类型、标签、确认人和确认备注进入批量实验摘要与人工评分表，并断言 `patent_batch_only` 会跳过未纳入样本且在 JSON/Markdown 摘要中留下过滤统计。

本轮新增验收点：`test_run_batch_experiment_writes_patent_evidence_summary` 还需断言 `repair_effectiveness_*` 进入 `batch_experiment_summary.json`、相对基线差值和 `batch_experiment_review.csv`，并在人工填写入选证据后生成 `repair_effectiveness_summary`，汇总修复前后问题总数、问题减少率、已解决/新增/回归数量、状态分布和 `repair_effectiveness.json` 报告路径。

本轮补充验收点：`test_table_reconstruction_uses_meta_merged_cell_candidates` 需断言 canonical `block.meta.table.merged_cell_candidates` 能被表格重建规范化、继承 OCR 来源、与本地候选去重，并进入 `table_reconstruction.json`、`structure_hints_manifest.json` 和翻译提示；`test_table_ocr_plain_text_infers_merged_cell_candidates` 还需覆盖普通表格 OCR 文本从回写、QA、晋级到表格重建和结构提示的完整链路。

本轮补充验收点：`test_table_reconstruction_marks_visual_span_bbox_evidence` 需断言外部 OCR/版面引擎提供 span bbox 时，候选可标记为 `visually_supported`、`visual_span_bbox` 和 `span_reported`，但不能自动变成 `human_confirmed`；`test_table_ocr_plain_text_infers_merged_cell_candidates` 需断言本地文本推断加区域均分 bbox 只能得到 `candidate`、`estimated_bbox` 和 `estimated`，避免把估算网格误认为视觉确认。

本轮补充验收点：`test_table_merged_cell_review_summarizes_confirmation_status` 需断言 `table_merged_cell_review.json/md` 能把普通候选、视觉支持候选、已人工确认和已拒绝候选分开统计，并生成稳定 `review_id` 与人工决策占位；`test_pipeline_writes_document_ir_and_structure_manifest` 需断言 echo 管线生成 `table_merged_cell_review.json/md`，且 `run_metrics` 含 `table_merged_cell_review` 阶段；`test_experiment_metrics_aggregates_quality_evidence` 需断言 `table_merged_cell_review_*` 进入质量指标、比例指标、breakdowns 和 evidence files；`test_bundle_includes_structure_qa_and_repair_artifacts` 需断言资料包包含人工确认清单并使用中文路径。

本轮补充验收点：`test_table_structure_publish_uses_human_review_without_overwriting_source` 需断言人工确认/拒绝会生成独立确认结构副本且不覆盖源 `table_reconstruction.json`；`test_confirm_table_structure_publish_for_completed_job_writes_confirmed_copy` 需断言 Web 确认能写出 `table_structure_publish.json/md` 与 `table_reconstruction_confirmed.json`；`test_confirm_table_structure_publish_blocks_and_removes_stale_copy` 需断言仍有待复核项时返回 409、写出阻断报告并删除旧确认副本；`test_pipeline_writes_document_ir_and_structure_manifest` 需断言默认管线只生成 pending 发布报告、不提前生成确认副本；`test_bundle_includes_structure_qa_and_repair_artifacts` 需断言发布报告和确认副本使用中文资料包路径。

本轮补充验收点：`test_table_merged_cell_review_preview_renders_png` 需断言表格合并候选可从任务 `input.pdf` 渲染 PNG 预览，并优先使用 `bbox_evidence.span_bbox` 画框；`test_table_merged_cell_review_preview_rejects_invalid_page_no` 需断言缺失或非法页码会被拒绝，避免预览接口读取非候选页或形成任意路径入口。前端构建需覆盖 `TableMergedCellReviewModal` 的候选切换、预览加载、错误提示和 object URL 清理。

本轮补充验收点：`test_structure_translation_hints_include_relationships_and_entities` 需断言同一 chunk 内表格、图注、脚注和实体候选会同时进入 `build_structure_translation_hints()`，并保留 `caption_for_table`、`footnote_for_table` 和实体候选弱提示；`test_structure_translation_hints_record_cross_page_relationships` 需断言跨页图注/表注父子关系会进入结构提示并记录跨页计数；`test_experiment_metrics_aggregates_quality_evidence` 与 `test_run_batch_experiment_writes_patent_evidence_summary` 需断言 `structure_hint_relationship_count`、`structure_hint_relationship_cross_page_count`、`structure_hint_entity_count`、`structure_hint_relationship_per_chunk`、`structure_hint_entity_per_chunk` 和 `structure_hint_relationship_type_counts` 进入单次与批量实验指标。

本轮补充验收点：`test_translation_qa_reports_caption_relation_mismatch` 需断言译文缺失图注/表注/脚注关系锚点时生成 `caption_or_footnote_relation_mismatch`，并把缺失锚点锁定到修复请求；`test_translation_qa_reports_table_footnote_binding_mismatch` 需断言表格脚注标记从源单元格脱落时生成 `table_footnote_binding_mismatch` 和 `repair_table_footnote_binding`；`test_translation_qa_accepts_preserved_structure_relations` 需断言关系锚点与脚注绑定保留时不误报；`test_experiment_metrics_aggregates_quality_evidence` 与 `test_run_batch_experiment_writes_patent_evidence_summary` 需断言结构关系异常率和表格脚注绑定异常率进入单次与批量实验指标。

```bash
python -m unittest discover -s tests
```

最近一次 JOB-01 校验（2026-07-06）：`python -m py_compile pdf_translate\server\jobs.py pdf_translate\server\routes_web.py pdf_translate\server\app.py pdf_translate\server\audit_humanize.py tests\test_job_status.py` 通过；`python -m unittest tests.test_job_status` 通过。

最近一次 SEC-01 校验（2026-07-01）：`python -m py_compile pdf_translate\server\security_preflight.py pdf_translate\server\app.py pdf_translate\server\routes_web.py pdf_translate\server\settings_service.py pdf_translate\cli.py pdf_translate\zip_bundle.py tests\test_web_upload_security.py tests\test_zip_bundle_security.py` 通过；`python -m unittest tests.test_web_upload_security tests.test_zip_bundle_security tests.test_security_preflight tests.test_structure_ir.StructureIRTests.test_bundle_includes_structure_qa_and_repair_artifacts` 通过，12 个测试通过，1 个 symlink 权限相关用例按预期跳过。

最近一次 SEC-01 增量校验（2026-07-01）：`python -m py_compile pdf_translate\server\auth_deps.py pdf_translate\server\security_preflight.py pdf_translate\server\app.py tests\test_auth_deps.py tests\test_security_preflight.py` 通过；`python -m unittest tests.test_auth_deps tests.test_security_preflight` 通过，14 个测试通过。

最近一次 SEC-01 密钥管理校验（2026-07-01）：`python -m py_compile pdf_translate\server\secrets_store.py pdf_translate\server\settings_service.py pdf_translate\server\security_preflight.py tests\test_secret_store.py tests\test_security_preflight.py` 通过；`python -m unittest tests.test_secret_store tests.test_security_preflight` 通过，16 个测试通过。

最近一次 CHUNK-01/TABLE-01 续表边界校验（2026-07-01）：`python -m py_compile pdf_translate\structure_boundaries.py pdf_translate\chunkers\structure.py pdf_translate\qa\structure.py pdf_translate\qa\chunk_boundary.py pdf_translate\qa\metrics.py tests\test_structure_ir.py` 通过；`python -m unittest tests.test_structure_ir.StructureIRTests.test_structure_qa_reports_page_boundary_fragments tests.test_structure_ir.StructureIRTests.test_structure_chunks_protect_page_boundary_fragments tests.test_structure_ir.StructureIRTests.test_structure_chunks_prioritize_table_continuation_boundary tests.test_structure_ir.StructureIRTests.test_chunk_boundary_qa_reports_split_and_protected_fragments tests.test_structure_ir.StructureIRTests.test_experiment_metrics_aggregates_quality_evidence` 通过，5 个测试通过。
最近一次 QA-02 公式不变量校验（2026-07-01）：`python -m py_compile pdf_translate\qa\translation.py pdf_translate\qa\repair.py pdf_translate\qa\metrics.py pdf_translate\experiments.py tests\test_structure_ir.py` 通过；`python -m unittest tests.test_structure_ir.StructureIRTests.test_translation_qa_reports_formula_invariant_mismatch tests.test_structure_ir.StructureIRTests.test_translation_qa_reports_missing_invariants tests.test_structure_ir.StructureIRTests.test_experiment_metrics_aggregates_quality_evidence` 通过，3 个测试通过。

最近一次 REPAIR-01 发布确认校验（2026-07-05）：`python -m py_compile pdf_translate\qa\repair.py pdf_translate\pipeline.py pdf_translate\cli.py pdf_translate\qa\metrics.py pdf_translate\experiments.py pdf_translate\zip_bundle.py tests\test_structure_ir.py` 通过；`python -m unittest tests.test_structure_ir.StructureIRTests.test_translation_qa_reports_table_cell_token_mismatch tests.test_structure_ir.StructureIRTests.test_experiment_metrics_aggregates_quality_evidence tests.test_structure_ir.StructureIRTests.test_pipeline_writes_document_ir_and_structure_manifest tests.test_structure_ir.StructureIRTests.test_bundle_includes_structure_qa_and_repair_artifacts` 通过，4 个测试通过。

最近一次 EXP-01/REPAIR-01 批量修复发布审核校验（2026-07-05）：`python -m unittest tests.test_experiments.BatchExperimentTests.test_run_batch_experiment_writes_patent_evidence_summary` 通过；该用例断言 `batch_experiment_review.csv` 写入 `repair_publish_confirmed`、`repair_publish_published`、`repair_publish_open_issue_count`、`repair_publish_status_counts`、`repair_publish_report` 和 `repair_published_full`，并断言 `batch_experiment_evidence.json/md` 汇总发布确认数、发布稿数、开放合并问题和状态分布。

最近一次 Web 修复发布产物校验（2026-07-05）：`python -m py_compile pdf_translate\server\jobs.py pdf_translate\server\routes_web.py pdf_translate\server\database.py tests\test_job_status.py` 通过；`python -m unittest tests.test_job_status` 通过，覆盖 Web 任务产物摘要中的 `repair_publish_*`、`repair_published_full_*` 字段和开放合并问题 warning；`cd frontend && npm run build` 通过，用户端和管理端静态产物已更新。

最近一次 Web 修复稿确认校验（2026-07-05）：`python -m py_compile pdf_translate\server\routes_web.py tests\test_job_status.py` 通过；`python -m unittest tests.test_job_status` 通过，覆盖已完成任务通过 Web 确认逻辑生成 `published_full.md`、更新 `repair_publish.json/md` 并进入任务产物摘要；`cd frontend && npm run build` 通过，用户端和管理端确认按钮已进入静态产物。

最近一次局部修复补丁审核产物校验（2026-07-05）：`python -m py_compile pdf_translate\qa\repair.py pdf_translate\pipeline.py pdf_translate\qa\metrics.py pdf_translate\server\jobs.py pdf_translate\server\routes_web.py pdf_translate\experiments.py tests\test_structure_ir.py tests\test_job_status.py tests\test_experiments.py` 通过；`python -m unittest tests.test_experiments.BatchExperimentTests.test_run_batch_experiment_writes_patent_evidence_summary` 通过，覆盖 `repair_patch_review.json/md`、任务产物摘要、Web 下载入口、批量实验字段和证据摘要。

最近一次局部修复补丁人工审核门禁校验（2026-07-05）：`python -m py_compile pdf_translate\qa\repair.py pdf_translate\pipeline.py pdf_translate\qa\metrics.py pdf_translate\server\routes_web.py pdf_translate\server\jobs.py` 通过；`python -m unittest tests.test_structure_ir tests.test_job_status tests.test_experiments` 通过，67 个测试通过；`npm run build` 通过。新增断言覆盖人工拒绝补丁后 `repair_publish` 进入 `blocked_patch_review`、不生成 `published_full.md`，以及 Web 确认发布保留既有 `repair_patch_review.json` 人工决策而不是重建覆盖。

最近一次 REPAIR-01 列表式逐补丁审核台校验（2026-07-06）：`python -m unittest tests.test_structure_ir.StructureIRTests.test_repair_patch_review_batch_decision_updates_atomically tests.test_job_status.JobStatusSnapshotTests.test_confirm_repair_publish_respects_existing_patch_review_gate` 通过，覆盖补丁批量决策原子性、批量清空后的发布阻断恢复和既有发布门禁；`cd frontend && npm run build` 通过，用户端和管理端补丁审核入口已从 prompt 流程切换为列表式审核台。

最近一次 REPAIR-01 补丁差异预览校验（2026-07-06）：`python -m unittest tests.test_structure_ir.StructureIRTests.test_translation_qa_reports_table_cell_token_mismatch` 通过，覆盖表格单元格 token 修复在 `repair_merge.json`、`repair_patch_review.json` 和 `repair_patch_review.md` 中保留当前译文、修复候选、合并后片段和统一 diff；`python -m unittest tests.test_structure_ir.StructureIRTests.test_repair_patch_review_batch_decision_updates_atomically` 通过，确认旧式候选片段仍可回退显示；`cd frontend && npm run build` 通过，审核台详情侧栏可展示结构化差异预览。

最近一次 EXP-01 确认后样本覆盖度审计校验（2026-07-06）：`python -m py_compile pdf_translate\experiments.py tests\test_experiments.py` 通过；`python -m unittest tests.test_experiments` 通过，4 个测试通过，覆盖 `selected_coverage` / `patent_batch_coverage` 在 manifest、summary、Markdown 和 evidence 中的贯通。

最近一次 UX-01 术语冲突审核写回校验（2026-07-06）：`python -m py_compile pdf_translate\memory_store.py pdf_translate\qa\translation.py tests\test_structure_ir.py` 通过；`python -m unittest tests.test_structure_ir.StructureIRTests.test_memory_store_applies_glossary_review_decisions tests.test_structure_ir.StructureIRTests.test_memory_store_records_glossary_conflicts_for_review tests.test_structure_ir.StructureIRTests.test_translation_qa_reports_glossary_conflicts` 通过，3 个测试通过。

最近一次 UX-01 术语作用域注入校验（2026-07-06）：`python -m py_compile pdf_translate\memory_store.py pdf_translate\pipeline.py tests\test_structure_ir.py` 通过；`python -m unittest tests.test_structure_ir.StructureIRTests.test_chunk_glossary_context_uses_heading_structure_and_block_ids tests.test_structure_ir.StructureIRTests.test_memory_store_applies_glossary_review_decisions` 通过，2 个测试通过。
最近一次完整回归（2026-07-06）：`python -m unittest discover -s tests` 通过，138 个测试通过、1 个 symlink 权限相关用例按预期跳过；`git diff --check` 通过。

最近一次完整回归（2026-07-06）：`python -m unittest discover -s tests` 通过，134 个测试通过、1 个 symlink 权限相关用例按预期跳过；`git diff --check` 通过，仅有 Windows 换行提示。

最近一次 REPAIR-01 回滚演练校验（2026-07-06）：`python -m py_compile pdf_translate\qa\repair.py pdf_translate\pipeline.py pdf_translate\cli.py pdf_translate\qa\metrics.py pdf_translate\zip_bundle.py pdf_translate\server\jobs.py pdf_translate\server\routes_web.py pdf_translate\experiments.py tests\test_structure_ir.py tests\test_job_status.py tests\test_experiments.py` 通过；定向回归覆盖 `repair_rollback.json/md`、`rollback_full.md`、CLI `--rollback-repairs`、Web 确认回滚、任务产物摘要、下载入口、zip 中文路径、实验指标、批量评分表和评分后证据摘要。

最近一次 REPAIR-01 正式稿替换与正式回滚校验（2026-07-06）：`python -m py_compile pdf_translate\qa\repair.py pdf_translate\pipeline.py pdf_translate\cli.py pdf_translate\qa\metrics.py pdf_translate\zip_bundle.py pdf_translate\server\jobs.py pdf_translate\server\routes_web.py pdf_translate\experiments.py tests\test_structure_ir.py tests\test_job_status.py tests\test_experiments.py` 通过；定向回归覆盖 `repair_formal_replace.json/md`、`formal_full.md`、`formal_full.before_repair.md`、`repair_formal_rollback.json/md`、`formal_full.repair_applied.md`、CLI 显式确认、Web 确认动作、任务产物摘要、下载入口、zip 中文路径、实验指标、批量评分表和评分后证据摘要；完整回归 `python -m unittest discover -s tests` 通过，115 个测试通过、1 个 symlink 权限相关用例按预期跳过；`cd frontend && npm run build` 通过；`git diff --check` 通过。

最近一次 B2 结构关系提示注入校验（2026-07-06）：`python -m py_compile pdf_translate\qa\table_reconstruction.py pdf_translate\pipeline.py pdf_translate\qa\metrics.py pdf_translate\qa\__init__.py tests\test_structure_ir.py` 通过；`python -m unittest tests.test_structure_ir.StructureIRTests.test_structure_translation_hints_include_relationships_and_entities tests.test_structure_ir.StructureIRTests.test_structure_translation_hints_record_cross_page_relationships tests.test_structure_ir.StructureIRTests.test_experiment_metrics_aggregates_quality_evidence tests.test_structure_ir.StructureIRTests.test_pipeline_writes_document_ir_and_structure_manifest` 通过，4 个测试通过；`python -m py_compile pdf_translate\experiments.py tests\test_experiments.py` 通过；`python -m unittest tests.test_experiments.BatchExperimentTests.test_run_batch_experiment_writes_patent_evidence_summary tests.test_structure_ir.StructureIRTests.test_experiment_metrics_aggregates_quality_evidence tests.test_structure_ir.StructureIRTests.test_pipeline_writes_document_ir_and_structure_manifest` 通过，3 个测试通过；完整回归 `python -m unittest discover -s tests` 通过，117 个测试通过、1 个 symlink 权限相关用例按预期跳过；`git diff --check` 通过。
最近一次 QA-02/B2 译后结构关系保真 QA 校验（2026-07-06）：`python -m py_compile pdf_translate\qa\translation.py pdf_translate\qa\repair.py pdf_translate\qa\metrics.py pdf_translate\experiments.py tests\test_structure_ir.py tests\test_experiments.py` 通过；`python -m unittest tests.test_structure_ir.StructureIRTests.test_translation_qa_reports_caption_relation_mismatch tests.test_structure_ir.StructureIRTests.test_translation_qa_reports_table_footnote_binding_mismatch tests.test_structure_ir.StructureIRTests.test_translation_qa_accepts_preserved_structure_relations tests.test_structure_ir.StructureIRTests.test_experiment_metrics_aggregates_quality_evidence tests.test_experiments.BatchExperimentTests.test_run_batch_experiment_writes_patent_evidence_summary` 通过，5 个测试通过；完整回归 `python -m unittest discover -s tests` 通过，120 个测试通过、1 个 symlink 权限相关用例按预期跳过；`git diff --check` 通过。

最近一次 TABLE-01/OCR-01 表格 OCR 结构契约、结构化结果透传与本地结构门禁校验（2026-07-01）：`python -m py_compile pdf_translate\qa\ocr_candidates.py pdf_translate\qa\metrics.py tests\test_structure_ir.py` 通过；`python -m unittest tests.test_structure_ir.StructureIRTests.test_table_ocr_writeback_preserves_structure_context tests.test_structure_ir.StructureIRTests.test_structured_table_ocr_gate_requires_locked_tokens_and_bboxes tests.test_structure_ir.StructureIRTests.test_experiment_metrics_aggregates_quality_evidence` 通过，3 个测试通过。
最近一次 TABLE-01/OCR-01 普通表格 OCR 文本本地结构化与合并候选校验（2026-07-06）：`python -m py_compile pdf_translate\vision\ocr_writeback.py pdf_translate\qa\ocr_candidates.py tests\test_structure_ir.py` 通过；`python -m unittest tests.test_structure_ir.StructureIRTests.test_table_ocr_writeback_preserves_structure_context tests.test_structure_ir.StructureIRTests.test_table_ocr_plain_text_can_be_locally_structured tests.test_structure_ir.StructureIRTests.test_table_ocr_plain_text_infers_merged_cell_candidates` 通过，3 个测试通过。
最近一次 TABLE-01/OCR-01 OCR 合并候选接入表格重建校验（2026-07-06）：`python -m py_compile pdf_translate\qa\table_reconstruction.py tests\test_structure_ir.py` 通过；`python -m unittest tests.test_structure_ir.StructureIRTests.test_table_reconstruction_uses_meta_merged_cell_candidates tests.test_structure_ir.StructureIRTests.test_table_ocr_plain_text_infers_merged_cell_candidates tests.test_structure_ir.StructureIRTests.test_table_reconstruction_reports_merged_cell_candidates` 通过，3 个测试通过。
最近一次 TABLE-01 合并候选视觉证据分级校验（2026-07-06）：`python -m py_compile pdf_translate\qa\table_reconstruction.py pdf_translate\vision\ocr_writeback.py pdf_translate\qa\metrics.py tests\test_structure_ir.py` 通过；`python -m unittest tests.test_structure_ir.StructureIRTests.test_table_reconstruction_uses_meta_merged_cell_candidates tests.test_structure_ir.StructureIRTests.test_table_reconstruction_marks_visual_span_bbox_evidence tests.test_structure_ir.StructureIRTests.test_table_ocr_plain_text_infers_merged_cell_candidates tests.test_structure_ir.StructureIRTests.test_table_reconstruction_reports_merged_cell_candidates` 通过，4 个测试通过；`python -m unittest tests.test_structure_ir` 通过，54 个测试通过；`python -m unittest discover -s tests` 通过，100 个测试通过，1 个 symlink 权限相关用例按预期跳过。
最近一次 TABLE-01 合并候选人工确认审计产物校验（2026-07-06）：`python -m py_compile pdf_translate\qa\table_reconstruction.py pdf_translate\pipeline.py pdf_translate\qa\metrics.py pdf_translate\zip_bundle.py tests\test_structure_ir.py` 通过；`python -m unittest tests.test_structure_ir.StructureIRTests.test_table_merged_cell_review_summarizes_confirmation_status tests.test_structure_ir.StructureIRTests.test_experiment_metrics_aggregates_quality_evidence tests.test_structure_ir.StructureIRTests.test_bundle_includes_structure_qa_and_repair_artifacts` 通过，3 个测试通过；`python -m unittest tests.test_structure_ir.StructureIRTests.test_pipeline_writes_document_ir_and_structure_manifest` 通过；`python -m unittest discover -s tests` 通过，101 个测试通过，1 个 symlink 权限相关用例按预期跳过。
最近一次 TABLE-01 合并候选 Web 人工确认回写校验（2026-07-06）：`python -m py_compile pdf_translate\qa\table_reconstruction.py pdf_translate\qa\metrics.py pdf_translate\server\jobs.py pdf_translate\server\routes_web.py pdf_translate\server\database.py tests\test_structure_ir.py tests\test_job_status.py` 通过；`python -m unittest tests.test_structure_ir.StructureIRTests.test_table_merged_cell_review_summarizes_confirmation_status tests.test_structure_ir.StructureIRTests.test_experiment_metrics_aggregates_quality_evidence tests.test_job_status.JobStatusSnapshotTests.test_artifact_summary_reports_repair_publish_status` 通过，3 个测试通过；`cd frontend && npm run build` 通过，用户端和管理端表格确认入口已进入静态产物；`python -m unittest discover -s tests` 通过，101 个测试通过，1 个 symlink 权限相关用例按预期跳过。
最近一次 TABLE-01 合并候选筛选与批量审核校验（2026-07-06）：`python -m py_compile pdf_translate\qa\table_reconstruction.py pdf_translate\server\routes_web.py tests\test_structure_ir.py` 通过；`python -m unittest tests.test_structure_ir.StructureIRTests.test_table_merged_cell_review_summarizes_confirmation_status tests.test_structure_ir.StructureIRTests.test_table_merged_cell_review_batch_decision_updates_atomically` 通过，2 个测试通过；`python -m unittest discover -s tests` 通过，107 个测试通过，1 个 symlink 权限相关用例按预期跳过；`cd frontend && npm run build` 通过，审核面板已支持状态/证据/关键词筛选、当前筛选勾选和批量确认/拒绝/复查/清空。
最近一次 TABLE-01/OUT-01 双语 HTML 表格结构补丁渲染校验（2026-07-06）：`python -m py_compile pdf_translate\exporters\bilingual_html.py pdf_translate\pipeline.py tests\test_structure_ir.py` 通过；`python -m unittest tests.test_structure_ir.StructureIRTests.test_bilingual_html_applies_confirmed_table_structure_patches` 通过，确认副本会输出 `colspan` 和补丁追踪属性，未确认 source 视图不会改变 HTML 表格。
最近一次 TABLE-01/OUT-01 结构化 PDF 表格结构补丁渲染校验（2026-07-06）：`python -m py_compile pdf_translate\exporters\translated_pdf.py pdf_translate\qa\metrics.py tests\test_structure_ir.py` 通过；`python -m unittest tests.test_structure_ir.StructureIRTests.test_translated_pdf_report_marks_confirmed_table_reconstruction tests.test_structure_ir.StructureIRTests.test_translated_pdf_renders_confirmed_rowspan_patches tests.test_structure_ir.StructureIRTests.test_experiment_metrics_aggregates_quality_evidence` 通过；`python -m unittest tests.test_structure_ir` 通过，结构化 PDF 会在 confirmed 视图下渲染 `colspan` / `rowspan` 并跳过 covered cell，未确认 source 视图仍保留原始表格；另用 PyMuPDF 渲染 smoke PDF 页面 PNG，确认基础合并表格无明显重叠。
最近一次 TABLE-01/REPAIR-01 表格结构补丁进入单元格级修复定位校验（2026-07-06）：`python -m py_compile pdf_translate\qa\translation.py pdf_translate\qa\repair.py pdf_translate\qa\metrics.py tests\test_structure_ir.py` 通过；`python -m unittest tests.test_structure_ir.StructureIRTests.test_repair_requests_promote_confirmed_structure_patch_into_merge_target tests.test_structure_ir.StructureIRTests.test_experiment_metrics_aggregates_quality_evidence` 通过，确认译后 QA 会生成 `matched_structure_patches`，修复请求会生成 `merge_target.structure_patch_context` 与 `structure_patch_source_review_ids`，验证、合并、补丁审核和实验指标都会保留结构补丁定位证据。
最近一次 OCR-01 公式 OCR 结构契约、结构化结果透传与本地结构门禁校验（2026-07-01）：`python -m py_compile pdf_translate\vision\ocr_tasks.py pdf_translate\vision\ocr_writeback.py pdf_translate\qa\ocr_candidates.py pdf_translate\vision\ocr_promotion.py pdf_translate\qa\metrics.py pdf_translate\experiments.py tests\test_structure_ir.py` 通过；`python -m unittest tests.test_structure_ir.StructureIRTests.test_formula_ocr_writeback_preserves_structure_context tests.test_structure_ir.StructureIRTests.test_table_ocr_writeback_preserves_structure_context tests.test_structure_ir.StructureIRTests.test_experiment_metrics_aggregates_quality_evidence` 通过，3 个测试通过。
最近一次 OCR-01 本地结构化 JSON 执行器校验（2026-07-01）：`python -m py_compile pdf_translate\vision\ocr_executor.py tests\test_structure_ir.py` 通过；`python -m unittest tests.test_structure_ir.StructureIRTests.test_local_ocr_executor_runs_ready_tasks_into_results_payload tests.test_structure_ir.StructureIRTests.test_local_ocr_executor_allows_task_recommended_engines tests.test_structure_ir.StructureIRTests.test_local_ocr_executor_accepts_structured_json_stdout` 通过，3 个测试通过。
最近一次 OCR-01 结构化 OCR canonical 晋级校验（2026-07-01）：`python -m py_compile pdf_translate\vision\ocr_promotion.py pdf_translate\qa\metrics.py pdf_translate\experiments.py tests\test_structure_ir.py tests\test_experiments.py` 通过；`python -m unittest tests.test_structure_ir.StructureIRTests.test_table_ocr_writeback_preserves_structure_context tests.test_structure_ir.StructureIRTests.test_formula_ocr_writeback_preserves_structure_context tests.test_structure_ir.StructureIRTests.test_experiment_metrics_aggregates_quality_evidence tests.test_experiments.BatchExperimentTests.test_run_batch_experiment_writes_patent_evidence_summary` 通过，4 个测试通过。
最近一次 EXP-01/OCR 表格与公式结构门禁批量证据校验（2026-07-01）：`python -m py_compile pdf_translate\experiments.py tests\test_experiments.py` 通过；`python -m unittest tests.test_experiments.BatchExperimentTests.test_run_batch_experiment_writes_patent_evidence_summary` 通过，1 个测试通过。
最近一次 EXP-01 人工评分证据摘要校验（2026-07-01）：`python -m py_compile pdf_translate\experiments.py pdf_translate\cli.py tests\test_experiments.py` 通过；`python -m unittest tests.test_experiments.BatchExperimentTests.test_run_batch_experiment_writes_patent_evidence_summary` 通过；`python -m pdf_translate.cli experiment-evidence --help` 通过。
最近一次 EXP-01 样本清单、覆盖度与 Markdown 报告校验（2026-07-01）：`python -m py_compile pdf_translate\experiments.py pdf_translate\cli.py tests\test_experiments.py` 通过；`python -m unittest tests.test_experiments.BatchExperimentTests.test_write_sample_manifest_classifies_pdf_samples` 通过；`python -m pdf_translate.cli experiment-samples --help` 通过。
最近一次 EXP-01 样本人工确认字段校验（2026-07-01）：`python -m py_compile pdf_translate\experiments.py tests\test_experiments.py` 通过；`python -m unittest tests.test_experiments.BatchExperimentTests.test_write_sample_manifest_classifies_pdf_samples tests.test_experiments.BatchExperimentTests.test_run_batch_experiment_writes_patent_evidence_summary tests.test_experiments.BatchExperimentTests.test_sample_metadata_supports_positional_json_and_relative_tsv` 通过。
最近一次 EXP-01 确认样本过滤跑批校验（2026-07-01）：`python -m py_compile pdf_translate\experiments.py pdf_translate\cli.py tests\test_experiments.py` 通过；`python -m unittest tests.test_experiments.BatchExperimentTests.test_run_batch_experiment_writes_patent_evidence_summary` 通过；`python -m pdf_translate.cli experiment --help` 通过。

最近一次 OUT-01 PDF 输出校验（2026-07-01）：`python -m py_compile pdf_translate\exporters\translated_pdf.py pdf_translate\exporters\__init__.py pdf_translate\pipeline.py pdf_translate\qa\metrics.py pdf_translate\zip_bundle.py pdf_translate\server\jobs.py pdf_translate\server\routes_web.py pdf_translate\server\database.py pdf_translate\experiments.py tests\test_structure_ir.py tests\test_experiments.py tests\test_job_status.py tests\test_zip_bundle_security.py` 通过；`python -m unittest tests.test_structure_ir.StructureIRTests.test_translated_pdf_exporter_writes_readable_pdf_and_report tests.test_structure_ir.StructureIRTests.test_pipeline_writes_document_ir_and_structure_manifest tests.test_structure_ir.StructureIRTests.test_experiment_metrics_aggregates_quality_evidence tests.test_structure_ir.StructureIRTests.test_bundle_includes_structure_qa_and_repair_artifacts tests.test_experiments.BatchExperimentTests.test_run_batch_experiment_writes_patent_evidence_summary tests.test_job_status.JobStatusSnapshotTests.test_merge_status_into_rows_preserves_database_created_at tests.test_job_status.JobStatusSnapshotTests.test_artifact_summary_marks_done_without_translation_inconsistent tests.test_job_status.JobStatusSnapshotTests.test_merge_status_into_rows_marks_missing_runtime_status tests.test_zip_bundle_security.ZipBundleSecurityTests.test_iter_bundle_files_keeps_regular_files` 通过，9 个测试通过；补充结构关系输出审计后，`python -m unittest tests.test_structure_ir.StructureIRTests.test_translated_pdf_exporter_writes_readable_pdf_and_report tests.test_structure_ir.StructureIRTests.test_experiment_metrics_aggregates_quality_evidence tests.test_structure_ir.StructureIRTests.test_pipeline_writes_document_ir_and_structure_manifest` 通过，3 个测试通过。

最近一次 OUT-01 前端入口校验（2026-07-01）：`npm run build` 通过，构建产物已写入 `pdf_translate/server/static/`。

最近一次 EXP-01 多维人工评分表校验（2026-07-01）：`python -m py_compile pdf_translate\experiments.py tests\test_experiments.py` 通过；`python -m unittest tests.test_experiments.BatchExperimentTests.test_run_batch_experiment_writes_patent_evidence_summary` 通过。

最近一次全量回归（2026-07-06）：`python -m unittest discover -s tests` 通过，134 个测试通过，1 个 symlink 权限相关用例按预期跳过。

## 手工回归场景

| 场景 | 输入 | 期望 |
| --- | --- | --- |
| 最小流程 | 1 个文本型 PDF，后端 `echo` | 任务完成，生成 Markdown、links CSV、zip |
| 串联翻译 | 3 页以上论文，重叠 1 页 | 有前文摘要与尾部衔接，无明显重复 |
| 并联翻译 | 多块文本，workers > 1 | 输出顺序正确，段尾顺延文件为空 |
| 精品翻译 | 已配置硅基巡视模型 | `output/survey/*.json` 生成，术语写入 glossary |
| 取消任务 | 长任务运行中点击终止 | 当前块结束后状态为 cancelled，可下载已译部分 |
| 管理端 | 管理员配置 DeepSeek 与后端开关 | 用户端可选后端与管理端一致 |
| 权限 | 普通用户访问他人任务 | 返回 404 或无权访问 |
| 表格密集论文 | 含多列表格、脚注、单位和显著性符号 | 表格行列可读，数字和单位不丢失 |
| 图像/扫描页论文 | 含低文本页、图片型表格或扫描页 | 生成 OCR/VLM 风险记录、页面预览、候选区域裁剪证据和 OCR 调度任务，低文本页不被静默跳过 |
| 跨页内容 | 跨页段落、续表、续图注 | 分段结果不产生明显半句切断和重复译文 |
| 双语 HTML | 任意文本型论文 | 原文、译文、QA 问题和修复建议能按块同屏对照 |
| PDF 输出原型 | 复杂表格和图注论文 | PDF 译文能保留标题、段落、表格、图注和页码关系 |

## 翻译质量指标

用于规划、专利原型和导师评审：

- 机器可读实验摘要：每次任务保存 `output/experiment_metrics.json`，并回链 `structure_qa.json`、`structure_chunks_manifest.json`、`table_reconstruction.json`、`table_merged_cell_review.json`、`structure_hints_manifest.json`、`chunk_boundary_qa.json`、`chunk_strategy_comparison.json`、`vision_route.json`、`vision_pages/`、`vision_crops/`、`ocr_tasks.json`、`ocr_results.json`、`ocr_writeback.json`、`ocr_candidate_qa.json`、`ocr_candidate_promotion.json`、`document_ir_ocr.json`、`document_ir_promoted.json`、`qa_report.json`、`repair_plan.json`、`repair_requests.json`、`repair_results.json`、`repair_validation.json`、`repair_merge.json`、`repair_patch_review.json`、`repair_effectiveness.json`、`repair_publish.json`、`published_full.md`、`repair_rollback.json`、`rollback_full.md`、`repair_formal_replace.json`、`formal_full.md`、`formal_full.before_repair.md`、`repair_formal_rollback.json`、`formal_full.repair_applied.md`、`repair_merge_qa.json`、`translated_full.pdf`、`translated_pdf_report.json`、`run_metrics.json`、`run_log.jsonl` 和 `cost_estimate.json`；其中 PDF 报告会把 caption/footnote 关系、表格脚注单元格绑定、续表组和结构关系输出证据汇入 `translated_pdf_*` 指标，翻译 QA 会把 `source_formula_token_count`、`missing_formula_token_count`、`source_equation_label_count`、`missing_equation_label_count`、`formula_token_missing_rate` 和 `equation_label_missing_rate` 汇入质量指标，修复效果对比会把 `repair_effectiveness_issue_reduction_rate`、已解决/新增/回归问题和 chunk 级变化汇入修复指标。
- 批量实验摘要：`pdf-translate experiment-samples <论文目录或PDF...> --output samples.csv --report samples.json` 可先生成样本清单、分析报告、Markdown 覆盖度报告和 `summary.coverage` 覆盖度缺口；`pdf-translate experiment` 会生成 `batch_experiment_manifest.json`、`batch_experiment_summary.json`、`batch_experiment_summary.md` 和 `batch_experiment_review.csv`；可通过 `--sample-manifest` 读取 `source_pdf`、`sample_id`、`pdf_type`、`tags`、`notes`，也可读取 `confirmed_pdf_type`、`confirmed_tags`、`include_in_patent_batch`、`reviewer`、`review_notes`，其中 `source_pdf` 支持相对样本清单的路径，缺省时按 PDF 入参顺序补位；加 `--patent-batch-only` 时只运行 `include_in_patent_batch` 为真值的样本，并在摘要中记录跳过数量。批量实验会为每个样本/策略保留续表拒绝类别、表格 OCR 门禁、公式 OCR 门禁、补丁审核统计、修复发布状态、回滚演练状态、正式替换状态、正式回滚状态、开放合并问题、补丁审核报告路径、发布报告路径、回滚报告路径、正式替换报告路径、正式回滚报告路径、样本确认信息、`human_score`、Markdown/HTML/PDF 可读性评分、表格可读性评分、图文脚注排版评分、术语一致性评分、结构连贯性评分、专利证据采纳结论、`reviewer` 和 `review_notes`；人工评分后可运行 `pdf-translate experiment-evidence --summary-json batch_experiment_summary.json` 生成 `batch_experiment_evidence.json` 与 `batch_experiment_evidence.md`，其中会汇总结构化表格候选、表格 bbox 覆盖、结构化公式候选、公式 token/编号回流、门禁问题分布、补丁审核摘要、局部修复发布审核摘要、回滚演练摘要、正式替换摘要和正式回滚摘要。
- 批量修复效果摘要：批量实验会把单篇 `repair_effectiveness.json` 的修复前后问题数、问题减少率、已解决/新增/回归数量和状态分布写入 `batch_experiment_review.csv` 与 `batch_experiment_evidence.json/md`，便于筛选真正适合作为专利交底证据的修复样本。
- 运行性能摘要：每次任务保存 `output/run_metrics.json`，记录总耗时、阶段耗时、翻译请求次数、HTTP 尝试次数、HTTP 重试次数、请求字符数、译文字符数、估算 token、跳过块和吞吐指标；`output/run_log.jsonl` 保留事件级明细。
- 成本估算摘要：每次任务保存 `output/cost_estimate.json`，记录成本画像是否命中、币种、单位价格、估算 token/字符/请求用量、可计费请求数和估算金额；未配置付费后端价格时，不把金额 0 解释为真实免费。
- 漏译率：源段落或结构块未在译文中对应出现的比例。
- 重复译文率：由重叠页或合并造成的重复片段比例。
- 术语一致率：术语库中的术语在译文中使用一致的比例。
- 数字保留率：源文数字集合在译文中保留的比例。
- 公式/变量保留率：公式编号、变量名、LaTeX 片段保留比例。
- 引用保留率：`[1]`、`(Smith, 2024)`、图表编号等引用保留比例。
- 表格行列保持率：输出表格维度与源表格维度一致的比例。
- 表格可重建率：`reconstructable_table_count / table_count`，用于观察源表格能否被本地结构层恢复为可消费单元格。
- 合并单元格候选率：`table_merged_cell_candidate_count / table_count`，用于观察缺列行、标题行或内部空单元格等疑似合并单元格现象在样本中的密度。
- Ragged 表格率：`table_ragged_table_rate`，用于观察表格抽取后行长不一致的比例，辅助区分真实合并单元格和抽取噪声。
- 表格空单元格率：`table_empty_cell_rate`，用于观察表格结构中空位密度，辅助定位 rowspan/colspan 候选和 OCR/解析缺口。
- 合并单元格候选类型分布：`experiment_metrics.breakdowns.table_merged_cell_candidate_type_counts`，用于区分 `rowspan`、`colspan` 等候选结构。
- 合并单元格候选人工确认率：关注 `table_merged_cell_review_required_count`、`table_merged_cell_review_pending_count`、`table_merged_cell_review_human_reviewed_count`、`table_merged_cell_review_human_confirmed_count`、`table_merged_cell_review_rejected_count` 和 `table_merged_cell_review_needs_revision_count`，用于观察高风险表格候选是否已经被人工确认或退回复查。
- 结构提示覆盖率：`structure_hint_chunk_count / chunk_count`，用于观察表格、续表、脚注绑定、合并候选等结构证据实际进入翻译请求的 chunk 比例。
- 结构提示长度：`structure_hint_char_count`、`structure_hint_avg_char_count`、`structure_hint_max_char_count`，用于观察结构保护提示对上下文预算的占用。
- 结构提示内合并候选分布：`experiment_metrics.breakdowns.structure_hint_merged_cell_candidate_type_counts` 与 `structure_hint_merged_cell_candidate_reason_counts`，用于观察真正注入翻译控制的合并候选类型和原因，而不只统计源表格候选总量。
- 结构关系提示覆盖：`structure_hint_relationship_count`、`structure_hint_relationship_cross_page_count`、`structure_hint_relationship_per_chunk` 和 `structure_hint_relationship_type_counts`，用于观察图注、表注、脚注父子关系是否实际进入翻译控制。
- 实体候选提示覆盖：`structure_hint_entity_count` 和 `structure_hint_entity_per_chunk`，用于观察专名候选是否作为弱提示进入翻译请求。
- 表格数字单元格率：含数字 token 的表格单元格比例，用于衡量表格保护翻译中需要锁定的关键单元格密度。
- 表格脚注单元格绑定率：`table_footnote_cell_binding_count / table_footnote_binding_count`，用于观察已归属表格脚注中有多少能进一步回到具体单元格。
- 表格脚注未绑定率：`table_footnote_unbound_count / table_footnote_binding_count`，用于观察脚注标记缺失、普通数字/字母歧义或复杂排版导致的单元格级归属失败比例。
- 表格形状异常率：`table_shape_error_count / source_table_count`，用于观察表格被线性化或行列丢失的比例。
- 表格单元格 token 异常率：`table_cell_token_error_count / table_numeric_cell_count`，用于观察关键数值单元格被错误改写或拆散的比例。
- 表格锁定 token 缺失率：`missing_table_locked_token_count / source_table_locked_token_count`，用于观察数字、单位和显著性符号是否保留在对应单元格。
- 修复请求就绪率：`repair_backend_request_count / repair_request_count`，用于观察 QA 问题中有多少可直接交给翻译后端进入局部重译。
- 修复执行成功率：`repair_succeeded_count / repair_executed_request_count`，用于观察已执行修复请求是否稳定产出候选修复片段。
- 修复验证通过率：`repair_validation_passed_count / (repair_validation_passed_count + repair_validation_failed_count)`，用于观察候选修复片段是否满足本地不变量门禁。
- 修复锁定 token 通过率：`1 - repair_validation_missing_locked_token_count / repair_validation_checked_locked_token_count`，用于观察局部修复是否真的补回数字、单位、实体或其他锁定 token。
- 修复合并应用率：`repair_merge_applied_count / repair_merge_candidate_count`，用于观察验证通过的候选修复有多少能被安全写入独立合并副本。
- 按证据定位的表格修复补丁率：`repair_merge_table_targeted_patch_count / repair_merge_applied_count`，用于观察表格类修复是否按 QA 证据定位到目标 Markdown 表格，而不是仅靠首表兜底替换。
- 修复发布率：`repair_publish_published / repair_publish_confirmed`，用于观察用户发起发布确认请求后是否真正生成 `published_full.md` 发布副本；`repair_publish_confirmed` 不是发布成功标记，发布成功必须看 `repair_publish_published`；同时关注 `repair_publish_open_issue_count`、`repair_publish_status_counts` 和 `patch_review_blocking_count`，避免带冲突或未通过审核的修复稿被误认为无风险发布。
- 回滚演练成功率：`repair_rollback_applied / repair_rollback_confirmed`，用于观察用户发起回滚演练后是否真正生成 `rollback_full.md`；同时关注 `repair_rollback_matches_original`、`repair_rollback_status_counts` 和 `published_preserved`，避免把未验证的回滚基线当作可恢复路径。
- 正式替换成功率：`repair_formal_replace_replaced / repair_formal_replace_confirmed`，用于观察用户发起正式替换后是否真正生成 `formal_full.md`，并关注 `repair_formal_replace_matches_published`、`repair_formal_replace_status_counts` 和备份是否存在。
- 正式回滚成功率：`repair_formal_rollback_applied / repair_formal_rollback_confirmed`，用于观察用户发起正式回滚后是否真正恢复 `formal_full.md`，并关注 `repair_formal_rollback_matches_backup`、`repair_formal_rollback_status_counts` 和 `formal_full.repair_applied.md` 快照是否存在。
- 修复补丁复核率：`repair_patch_review_required_count / repair_patch_review_count`，用于观察自动合并后的补丁中有多少仍需人工逐项判断；同时关注 `repair_patch_review_blocking_count`、`repair_patch_review_default_decision_counts`、`repair_patch_review_effective_decision_counts`、`repair_patch_review_human_decision_counts`、`repair_patch_review_human_reviewed_rate` 和 `repair_patch_review_risk_counts`，避免高风险补丁被直接当作可发布内容。
- 合并后 issue 降幅：`post_repair_issue_delta / translation_issue_count`，用于观察修复副本相对原译文 QA 是否减少问题。
- 续表识别率：跨页续表边界能被 `table_continuations` 识别并被结构分段保护的比例。
- 续表边界保护率：`experiment_metrics.rates.table_continuation_boundary_protected_rate`，用于观察结构分段相对固定页分段是否减少续表被页码切开的情况。
- 续表链合并率：`table_chain_merged_count / table_chain_candidate_count`，用于观察跨页表格链有多少能通过兼容性闸门形成合并重建视图。
- 续表链拒绝率：`table_chain_reject_count / table_chain_candidate_count`，用于观察疑似续表中有多少因列数漂移、表头突变或低置信风险被阻止强合并。
- 续表拒绝原因分布：`experiment_metrics.breakdowns.table_chain_reject_reason_category_counts`，用于观察续表候选被拒绝的主要类别，例如表头突变、列数漂移或不可重建片段。
- 续表解释密度：`table_chain_reject_reason_per_rejected_chain` 与 `table_chain_warning_reason_per_candidate_chain`，用于观察续表合并闸门是否为拒绝或低置信合并留下足够解释证据。
- 续表链行增益：`table_chain_row_gain`，用于观察合并续表后比首段表格新增的有效行数。
- 图注/脚注归位率：图注、表注、脚注能回到所属图表、表格、段落或页面的比例。
- 跨页父子关系成功率：`experiment_metrics.rates.cross_page_parent_success_rate`，用于观察同页归属失败后的一页跨页回退是否能找到可信父块。
- caption/footnote 跨页链接率：`caption_cross_page_link_rate` / `footnote_cross_page_link_rate`，用于区分图表说明与脚注在跨页场景中的归位效果。
- PDF 结构上下文覆盖率：`translated_pdf_structure_context_chunk_rate`，用于观察有多少 PDF 译文 chunk 明确写入了表格、图注、脚注、续表组或结构关系证据。
- 实体候选覆盖率：作者、人名、机构、数据集、模型名被本地规则识别为候选实体的比例。
- 人名/机构保留率：候选或确认实体在译文中被保留或一致翻译的比例。
- 实体缺失数：源文候选实体未在译文中保留的数量。
- 图像/扫描页覆盖率：需要 OCR/VLM 的页面被识别和处理的比例。
- 图像预览证据覆盖率：`vision_preview_page_count / page_count`，用于观察被路由页面是否已经生成可人工复核的页面预览。
- 图像候选区域裁剪密度：`vision_region_crop_count / routed_page_count`，用于观察路由页是否已经给出细粒度 OCR/VLM 候选区域。
- OCR 任务密度：`ocr_task_count / routed_page_count`，用于观察路由页是否已经转成可执行 OCR/VLM 待办。
- OCR 任务就绪率：`ocr_ready_task_count / ocr_task_count`，用于观察调度任务是否具备页面预览或区域裁剪输入图。
- OCR 表格结构契约任务率：`ocr_structured_contract_task_count / ocr_task_count`，用于观察表格类 OCR 任务是否已经从普通文本识别升级为结构化任务。
- OCR 表格上下文任务率：`ocr_table_context_task_count / ocr_task_count`，用于观察任务清单中有多少 OCR 任务携带源表格行列、表头、数字 token 和回写子目标。
- OCR 表格上下文就绪率：`ocr_table_context_ready_task_count / ocr_table_context_task_count`，用于观察表格 OCR 结构契约是否具备可执行输入图和可定位回写目标。
- OCR 结构化结果回写率：`ocr_structured_result_writeback_count / ocr_accepted_result_count`，用于观察接受的 OCR 结果中有多少携带单元格、bbox、合并候选或表格脚注等结构化字段。
- OCR 结构化结果候选率：`ocr_structured_result_candidate_count / ocr_candidate_qa_count`，用于观察进入候选 QA 的 OCR 文本中有多少保留了结构化表格结果。
- OCR 结构化单元格数：`ocr_structured_cell_count`，用于观察真实 OCR 结果回流的单元格级结构规模。
- OCR 结构化表格门禁通过率：`ocr_structured_table_gate_passed_count / ocr_structured_table_candidate_count`，用于观察结构化表格 OCR 中有多少满足行列、bbox 与 locked token 门禁。
- OCR 结构化表格复核率：`ocr_structured_table_gate_review_count / ocr_structured_table_candidate_count`，用于观察结构化表格 OCR 中有多少仍需人工或更强模型复核。
- OCR 公式上下文任务率：`ocr_formula_context_task_count / ocr_task_count`，用于观察公式区域是否已经从普通 OCR 文本识别升级为公式结构任务。
- OCR 公式上下文就绪率：`ocr_formula_context_ready_task_count / ocr_formula_context_task_count`，用于观察公式 OCR 任务是否具备可执行输入图和可定位回写目标。
- OCR 结构化公式门禁通过率：`ocr_structured_formula_gate_passed_count / ocr_structured_formula_candidate_count`，用于观察公式 OCR 中有多少同时保留文本、LaTeX、公式 token、编号和 locked token。
- OCR 结构化公式复核率：`ocr_structured_formula_gate_review_count / ocr_structured_formula_candidate_count`，用于观察公式 OCR 中有多少因缺 LaTeX、缺 token、缺编号、低置信或缺 locked token 需要人工或更强解析器复核。
- OCR 公式 token/编号回流数：`ocr_result_formula_token_count`、`ocr_result_equation_label_count`，用于观察真实 OCR 结果中回流的公式结构规模。
- OCR 表格单元格 bbox 覆盖率：`ocr_cell_bbox_count / ocr_structured_cell_count`，用于观察 OCR 返回的单元格 bbox 覆盖程度。
- OCR 结果载荷有效率：`ocr_result_payload_count / (ocr_result_payload_count + ocr_invalid_result_count)`，用于观察外部或本地 OCR 执行器返回的结果文件是否被规范化为有效结果。
- OCR 本地执行成功率：`ocr_executor_succeeded_task_count / ocr_executor_attempted_task_count`，用于观察可选本地 OCR 执行器在已有输入图任务上的成功情况。
- OCR 结果覆盖率：`ocr_result_count / ocr_task_count`，用于观察待执行任务中已有多少获得 OCR 结果载荷。
- OCR 结果接受率：`ocr_accepted_result_count / ocr_result_count`，用于观察 OCR 结果通过状态、文本和置信度门禁的比例。
- OCR 回写应用率：`(ocr_block_writeback_count + ocr_page_writeback_count) / ocr_task_count`，用于观察 OCR 候选文本是否已经进入增强 DocumentIR。
- OCR 候选可晋级率：`ocr_candidate_promotable_count / ocr_candidate_qa_count`，用于观察回写候选中有多少通过本地 QA 门禁。
- OCR 候选实际晋级率：`ocr_candidate_promoted_count / ocr_candidate_qa_count`，用于观察有多少 OCR 文本进入 `document_ir_promoted.json` 并参与结构分段。
- OCR 候选合格晋级率：`ocr_candidate_promoted_count / ocr_candidate_promotion_eligible_count`，用于观察通过 QA 的候选是否能定位到原始回写目标并被写入晋级 IR。
- OCR canonical 结构晋级数：`ocr_canonical_structure_promotion_count`，用于观察通过门禁后真正写入 `block.meta.table` 或 `block.meta.formula` 的结构化 OCR 候选数量。
- OCR 结构化表格晋级率：`ocr_structured_table_promotion_count / ocr_structured_table_gate_passed_count`，用于观察通过表格门禁的 OCR 候选有多少能转成 canonical 表格结构。
- OCR 结构化公式晋级率：`ocr_structured_formula_promotion_count / ocr_structured_formula_gate_passed_count`，用于观察通过公式门禁的 OCR 候选有多少能转成 canonical 公式结构。
- 页面交接碎片率：分页造成半句、跨页段落、续表被切断的比例。
- 边界切开率：已识别页面边界碎片中仍被当前 chunks 切开的比例。
- 边界保护率：已识别页面边界碎片中被结构分段器显式保护的比例。
- 边界切开降幅：结构分段相对固定页分段减少的切开边界数量和比例。
- 续表切开降幅：`active_table_continuation_split_reduction_vs_baseline` 与 `active_table_continuation_split_reduction_rate_vs_baseline`，用于比较结构分段相对固定页分段减少了多少续表跨页切开。
- 预算切分原因分布：`chunk_boundary_qa.summary.budget_split_reason_counts`，用于观察 chunk 是因目标预算、硬预算、页数阈值、结构关系还是文档结束而切分。
- 预算压力分布：`chunk_boundary_qa.summary.budget_pressure_counts`，用于观察 chunk 处于目标预算内、超过目标预算或超过硬预算的比例。
- 预算越界块率：`experiment_metrics.rates.budget_overflow_chunk_rate`，用于观察结构关系保护是否频繁推高单块大小。
- 结构关系保护次数：`experiment_metrics.quality.structural_relation_protected_count`，用于观察图注、表注、脚注与父块是否被结构分段器保护在同一块内。
- 英文残留比例：译文中非术语英文残留的比例。
- 单篇运行耗时：`run_metrics.summary.total_elapsed_ms`，用于不同流程版本的端到端性能对比。
- 阶段耗时：`run_metrics.summary.stage_elapsed_ms`，用于定位 PDF 抽取、结构分析、翻译、合并、QA 和修复中最慢的阶段。
- 翻译请求次数：`run_metrics.summary.translation_request_count`，用于估算 API 调用规模。
- HTTP 尝试与重试次数：`run_metrics.summary.http_attempt_count`、`run_metrics.summary.http_retry_count`、`experiment_metrics.performance.http_retry_count`，用于观察限流、断连和服务端错误对成本/耗时的影响。
- 可计费请求数：`cost_estimate.usage.billable_request_count` 和 `experiment_metrics.performance.billable_request_count`，用于在存在 HTTP 重试时估算固定请求成本。
- 请求/译文字符数与估算 token：`request_char_count`、`translated_char_count`、`estimated_request_token_count`、`estimated_translated_token_count`，用于接入后端价格表。
- 估算成本：`cost_estimate.summary.estimated_total_cost` 和 `experiment_metrics.performance.estimated_total_cost`，用于不同流程版本的费用对比。
- 输出可读性评分：Markdown、HTML、PDF 三类输出分别记录 1-5 分。
- 人工可读性评分：`batch_experiment_review.csv` 保留 `human_score` 总分，并用 `human_score_markdown`、`human_score_html`、`human_score_pdf`、`human_score_table_readability`、`human_score_figure_footnote_layout`、`human_score_terminology_consistency` 和 `human_score_structure_coherence` 记录 1-5 分；同时保留 `ocr_structured_table_gate_pass_rate`、`ocr_table_cell_bbox_coverage_rate`、`ocr_structured_table_gate_issues`、`ocr_structured_formula_gate_pass_rate`、`ocr_structured_formula_token_count`、`ocr_structured_formula_equation_label_count` 和 `ocr_structured_formula_gate_issues`，用于把结构化表格与公式 OCR 可信度纳入人工评审；`include_in_patent_evidence` 用于标记是否纳入专利证据，`patent_evidence_notes`、`reviewer` 和 `review_notes` 保留评审人和说明。

## 专利原型实验记录

每次实验建议保存：

```json
{
  "sample_id": "paper-001",
  "pdf_type": "normal | table-heavy | formula-heavy | multi-column | scanned",
  "pipeline_variant": "baseline | glossary | structure_chunk | table_protect | qa_repair",
  "backend": "deepseek | openai | ollama | local",
  "experiment_metrics_file": "output/experiment_metrics.json",
  "run_metrics_file": "output/run_metrics.json",
  "cost_estimate_file": "output/cost_estimate.json",
  "pages": 12,
  "chunks": 5,
  "elapsed_seconds": 0,
  "api_calls": 0,
  "estimated_request_tokens": 0,
  "estimated_response_tokens": 0,
  "estimated_cost": 0,
  "performance": {
    "total_elapsed_ms": 0,
    "translation_elapsed_ms": 0,
    "translation_request_count": 0,
    "request_char_count": 0,
    "translated_char_count": 0,
    "estimated_total_token_count": 0,
    "estimated_total_cost": 0,
    "cost_currency": "USD",
    "cost_profile_key": ""
  },
  "qa": {
    "missing_numbers": 0,
    "missing_glossary_terms": 0,
    "term_conflicts": 0,
    "table_dimension_errors": 0,
    "structure_hint_chunk_rate": 0,
    "structure_hint_avg_char_count": 0,
    "structure_hint_merged_cell_candidate_per_chunk": 0,
    "table_reconstruction_ready_rate": 0,
    "table_merged_cell_candidate_count": 0,
    "table_ragged_table_count": 0,
    "table_ragged_row_count": 0,
    "table_empty_cell_count": 0,
    "table_merged_cell_candidate_rate": 0,
    "table_ragged_table_rate": 0,
    "table_empty_cell_rate": 0,
    "table_numeric_cell_rate": 0,
    "table_caption_link_rate": 0,
    "table_footnote_binding_rate": 0,
    "table_footnote_cell_binding_rate": 0,
    "table_footnote_unbound_rate": 0,
    "table_shape_error_rate": 0,
    "table_cell_token_error_rate": 0,
    "table_locked_token_missing_rate": 0,
    "table_chain_candidate_count": 0,
    "table_chain_merged_count": 0,
    "table_chain_reject_count": 0,
    "table_chain_row_gain": 0,
    "table_chain_reject_reason_count": 0,
    "table_chain_warning_reason_count": 0,
    "table_chain_merge_rate": 0,
    "table_chain_reject_rate": 0,
    "table_chain_reject_reason_per_rejected_chain": 0,
    "table_chain_warning_reason_per_candidate_chain": 0,
    "repair_request_ready_rate": 0,
    "repair_execution_success_rate": 0,
    "repair_validation_pass_rate": 0,
    "repair_locked_token_pass_rate": 0,
    "repair_table_shape_validation_pass_rate": 0,
    "repair_merge_apply_rate": 0,
    "repair_merge_table_targeted_patch_rate": 0,
    "post_repair_issue_reduction_rate": 0,
    "table_continuation_count": 0,
    "table_footnote_count": 0,
    "entity_candidate_count": 0,
    "entity_unique_count": 0,
    "missing_entity_token_count": 0,
    "caption_or_footnote_misplaced": 0,
    "caption_orphan_count": 0,
    "footnote_orphan_count": 0,
    "relationship_warning_count": 0,
    "image_pages_detected": 0,
    "ocr_task_count": 0,
    "ocr_result_payload_count": 0,
    "ocr_invalid_result_count": 0,
    "ocr_executor_attempted_task_count": 0,
    "ocr_executor_success_rate": 0,
    "ocr_ready_task_rate": 0,
    "ocr_task_result_coverage_rate": 0,
    "ocr_result_acceptance_rate": 0,
    "ocr_writeback_apply_rate": 0,
    "page_boundary_fragments": 0,
    "page_boundary_fragment_rate": 0,
    "split_boundary_rate": 0,
    "protected_boundary_rate": 0,
    "active_split_reduction_rate_vs_baseline": 0,
    "english_residual_ratio": 0,
    "output_readability": {
      "markdown": 0,
      "html": 0,
      "pdf": 0
    }
  },
  "human_score": 0,
  "human_review": {
    "human_score_markdown": 0,
    "human_score_html": 0,
    "human_score_pdf": 0,
    "human_score_table_readability": 0,
    "human_score_figure_footnote_layout": 0,
    "human_score_terminology_consistency": 0,
    "human_score_structure_coherence": 0,
    "include_in_patent_evidence": "Y | N",
    "patent_evidence_notes": "",
    "reviewer": ""
  },
  "notes": ""
}
```

## 发布前检查清单

- README 能导航到 docs、安装说明和测试指南。
- 新增功能有最小测试或明确人工验收路径。
- 外部 API 失败时不会导致任务状态卡死。
- 管理员配置与用户端可见后端一致。
- 上传文件、输出路径、zip 打包路径都限制在任务目录。
- 完成的规划项已迁入发布历史。
- 专利原型相关实验保存配置、指标、样本类型和结果。
