# 发布历史

> **文档定位**：记录已经完成的能力、验证事实和仍需后续处理的风险。
> **适用范围**：当前仓库已有实现和后续版本发布记录。
> **最后更新**：2026-07-01
> **维护者**：项目负责人
> **关联文档**：[README](../README.md)、[更新规划](02-更新规划.md)、[版本更新状态](07-版本更新状态.md)

## 样本人工确认字段贯通（2026-07-01）

### 已完成

- `experiment-samples` 生成的 `samples.csv` 新增 `suggested_pdf_type`、`suggested_tags`、`confirmed_pdf_type`、`confirmed_tags`、`include_in_patent_batch`、`reviewer`、`review_notes`，用于把自动预分组和人工确认分开记录。
- `load_sample_metadata()` 读取样本清单时优先采用 `confirmed_pdf_type` / `confirmed_tags`，没有确认值时继续兼容旧的 `pdf_type` / `tags` / `labels` / `type` 字段。
- 批量实验 manifest、单次 record 和 `batch_experiment_review.csv` 会保留样本层面的 `include_in_patent_batch`、确认人和确认备注，便于后续导师筛选证据样本。

### 验证

- `python -m py_compile pdf_translate\experiments.py tests\test_experiments.py`
- `python -m unittest tests.test_experiments.BatchExperimentTests.test_write_sample_manifest_classifies_pdf_samples tests.test_experiments.BatchExperimentTests.test_run_batch_experiment_writes_patent_evidence_summary tests.test_experiments.BatchExperimentTests.test_sample_metadata_supports_positional_json_and_relative_tsv`

### 仍需后续

- 当前只打通人工确认字段，不自动过滤未纳入样本；真实跑批前仍需要人工按确认结果选择 PDF 入参或另行整理样本集合。

## 样本覆盖度 Markdown 报告（2026-07-01）

### 已完成

- `write_sample_manifest()` 新增可选 `markdown_path`，可把样本覆盖度、类型分布、标签分布和每篇 PDF 的关键指标写入 `samples.md`。
- `pdf-translate experiment-samples` 新增 `--markdown-report`，不传时默认生成和 CSV 同名的 Markdown 覆盖度报告。
- Markdown 报告面向导师/人工确认场景，重点展示申请前 6 类测试集的建议数量、当前数量、仍缺数量和样本关键指标。

### 验证

- `python -m py_compile pdf_translate\experiments.py pdf_translate\cli.py tests\test_experiments.py`
- `python -m unittest tests.test_experiments.BatchExperimentTests.test_write_sample_manifest_classifies_pdf_samples`
- `python -m pdf_translate.cli experiment-samples --help`

### 仍需后续

- 后续真实样本跑批时，需要让人工在 Markdown 报告基础上确认样本分类，再进入批量实验和评分表录入。

## 样本覆盖度与注释实体分类（2026-07-01）

### 已完成

- `experiment-samples` 样本分析新增图注/脚注线索、作者注释线索和实体候选统计，复用 DocumentIR 的实体候选规则识别机构、人名、模型名、数据集名和缩写。
- 样本标签新增 `annotation` 与 `entity`，在没有更高优先级类型时可归为 `annotation-entity-heavy`，用于覆盖用户前面提到的注释、人名、机构等可读性问题。
- `samples.json.summary.coverage` 新增申请前测试集覆盖度 checklist，按普通、表格密集、公式密集、多栏复杂、扫描/低文本、注释/实体密集 6 类给出建议数量、当前数量、缺口和 `ready_for_patent_batch` 状态。
- `pdf-translate experiment-samples` 会在命令行输出覆盖建议达成数量和仍需补充的样本类型，方便真实跑批前先做样本集整理。

### 验证

- `python -m py_compile pdf_translate\experiments.py pdf_translate\cli.py tests\test_experiments.py`
- `python -m unittest tests.test_experiments.BatchExperimentTests.test_write_sample_manifest_classifies_pdf_samples`

### 仍需后续

- 当前覆盖度是申请前样本准备的启发式提醒，不等同于导师确认或专利代理可直接采信的样本标注；真实证据集仍需要人工确认样本类别、录入评分并保留代表性样本说明。

## 批量实验样本清单生成（2026-07-01）

### 已完成

- 新增 `experiment-sample-manifest-v1` 样本清单分析报告格式，可把真实论文 PDF 预分组为普通、表格密集、公式密集、多栏复杂、扫描/低文本等样本类型。
- 新增 `pdf-translate experiment-samples` CLI 命令，支持输入 PDF 文件或目录，输出可直接传给 `pdf-translate experiment --sample-manifest` 的 `samples.csv`，并生成同名 JSON 分析报告。
- 样本预分组会检查页数、抽样页文本量、低文本页、图片页、表格关键词、数字密集行、公式符号和多栏块分布，帮助后续真实样本跑批前快速筛选测试集。
- `tests/test_experiments.py` 新增样本清单分类测试，覆盖表格密集 PDF 与低文本扫描型 PDF 的启发式分类和 `load_sample_metadata()` 回读。

### 验证

- `python -m py_compile pdf_translate\experiments.py pdf_translate\cli.py tests\test_experiments.py`
- `python -m unittest tests.test_experiments.BatchExperimentTests.test_write_sample_manifest_classifies_pdf_samples tests.test_experiments.BatchExperimentTests.test_run_batch_experiment_writes_patent_evidence_summary`
- `python -m pdf_translate.cli experiment-samples --help`

### 仍需后续

- 当前样本分类是跑批前的启发式预分组，不等同于正式人工标注；真实专利证据集仍需人工确认样本类型、录入评分，并与导师口径对齐。

## 批量实验人工评分证据摘要（2026-07-01）

### 已完成

- 新增 `batch-experiment-evidence-v1` 证据摘要格式，可读取 `batch_experiment_summary.json` 与填写后的 `batch_experiment_review.csv`，生成 `batch_experiment_evidence.json` 和 `batch_experiment_evidence.md`。
- 新增 `pdf-translate experiment-evidence` CLI 命令，支持 `--summary-json`、`--review-csv`、`--output-dir` 与 `--require-selected`，用于在导师/人工评分后导出专利证据候选清单。
- 证据摘要会聚合总分、Markdown/HTML/PDF 可读性、表格可读性、图文脚注排版、术语一致性、结构连贯性、OCR 结构门禁通过率、bbox 覆盖率和门禁问题分布。
- 入选证据只读取 `include_in_patent_evidence` 显式采纳的成功运行；失败运行会保留到 `run_failures`，避免误纳入专利效果证据。
- 每条证据候选保留 `evidence_id`、样本元数据、策略、人工评分、专利证据说明、关键自动指标、OCR 结构门禁摘要和译文 PDF/HTML/指标文件路径。

### 验证

- `python -m py_compile pdf_translate\experiments.py pdf_translate\cli.py tests\test_experiments.py`
- `python -m unittest tests.test_experiments.BatchExperimentTests.test_run_batch_experiment_writes_patent_evidence_summary`
- `python -m pdf_translate.cli experiment-evidence --help`

### 仍需后续

- 当前解决的是“评分表回读并汇总成证据摘要”，真实论文样本仍需实际跑批、人工评分和导师筛选；证据摘要不是法律交底书本身，后续仍需专利代理师整理权利要求。

## 批量实验 OCR 结构门禁指标汇总（2026-07-01）

### 已完成

- `batch_experiment_summary.json` 现在会汇总表格 OCR 结构契约、表格上下文、结构化结果候选、结构化表格门禁通过/复核/阻断、locked token 缺失、行列不匹配、cell bbox 缺失和 bbox 覆盖率等指标。
- `batch_experiment_summary.md` 新增 `OCR structured table gate` 均值表，用于快速查看不同策略下结构化表格 OCR 候选数、门禁通过率、复核数、bbox 覆盖率和门禁问题分布。
- `batch_experiment_review.csv` 新增 `ocr_structured_table_gate_pass_rate`、`ocr_table_cell_bbox_coverage_rate`、`ocr_structured_table_gate_issues` 等列，方便导师或人工评审把结构化 OCR 质量直接纳入专利证据筛选。
- `COMPARISON_FIELDS` 新增 OCR 结构门禁通过率、bbox 覆盖率和复核数差值，批量实验可比较不同策略相对基线的表格 OCR 结构证据变化。
- `ocr_structured_table_row_col_match_rate` 和 `ocr_table_cell_bbox_coverage_rate` 增加边界钳制，避免复合错误把匹配率扣成负数或 bbox 覆盖率超过 100%。

### 验证

- `python -m py_compile pdf_translate\experiments.py pdf_translate\qa\metrics.py tests\test_experiments.py`
- `python -m unittest tests.test_experiments.BatchExperimentTests.test_run_batch_experiment_writes_patent_evidence_summary`

### 仍需后续

- 当前完成的是批量证据汇总层，不等于真实高精度表格 OCR 引擎已经接入；后续仍需用真实表格密集、扫描和图片型表格论文跑批，并录入人工评分。

## 结构化表格 OCR 本地门禁（2026-07-01）

### 已完成

- `ocr_candidate_qa` 新增结构化表格 OCR 本地门禁：对 `target_structure_type=table` 的候选检查 `structured_cells` 是否非空、单元格行列坐标是否可解析、行列范围是否匹配 `table_context`、`cell_bboxes` 是否覆盖、`locked_tokens` 是否能在单元格文本中找到。
- 结构化表格 OCR 候选通过门禁时不再自动追加 `needs_table_structure_review`，可进入候选晋级；缺结构、缺 bbox、缺 locked token、行列不一致、低置信或引擎 warning 仍会进入复核或阻断。
- `ocr_candidate_qa.json` 新增 `structured_table_gate_counts`、`structured_table_gate_issue_counts`、`structured_table_gate_passed_count`、`structured_table_gate_review_count`、`structured_table_missing_locked_token_count` 等摘要字段。
- `experiment_metrics.json` 新增 `ocr_structured_table_gate_passed_count`、`ocr_structured_table_gate_review_count`、`ocr_structured_table_missing_locked_token_count`、`ocr_structured_table_gate_pass_rate`、`ocr_structured_table_structure_review_rate` 和 `ocr_table_cell_bbox_coverage_rate` 等指标。

### 验证

- `python -m py_compile pdf_translate\qa\ocr_candidates.py pdf_translate\qa\metrics.py tests\test_structure_ir.py`
- `python -m unittest tests.test_structure_ir.StructureIRTests.test_table_ocr_writeback_preserves_structure_context tests.test_structure_ir.StructureIRTests.test_structured_table_ocr_gate_requires_locked_tokens_and_bboxes tests.test_structure_ir.StructureIRTests.test_experiment_metrics_aggregates_quality_evidence`，3 个测试通过。

### 仍需后续

- 当前门禁仍是本地规则校验，不等于真实高精度表格 OCR 引擎；后续需要接入能自动生成稳定 `structured_cells` / `cell_bboxes` 的 OCR/版面分析器，并实现单元格级正式晋级和人工确认。

## 表格 OCR 结构化结果透传（2026-07-01）

### 已完成

- `ocr-results-v1` 结果中若包含 `structured_cells`、`cell_bboxes`、`merged_cell_candidates` 或 `table_footnotes`，`ocr_writeback` 会把这些结构化字段写入 `document_ir_ocr.json` 的 OCR 候选。
- `ocr_candidate_qa.json` 会保留上述结构字段，并新增结构化结果候选数、结构化单元格数、cell bbox 数、OCR 合并候选数和 OCR 表格脚注数等汇总字段。
- `ocr_candidate_promotion.json` 和晋级 IR 的 promotion meta 会保留结构化 OCR 结果，供后续单元格级晋级、人工确认或表格修复使用。
- `experiment_metrics.json` 新增 `ocr_structured_result_writeback_count`、`ocr_structured_result_candidate_count`、`ocr_structured_cell_count`、`ocr_cell_bbox_count`、`ocr_result_merged_cell_candidate_count`、`ocr_result_table_footnote_count`、`ocr_structured_result_writeback_rate` 和 `ocr_structured_result_candidate_rate` 等指标。

### 验证

- `python -m py_compile pdf_translate\vision\ocr_writeback.py pdf_translate\qa\ocr_candidates.py pdf_translate\vision\ocr_promotion.py pdf_translate\qa\metrics.py tests\test_structure_ir.py`
- `python -m unittest tests.test_structure_ir.StructureIRTests.test_table_ocr_writeback_preserves_structure_context tests.test_structure_ir.StructureIRTests.test_experiment_metrics_aggregates_quality_evidence`，2 个测试通过。

### 仍需后续

- 当前完成的是结构化结果透传与统计，尚未自动生成真实 `structured_cells` / `cell_bboxes`；后续仍需接入高精度表格 OCR/版面分析器，并实现单元格级正式晋级和真实扫描样本校准。

## 表格 OCR 结构化任务契约（2026-07-01）

### 已完成

- `ocr_tasks.json` 的表格区域任务新增 `layout_scope=table_region`、`target_structure_type=table`、`table_context`、`structure_contract` 和 `writeback.subtarget`，明确表格 OCR 需要保留源表格 ID、页码、bbox、行列数、表头、数字 token、locked token 与可选结构化返回字段。
- `ocr_writeback.json`、`document_ir_ocr.json`、`ocr_candidate_qa.json` 和 `ocr_candidate_promotion.json` 会透传表格上下文、子目标和结构契约，当前仍保持 append-only 候选写回，不替换源文本。
- `experiment_metrics.json` 新增 `ocr_structured_contract_task_count`、`ocr_table_context_task_count`、`ocr_table_context_ready_task_count`、`ocr_table_context_writeback_count`、`ocr_table_context_candidate_count`、`ocr_structured_contract_task_rate` 和 `ocr_task_structure_target_counts` 等指标。

### 验证

- `python -m py_compile pdf_translate\vision\ocr_tasks.py pdf_translate\vision\ocr_writeback.py pdf_translate\qa\ocr_candidates.py pdf_translate\vision\ocr_promotion.py pdf_translate\qa\metrics.py tests\test_structure_ir.py`
- `python -m unittest tests.test_structure_ir.StructureIRTests.test_ocr_task_manifest_schedules_region_crops_for_writeback tests.test_structure_ir.StructureIRTests.test_table_ocr_writeback_preserves_structure_context tests.test_structure_ir.StructureIRTests.test_experiment_metrics_aggregates_quality_evidence`，3 个测试通过。

### 仍需后续

- 真实高精度表格 OCR 引擎、`structured_cells` / `cell_bboxes` 生成、单元格级晋级和真实扫描/图片型表格样本校准仍需后续推进。

## 结构化 PDF 结构关系输出审计（2026-07-01）

### 已完成

- `write_translated_pdf()` 新增 `structure_qa` 与 `table_reconstruction` 输入，`translated_pdf_report.json` 会记录 caption/footnote 关系、跨页归位、表格脚注单元格绑定、续表组、合并单元格候选和结构关系输出证据。
- 结构化 PDF 正文会按 chunk 写入 `Structure context` 证据框，方便人工审阅时看到当前译文块关联了哪些源表格、图注/脚注、续表组和受保护结构关系。
- `experiment_metrics.json` 新增 `translated_pdf_structure_context_chunk_count`、`translated_pdf_source_table_reference_count`、`translated_pdf_source_footnote_cell_binding_count`、`translated_pdf_structural_relation_reference_count` 与 `translated_pdf_structure_context_chunk_rate` 等指标。
- `tests/test_structure_ir.py` 已覆盖 PDF 文本可抽取的结构证据、报告字段和实验指标聚合。

### 验证

- `python -m py_compile pdf_translate\exporters\translated_pdf.py pdf_translate\pipeline.py pdf_translate\qa\metrics.py tests\test_structure_ir.py`
- `python -m unittest tests.test_structure_ir.StructureIRTests.test_translated_pdf_exporter_writes_readable_pdf_and_report tests.test_structure_ir.StructureIRTests.test_experiment_metrics_aggregates_quality_evidence tests.test_structure_ir.StructureIRTests.test_pipeline_writes_document_ir_and_structure_manifest`，3 个测试通过。

### 仍需后续

- 当前仍是结构化 PDF 译文重排和证据展示，不是原 PDF 坐标回填；复杂图表原位重排、页码关系精修和真实样本人工可读性评分仍需继续推进。

## 批量实验多维人工评分表（2026-07-01）

### 已完成

- `batch_experiment_review.csv` 在保留 `human_score`、`reviewer` 和 `review_notes` 的基础上，新增 Markdown、HTML、PDF、表格可读性、图文脚注排版、术语一致性和结构连贯性等人工评分列。
- 新增 `include_in_patent_evidence` 与 `patent_evidence_notes`，便于导师评审后标记哪些样本/策略组合可纳入专利交底证据。
- `tests/test_experiments.py` 已覆盖新增评分列，避免后续批量实验表结构回退。

### 验证

- `python -m unittest tests.test_experiments.BatchExperimentTests.test_run_batch_experiment_writes_patent_evidence_summary`

### 仍需后续

- 仍需按导师口径准备真实样本集，实际录入人工评分，并把评分结果与结构指标、成本和耗时一起归档。

## 结构化 PDF 译文前端入口（2026-07-01）

### 已完成

- 用户端当前任务卡片新增可下载产物摘要，能显示原文 PDF、译文 MD、译文 PDF、双语 HTML 和资料包 ZIP 的可用状态。
- 用户端新增“译文 PDF”下载按钮，调用 `/api/jobs/{job_id}/download/translated.pdf`，未生成时按钮保持禁用。
- 管理端任务表新增可用产物摘要，并把“原PDF”和“译PDF”下载按钮分开；译文 PDF 使用 `kind=output_pdf` 下载。
- 已重新构建 `pdf_translate/server/static/`，Web 交付物包含本次前端入口变更。

### 验证

- `npm run build`

### 仍需后续

- PDF 本身仍是结构化译文重排原型；复杂图表、脚注、多栏页码关系和可读性评分入口需要后续真实样本验证。

## 结构化 PDF 译文输出原型（2026-07-01）

### 已完成

- 新增 `pdf_translate.exporters.translated_pdf`，基于现有翻译 chunk 重新排版生成结构化 PDF 译文，支持标题、段落、Markdown 表格、QA 问题和局部修复建议的基础展示。
- 管线会生成 `output/translated_full.pdf` 和 `output/translated_pdf_report.json`；报告记录生成状态、页数、chunk 数、译文字符数、表格数、QA issue 数、修复项数和 warning 数。
- `experiment_metrics.json` 新增 `translated_pdf_*` 质量字段，并在 evidence files 中回链 PDF 译文和 PDF 生成报告；批量实验记录和人工评分 CSV 也会保存 `translated_pdf` 路径。
- zip 资料包新增 `译文/结构化译文.pdf` 与 `质量/PDF译文生成报告.json`；Web 用户端和管理端新增直接下载 `translated_full.pdf` 的服务端接口。
- Web 任务产物摘要新增 `translated_pdf_ready` 和 `translated_pdf_bytes`，完成态缺少 PDF 时仅给出兼容 warning，不把历史任务误判为失败。

### 验证

- `python -m py_compile pdf_translate\exporters\translated_pdf.py pdf_translate\exporters\__init__.py pdf_translate\pipeline.py pdf_translate\qa\metrics.py pdf_translate\zip_bundle.py pdf_translate\server\jobs.py pdf_translate\server\routes_web.py pdf_translate\server\database.py pdf_translate\experiments.py tests\test_structure_ir.py tests\test_experiments.py tests\test_job_status.py tests\test_zip_bundle_security.py`
- `python -m unittest tests.test_structure_ir.StructureIRTests.test_translated_pdf_exporter_writes_readable_pdf_and_report tests.test_structure_ir.StructureIRTests.test_pipeline_writes_document_ir_and_structure_manifest tests.test_structure_ir.StructureIRTests.test_experiment_metrics_aggregates_quality_evidence tests.test_structure_ir.StructureIRTests.test_bundle_includes_structure_qa_and_repair_artifacts tests.test_experiments.BatchExperimentTests.test_run_batch_experiment_writes_patent_evidence_summary tests.test_job_status.JobStatusSnapshotTests.test_merge_status_into_rows_preserves_database_created_at tests.test_job_status.JobStatusSnapshotTests.test_artifact_summary_marks_done_without_translation_inconsistent tests.test_job_status.JobStatusSnapshotTests.test_merge_status_into_rows_marks_missing_runtime_status tests.test_zip_bundle_security.ZipBundleSecurityTests.test_iter_bundle_files_keeps_regular_files`，9 个测试通过。

### 仍需后续

- 当前是结构化译文重排，不是原 PDF 坐标回填；复杂图表、脚注、多栏页码关系和原版式对齐仍需真实样本校准。
- 后续还需补 PDF 可读性评审入口和复杂样本人工评分。

## 续表跨页边界受控保护（2026-07-01）

### 已完成

- `structure_qa.json.page_boundary_fragments[]` 新增 `continuation_kind`、`stitch_action`、`stitch_confidence`、`joiner` 与 `merged_preview`，把跨页段落和续表边界转成可审计的续接候选。
- `build_structure_chunks()` 在保护续表跨页边界时，会写入 `protected_table_continuation:<boundary>` warning，并在 chunk 文本前注入“结构续接提示”，提醒翻译阶段不要把页码边界当作自然段落边界。
- `chunk_boundary_qa.json` 新增 `table_continuation_boundary_count`、`table_continuation_protected_count`、`table_continuation_split_count`、`table_continuation_*_rate` 等字段，用于区分普通跨页断句和续表跨页切开。
- `chunk_strategy_comparison.json` 新增固定页分段与结构分段之间的续表切开降幅，便于直接观察结构分段对表格跨页碎片的改善。
- `experiment_metrics.json` 新增续接类型 breakdown、续接动作 breakdown、续表边界保护率和续表切开降幅，可进入批量专利实验摘要。

### 验证

- `python -m py_compile pdf_translate\structure_boundaries.py pdf_translate\chunkers\structure.py pdf_translate\qa\structure.py pdf_translate\qa\chunk_boundary.py pdf_translate\qa\metrics.py tests\test_structure_ir.py`
- `python -m unittest tests.test_structure_ir.StructureIRTests.test_structure_qa_reports_page_boundary_fragments tests.test_structure_ir.StructureIRTests.test_structure_chunks_protect_page_boundary_fragments tests.test_structure_ir.StructureIRTests.test_structure_chunks_prioritize_table_continuation_boundary tests.test_structure_ir.StructureIRTests.test_chunk_boundary_qa_reports_split_and_protected_fragments tests.test_structure_ir.StructureIRTests.test_experiment_metrics_aggregates_quality_evidence`，5 个测试通过。

### 仍需后续

- 当前只对“相邻页面尾块和首页块均为表格”的强续表信号做分段保护；复杂多栏续表、跨页表题和图片型表格仍需真实样本校准。
- 后续需要把续表保护效果纳入真实论文跑批和人工评分，验证表格可读性提升是否稳定。

## 管理端密钥加密存储基础（2026-07-01）

### 已完成

- 新增 `pdf_translate.server.secrets_store`，支持使用 `PDF_TRANSLATE_SECRET_KEY` 或 `PDF_TRANSLATE_SECRET_KEY_FILE` 对管理端保存的 API Key 做本地加密。
- 管理端新保存的 API Key 在密钥已配置时会以 `enc:v1:` 前缀密文写入 SQLite；未配置本地加密密钥时仍保持旧行为，便于本地开发。
- 旧版 SQLite 明文 API Key 保持兼容读取，不做自动改写；安全预检会提示管理员设置加密密钥后重新保存。
- `security-preflight-v1` 新增 API Key 明文/密文数量、密文字段名、可解密/不可解密字段名和待重新加密数量；缺少或错误的本地加密密钥会进入风险清单，但报告不输出任何密钥值。
- 新增 `tests/test_secret_store.py`，并扩展 `tests/test_security_preflight.py`，覆盖密钥加密写入、旧明文兼容、缺失/错误加密密钥预警和报告不泄露密钥值。

### 验证

- `python -m py_compile pdf_translate\server\secrets_store.py pdf_translate\server\settings_service.py pdf_translate\server\security_preflight.py tests\test_secret_store.py tests\test_security_preflight.py`
- `python -m unittest tests.test_secret_store tests.test_security_preflight`，16 个测试通过。

### 仍需后续

- SEC-01 仍是部分完成：还需要补齐部署验收、真实公网环境回归，以及面向正式运维的外部密钥托管方案。

## JWT 过期与生产启动门禁（2026-07-01）

### 已完成

- 登录 token 新增 `iat` 与 `exp` 声明，有效期由 `PDF_TRANSLATE_JWT_TTL_MINUTES` 控制，默认 720 分钟。
- `decode_token()` 要求 token 必须包含 `exp/sub/username/role`，过期 token 和缺少 `exp` 的历史 token 会返回 401。
- `security-preflight-v1` 新增 `jwt` 配置摘要，并检查非法或默认 JWT TTL。
- 新增 `assert_production_security_ready()`；当 `PDF_TRANSLATE_ENV` / `PDF_TRANSLATE_DEPLOYMENT_MODE` 为生产类值时，Web app 初始化会阻断高/中风险配置。
- 新增 `tests/test_auth_deps.py`，并扩展 `tests/test_security_preflight.py`，覆盖 JWT 过期、缺 `exp` 拒绝、TTL 配置和生产启动门禁。

### 验证

- `python -m py_compile pdf_translate\server\auth_deps.py pdf_translate\server\security_preflight.py pdf_translate\server\app.py tests\test_auth_deps.py tests\test_security_preflight.py`
- `python -m unittest tests.test_auth_deps tests.test_security_preflight`，14 个测试通过。

### 仍需后续

- 后续“管理端密钥加密存储基础”已补齐本地加密落盘；SEC-01 仍需继续推进部署验收和真实公网环境回归。

## 上传与资料包边界加固（2026-07-01）

### 已完成

- Web 上传保存从一次性 `file.read()` 改为流式读取，先校验 `%PDF-` 魔数，再按块写入 `input.pdf`。
- 上传超出 `PDF_TRANSLATE_MAX_UPLOAD_MB` 时，会删除已写出的半截文件，并由任务创建流程清理刚创建的任务目录。
- 上传审计日志新增 `upload_bytes` 与 `upload_limit_mb`，便于排查异常上传和生产安全问题。
- zip 资料包固定候选文件和目录遍历都跳过 symlink，并要求 `resolve()` 后仍位于任务工作目录内，避免符号链接把外部文件带入资料包。
- 新增 `tests/test_web_upload_security.py` 和 `tests/test_zip_bundle_security.py`，覆盖上传魔数、流式读取、超限清理、普通资料包文件保留和 symlink 外链跳过。

### 验证

- `python -m py_compile pdf_translate\server\routes_web.py pdf_translate\zip_bundle.py tests\test_web_upload_security.py tests\test_zip_bundle_security.py`
- `python -m unittest tests.test_web_upload_security tests.test_zip_bundle_security tests.test_security_preflight tests.test_structure_ir.StructureIRTests.test_bundle_includes_structure_qa_and_repair_artifacts`，12 个测试通过，1 个 symlink 权限相关用例按预期跳过。

### 仍需后续

- 后续“JWT 过期与生产启动门禁”已补齐 JWT 过期时间和生产模式启动强制门禁；后续“管理端密钥加密存储基础”已补齐本地加密落盘；部署验收仍需继续推进。

## 生产安全预检与上传限制配置（2026-07-01）
### 已完成
- 新增 `pdf_translate.server.security_preflight`，统一生成 `security-preflight-v1` 报告，覆盖管理员引导密码、CORS、JWT secret、上传上限、数据目录、Web 任务目录和本地 SQLite API Key 存储提示。
- 新增 CLI `pdf-translate security-check`，可在不开 Web 的情况下输出同一份生产安全预检 JSON。
- 新增管理端接口 `/api/admin/security/preflight`，供管理员在部署前查看同源风险清单。
- Web 上传大小从硬编码 120MB 改为 `PDF_TRANSLATE_MAX_UPLOAD_MB`，非法配置会回退默认值并进入预检报告；超限任务会清理刚创建的任务目录，减少孤儿目录。
- 管理端设置快照不再回传已保存的 API Key 明文，只返回 `secret_fields` 配置状态；保存时继续保持空值不覆盖已有密钥。
### 验证

- `python -m py_compile pdf_translate\server\security_preflight.py pdf_translate\server\app.py pdf_translate\server\routes_web.py pdf_translate\server\settings_service.py pdf_translate\cli.py`
- `python -m unittest tests.test_security_preflight`，6 个测试通过。
### 仍需后续

- 后续“上传与资料包边界加固”已补齐上传流式读取、PDF 魔数校验和 zip symlink 边界；后续“JWT 过期与生产启动门禁”已补齐 JWT 过期时间和生产模式启动强制门禁；后续“管理端密钥加密存储基础”已补齐本地加密落盘；部署验收仍需继续推进。

## CLI/Web 状态诊断口径统一（2026-07-01）

### 已完成

- `JobRegistry` 新增恢复报告，记录启动时扫描目录数、恢复任务数、缺失状态快照、无效 JSON 和快照 `job_id` 与目录不匹配等情况。
- 任务详情、用户/管理员任务列表和 CLI 现在复用同一套运行态、产物摘要、管线 `state.json` 诊断和恢复 warning。
- `pipeline_state_fields_for_record()` 会读取 `output/state.json`、`output/chunks_manifest.json` 和 `output/chunks/`，输出已完成 chunk、待处理 chunk、续跑可用性、恢复状态和缺失 chunk 文件 warning。
- 新增 `pdf-translate web-status`，可在不启动 Web 的情况下读取 `web_status.json` 与管线 state 诊断，方便脚本侧核对 Web 任务状态。
- 新增 `/api/admin/jobs/hydration-report`，用于管理员查看启动恢复报告和数据库/磁盘漂移摘要。

### 验证

- `python -m py_compile pdf_translate\server\jobs.py pdf_translate\server\routes_web.py pdf_translate\cli.py tests\test_job_status.py`
- `python -m unittest tests.test_job_status`，10 个测试通过。

### 仍需后续

- JOB-01 仍是部分完成：创建、取消、完成、失败的端到端恢复审计和自动恢复策略还需要继续推进。

## 任务存储巡检与孤儿清理（2026-07-01）
### 已完成
- `JobRegistry.storage_drift()` 可对比 SQLite 任务索引和 `web_jobs/` 工作目录，识别数据库缺目录、目录缺数据库索引和运行中任务。
- 管理员新增 `/api/admin/jobs/reconcile`，默认只巡检；传入 `apply=true` 时才删除数据库缺目录记录和未索引工作目录，并跳过运行中任务。
- `remove_job()` 增加 job_id 路径边界保护，避免清理历史异常数据时通过路径穿越删除任务根目录外的内容。
- 管理端“任务与文件”页新增任务存储巡检与孤儿清理入口，清理前会二次确认，清理后刷新任务列表。

### 验证

- `python -m py_compile pdf_translate\server\jobs.py pdf_translate\server\database.py pdf_translate\server\routes_web.py tests\test_job_status.py`
- `python -m unittest tests.test_job_status`：7 个测试通过。
- `npm run build`

### 仍需后续

- JOB-01 仍是部分完成：创建、取消、完成、失败的端到端恢复审计和自动恢复策略还需要继续推进。

## 任务产物一致性摘要（2026-07-01）
### 已完成
- `JobRegistry` 新增统一产物摘要，暴露原 PDF、译文 Markdown、双语 HTML、zip 打包可用性、产物一致性状态和 warning。
- 用户任务列表、管理员任务列表和单任务详情复用同一份产物摘要，减少列表状态与下载接口不一致。
- 管理端任务表新增产物状态列，并按产物摘要禁用不可下载的 PDF、MD、ZIP 按钮。
- 管理端 zip 下载接口补齐空译文检查，避免状态完成但译文缺失时生成无意义打包文件。

### 验证

- `python -m py_compile pdf_translate\server\jobs.py pdf_translate\server\routes_web.py tests\test_job_status.py`
- `python -m unittest tests.test_job_status`：5 个测试通过。
- `npm run build`

### 仍需后续

- JOB-01 仍是部分完成：创建、取消、完成、失败的端到端恢复审计和自动恢复策略还需要继续推进。

## 任务状态主源最小收口（2026-07-01）
### 已完成
- `JobRecord` 新增统一状态快照序列化与恢复入口，`web_status.json` 写入 `schema_version: web-job-status-v1`。
- `JobRegistry._persist()` 改为临时文件加 `os.replace()` 原子更新，降低状态文件半写入风险。
- `hydrate_from_disk()` 统一使用状态快照恢复内存任务，并以状态文件所在目录作为 `work_dir`，不信任 JSON 内可被篡改的路径字段。
- `/api/user/jobs` 和 `/api/admin/jobs` 会通过 `merge_status_into_rows()` 合并运行态字段；SQLite `created_at` 继续作为列表时间保留。
- 开发者文档、更新规划和版本更新状态已同步 JOB-01 当前边界。

### 验证

- `python -m py_compile pdf_translate\server\jobs.py pdf_translate\server\routes_web.py tests\test_job_status.py`
- `python -m unittest tests.test_job_status`：4 个测试通过。

### 仍需后续

- JOB-01 仍是部分完成：创建、取消、完成、失败的端到端恢复审计和自动恢复策略还需要继续推进。

## 统一后端能力注册表（2026-07-01）
### 已完成
- 新增 `pdf_translate/translators/registry.py`，统一声明翻译后端 id、展示名称、别名、配置字段、默认启用状态、自定义 API 支持、段尾顺延能力和能力标签。
- `build_translator()`、CLI help、成本模型名、管线段尾顺延判断、`settings_service`、用户后端列表和管理端后端勾选均改为读取同一份 registry。
- 管理端 `enabled_backends` 会归一化别名并拒绝未知后端；用户端自定义 API 后端由服务端返回，可覆盖 OpenAI 兼容、DeepSeek、Ollama 和 DeepL。
- `ARCH-01` 从更新规划迁入版本更新状态的已完成任务归档。

### 验证

- `python -m py_compile pdf_translate\translators\registry.py pdf_translate\translators\factory.py pdf_translate\server\settings_service.py pdf_translate\server\routes_web.py pdf_translate\costing.py pdf_translate\pipeline.py pdf_translate\cli.py tests\test_backend_registry.py`
- `python -m unittest tests.test_backend_registry`
- `python -m unittest discover -s tests`：49 个测试通过。

### 仍需后续

- 生产安全配置和真实后端连通性验收仍按后续规划推进。

## 版本更新状态文档拆分（2026-06-30）
### 已完成
- 新增 `docs/07-版本更新状态.md`，集中记录当前完成度、版本状态总表、已完成任务归档和下一步缺口。
- 重写 `docs/02-更新规划.md`，移出已完成任务和历史流水账，只保留未来规划、优先级任务和验收方式。
- README 与方向总览已补充新文档入口，并明确规划文档、发布历史和版本状态文档的边界。

### 验证

- 文档索引已可从 README 和 `docs/00-方向总览.md` 进入。
- 本次只调整文档结构，不改变代码执行路径。

### 仍需后续

- 后续每完成一项规划任务，需要同时更新版本更新状态和发布历史，避免完成事实重新堆回规划文档。

## 表格修复目标定位证据（2026-06-30）
### 已完成
- `table_cell_token_mismatch` 的单元格证据新增 `source_table_shape`，表格单元格级候选修复现在会同时验证锁定 token 与源表格行列形状。
- `repair_requests.json.requests[].merge_target` 新增目标 `table_index`、表格 ID、目标单元格数量和缺失 token 摘要；`repair_validation.json` 会透传该定位证据。
- `repair_merge.json` 对 `repair_table_cell_tokens` / `repair_table_shape` 优先采用 `replace_markdown_table_by_evidence`，按 QA 证据中的目标表格索引替换对应 Markdown 表格；定位缺失时才回退首表替换。
- `repair_merge.json.summary` 新增 `table_targeted_patch_count`、`strategy_counts` 和 `applied_strategy_counts`；`experiment_metrics.json` 与批量实验摘要新增 `repair_merge_table_targeted_patch_count`、`repair_merge_table_targeted_patch_rate`、`repair_merge_strategy_counts` 和 `repair_merge_applied_strategy_counts`。
- 开发者文档、更新规划、专利优化方向和测试验收指南已同步 REPAIR-01 当前状态。

### 验证

- `python -m py_compile pdf_translate\qa\repair.py pdf_translate\qa\translation.py pdf_translate\qa\metrics.py pdf_translate\experiments.py tests\test_structure_ir.py tests\test_experiments.py`
- `python -m unittest tests.test_structure_ir.StructureIRTests.test_translation_qa_reports_table_cell_token_mismatch tests.test_structure_ir.StructureIRTests.test_repair_merge_targets_markdown_table_from_cell_evidence tests.test_structure_ir.StructureIRTests.test_experiment_metrics_aggregates_quality_evidence tests.test_experiments.BatchExperimentTests.test_run_batch_experiment_writes_patent_evidence_summary`
- `python -m unittest discover -s tests`：45 个测试通过。

### 仍需后续

- 当前仍写入独立修复副本，不覆盖正式译文 chunk；人工确认、失败回滚和正式发布合并策略仍待实现。
- 当前表格类修复以整张目标 Markdown 表格替换为主，尚未实现单元格级原位编辑、同 chunk 多补丁串行合并和真实模型效果对比。

## 结构提示清单证据（2026-06-30）
### 已完成
- 新增 `output/structure_hints_manifest.json`，逐 chunk 记录实际用于翻译请求的结构保护提示文本、页码、块 ID、命中表格 ID、续表组 ID、提示长度、脚注绑定数、锁定 token 数和合并单元格候选类型/原因分布。
- 串行与并行翻译路径统一从 `structure_hints_manifest.json` 复用提示文本；清单缺失时才回退到即时构建，减少提示审计与真实请求不一致的风险。
- `experiment_metrics.json` 新增结构提示覆盖率、空提示 chunk 数、提示总长度/平均长度/最大长度、提示内表格数、续表组数、脚注绑定数、锁定 token 数以及提示内合并候选 breakdowns。
- 批量实验摘要新增结构提示相关质量字段、比例字段和 breakdowns；资料包会把 `output/structure_hints_manifest.json` 映射为 `设置/结构提示清单.json`。
- 开发者文档、更新规划、专利优化方向和测试验收指南已同步本轮 TABLE-01 状态。

### 验证

- `python -m py_compile pdf_translate\qa\table_reconstruction.py pdf_translate\pipeline.py pdf_translate\qa\metrics.py pdf_translate\experiments.py pdf_translate\zip_bundle.py`
- `python -m unittest tests.test_structure_ir.StructureIRTests.test_table_reconstruction_reports_merged_cell_candidates tests.test_structure_ir.StructureIRTests.test_experiment_metrics_aggregates_quality_evidence tests.test_structure_ir.StructureIRTests.test_bundle_includes_structure_qa_and_repair_artifacts`
- `python -m unittest tests.test_structure_ir.StructureIRTests.test_pipeline_writes_document_ir_and_structure_manifest`
- `python -m unittest tests.test_experiments.BatchExperimentTests.test_run_batch_experiment_writes_patent_evidence_summary`
- `python -m unittest discover -s tests`：44 个测试通过。

### 仍需后续

- 需要在真实表格密集论文上比较结构提示覆盖率、表格形状异常率和表格锁定 token 缺失率，验证提示注入是否带来可观察改进。
- 当前清单证明“结构证据进入翻译控制”，但不等同于高精度表格重排；PDF 译文输出、真实模型效果对比和人工评分仍需继续推进。

## 合并单元格候选提示注入（2026-06-30）
### 已完成
- `build_table_translation_hints()` 现在会把当前 chunk 命中的疑似合并单元格候选压缩进「结构保护提示」。
- 单表提示会输出 `疑似合并单元格候选（未确认，仅作结构保护提示，不作为已确认合并结构处理）`，并保留候选坐标、`rowspan` / `colspan` 类型、span 尺寸、候选原因、置信度、锚文本和候选覆盖空位摘要。
- 续表组提示会输出组内候选数量和来源表格 ID，使跨页表格链中的候选风险能随续表结构一起进入翻译阶段。
- 每表默认最多提示 3 个候选，避免结构提示过长；候选仍被明确标注为未确认风险，不把它等同于真实合并单元格识别或 PDF 重排。
- OpenAI/DeepSeek 兼容提示词版本从 `v4` 升级到 `v5`，系统提示明确要求模型只把疑似合并单元格候选当作结构风险处理，不自行声称已确认合并或重排。

### 验证

- `python -m py_compile pdf_translate\qa\table_reconstruction.py pdf_translate\translators\openai_compatible.py tests\test_structure_ir.py`
- `python -m unittest tests.test_structure_ir.StructureIRTests.test_table_reconstruction_report_builds_cell_contexts tests.test_structure_ir.StructureIRTests.test_table_reconstruction_reports_merged_cell_candidates tests.test_structure_ir.StructureIRTests.test_table_reconstruction_builds_continued_table_groups tests.test_structure_ir.StructureIRTests.test_table_reconstruction_rejects_incompatible_continued_table_groups tests.test_structure_ir.StructureIRTests.test_openai_user_message_includes_structure_hints`
- `python -m unittest discover -s tests`：44 个测试通过。

### 仍需后续

- 当前仍是翻译前风险提示，不执行真实合并单元格重排。
- 需要在真实表格密集论文上观察候选提示是否降低表格线性化和标题行误译，再决定是否进入单元格级局部修复执行器。

## 合并单元格候选证据（2026-06-30）
### 已完成
- `table_reconstruction.json.tables[]` 新增 `ragged_row_indices`、`ragged_row_count`、`empty_cell_count`、`merged_cell_candidate_count`、`merged_cell_candidate_type_counts`、`merged_cell_candidate_reason_counts` 和 `merged_cell_candidates`。
- 候选规则会把单行标题式缺列、尾部缺列和内部空单元格保守转换为 `colspan` / `rowspan` 候选，并记录候选原因、置信度、覆盖单元格、行列坐标和截断后的 anchor 文本。
- `continued_table_groups[]` 会聚合链内源表格的候选，并补充 `source_table_id` 与 `source_page_no`，便于跨页续表场景回查候选来源。
- `table_reconstruction.json.summary` 新增空单元格、ragged 表格/行、合并单元格候选总数、候选类型分布、候选原因分布和续表组级候选总数。
- `experiment_metrics.json` 新增 `table_merged_cell_candidate_count`、`table_ragged_table_count`、`table_ragged_row_count`、`table_empty_cell_count`、`continued_table_merged_cell_candidate_count`、`table_merged_cell_candidate_rate`、`table_ragged_table_rate`、`table_empty_cell_rate` 以及候选类型/原因 breakdowns。
- 批量实验摘要 Markdown 新增“平均合并候选”，人工评分 CSV 新增合并单元格候选数量和候选类型列，便于真实表格密集论文跑批时人工核对。

### 验证

- `python -m py_compile pdf_translate\qa\table_reconstruction.py pdf_translate\qa\metrics.py pdf_translate\experiments.py tests\test_structure_ir.py tests\test_experiments.py`
- `python -m unittest tests.test_structure_ir.StructureIRTests.test_table_reconstruction_report_builds_cell_contexts tests.test_structure_ir.StructureIRTests.test_table_reconstruction_reports_merged_cell_candidates tests.test_structure_ir.StructureIRTests.test_experiment_metrics_aggregates_quality_evidence tests.test_experiments.BatchExperimentTests.test_run_batch_experiment_writes_patent_evidence_summary`
- `python -m unittest discover -s tests`：44 个测试通过。

### 仍需后续

- 当前能力是“候选证据”而非精确合并单元格重排；需要用真实表格样本标注校准误报、漏报和置信度。
- 高精度合并单元格识别、复杂跨页表格重排、表格 OCR 与 PDF 译文重排仍需继续推进。

## 续表拒绝原因分布聚合（2026-06-30）
### 已完成
- `table_reconstruction.json.summary` 新增 `table_chain_reject_reason_count`、`table_chain_warning_reason_count`、`table_chain_reject_reason_counts`、`table_chain_reject_reason_category_counts`、`table_chain_warning_reason_counts` 和 `table_chain_warning_reason_category_counts`。
- 续表组内原始原因仍保留 `header_mismatch_segment_1` 这类定位信息，summary 额外把它归并为 `header_mismatch` 等稳定类别，便于跨样本统计。
- `experiment_metrics.json.quality` 新增续表拒绝/警告原因总量，`experiment_metrics.json.breakdowns` 新增续表拒绝/警告原因分布和类别分布。
- 批量实验摘要会在单次 records 中保留这些 breakdowns，在策略 aggregates 中合计续表拒绝类别，并在 Markdown 与人工评分 CSV 中展示续表拒绝类别入口。
- 该能力让 TABLE-01 的续表链不只输出合并/拒绝状态，还能解释拒绝原因，用于后续专利实验中比较“误合并拦截原因”和“低置信续表类型”。

### 验证

- `python -m py_compile pdf_translate\qa\table_reconstruction.py pdf_translate\qa\metrics.py pdf_translate\experiments.py tests\test_structure_ir.py tests\test_experiments.py`
- `python -m unittest tests.test_structure_ir.StructureIRTests.test_table_reconstruction_rejects_incompatible_continued_table_groups tests.test_structure_ir.StructureIRTests.test_experiment_metrics_aggregates_quality_evidence tests.test_structure_ir.StructureIRTests.test_pipeline_writes_document_ir_and_structure_manifest tests.test_experiments.BatchExperimentTests.test_run_batch_experiment_writes_patent_evidence_summary`

### 仍需后续

- 仍需在真实表格密集论文上统计各类续表拒绝原因的人工正确率，识别哪些类别容易误拒绝。
- 高精度合并单元格识别、跨多页复杂续表和 PDF 译文重排仍需继续推进。

## 表格脚注单元格级绑定证据（2026-06-30）
### 已完成
- `table_reconstruction.json` 在每个表格条目中新增 `footnote_bindings`，把已归属到表格的脚注进一步解析为候选标记，并尝试绑定到具体单元格坐标。
- 绑定规则保守匹配显著性符号、`p<0.05` 这类统计标记，以及显式 `[1]`、`(1)`、`^1`、`[a]` 等单元格脚注标记，避免普通数字被误判为脚注。
- 每条绑定会记录脚注块 ID、标记、命中单元格行列、匹配状态、未命中 warning 和匹配单元格摘要；续表合并候选也会生成组级 `footnote_bindings`。
- `table_reconstruction.json.summary` 新增 `table_footnote_binding_count`、`table_footnote_cell_binding_count`、`table_footnote_bound_cell_count`、`table_footnote_unbound_count` 和 `table_footnote_table_level_count`。
- `experiment_metrics.json` 新增脚注单元格绑定质量字段，以及 `table_footnote_cell_binding_rate`、`table_footnote_unbound_rate`；批量实验摘要同步纳入这两个质量口径。
- `build_table_translation_hints()` 会把命中的脚注单元格绑定压缩为短提示，供翻译阶段保留脚注与具体单元格的关系。

### 验证

- `python -m py_compile pdf_translate\qa\table_reconstruction.py pdf_translate\qa\metrics.py pdf_translate\experiments.py tests\test_structure_ir.py`
- `python -m unittest tests.test_structure_ir.StructureIRTests.test_table_reconstruction_report_builds_cell_contexts tests.test_structure_ir.StructureIRTests.test_experiment_metrics_aggregates_quality_evidence`
- `python -m unittest tests.test_structure_ir.StructureIRTests.test_table_reconstruction_builds_continued_table_groups tests.test_structure_ir.StructureIRTests.test_table_reconstruction_rejects_incompatible_continued_table_groups tests.test_structure_ir.StructureIRTests.test_table_translation_hints_are_chunk_scoped tests.test_structure_ir.StructureIRTests.test_openai_user_message_includes_structure_hints`
- `python -m unittest tests.test_structure_ir.StructureIRTests.test_pipeline_writes_document_ir_and_structure_manifest`
- `python -m unittest tests.test_experiments.BatchExperimentTests.test_run_batch_experiment_writes_patent_evidence_summary`

### 仍需后续

- 真实论文样本中仍需人工核对脚注单元格绑定准确率，尤其是多栏脚注、跨页脚注、复杂统计符号和合并单元格场景。
- 下一步建议把脚注单元格绑定与局部修复执行器衔接，让脚注错位能进入可验证的单元格级重译流程。

## 一页跨页关系归位证据（2026-06-29）

### 已完成
- `extract_document_ir()` 改为先构建完整 `DocumentIR.pages`，再对全篇 blocks 统一执行 `assign_block_parents()`，为跨页归位提供上下文。
- `assign_block_parents()` 保持同页优先；同页失败后只允许相邻 1 页内的图注、表注和脚注回退绑定，避免远距离误归属。
- `BlockIR.meta` 新增 `parent_page_no`、`parent_page_gap`、`cross_page_parent` 和 `cross_page_parent_attempted`，用于审计跨页父子关系来源。
- `structure_qa.json.relationships` 透传跨页归位字段，summary 新增 `cross_page_relationship_count`、`caption_cross_page_linked_count`、`caption_cross_page_orphan_count`、`footnote_cross_page_linked_count`、`footnote_cross_page_orphan_count` 和 `cross_page_parent_gap_max`。
- `experiment_metrics.json` 聚合跨页关系数量、跨页父子关系成功率、caption 跨页链接率和 footnote 跨页链接率。
- 结构分段已能在跨页表注与父表相邻时把它们保护进同一 chunk，并在 `structural_relation_ids` 中留下证据。
- 开发者文档、更新规划、专利优化方向和测试验收指南已同步 B2 当前状态。

### 验证
- `python -m unittest tests.test_structure_ir.StructureIRTests.test_assign_block_parents_links_captions_and_footnotes tests.test_structure_ir.StructureIRTests.test_assign_block_parents_links_cross_page_relationships tests.test_structure_ir.StructureIRTests.test_structure_chunks_protect_cross_page_parent_relation tests.test_structure_ir.StructureIRTests.test_experiment_metrics_aggregates_quality_evidence`
- `python -m py_compile pdf_translate/extractors/document_ir.py pdf_translate/qa/structure.py pdf_translate/qa/metrics.py tests/test_structure_ir.py`
- `python -m unittest discover -s tests`：43 个测试通过。

### 仍需后续
- 当前只允许相邻 1 页跨页回退；多页、多栏阅读顺序和图像区域裁剪后回填仍需继续推进。
- 真实论文样本仍需人工核对跨页归位准确率，避免启发式关系在复杂排版下误绑定。

## 预算驱动结构分段证据（2026-06-29）

### 已完成
- `StructureChunk` 新增 `structural_relation_ids`、`approx_tokens`、`budget_target_chars`、`budget_max_chars`、`split_reason`、`budget_overflow_chars` 和 `budget_pressure`。
- `build_structure_chunks()` 已能记录 `before_structural_relation`、`target_chars`、`max_chars`、`page_limit`、`protected_relation_page_limit` 和 `end_of_document` 等切分原因。
- 结构分段器在图注、表注、脚注与父块相邻时优先保护结构关系；若结构关系保护导致超过目标预算或硬预算，会写入对应 warning。
- `chunks_manifest.json` 与 `structure_chunks_manifest.json` 会暴露 `approx_tokens`、`budget` 和 `structural_relation_ids`，便于审计每个 chunk 的切分依据。
- `chunk_boundary_qa.json` 的 summary 和 chunks 明细新增预算切分原因、预算压力、预算越界字符数和结构关系保护统计。
- `experiment_metrics.json` 聚合预算越界块数、预算越界字符数、预算越界块率、结构关系保护次数、预算切分原因分布、预算压力分布，并在 `evidence_files` 中回链 `output/structure_chunks_manifest.json`。
- 开发者文档、更新规划、专利优化方向和测试验收指南已同步 CHUNK-01 当前状态。

### 验证
- `python -m unittest tests.test_structure_ir.StructureIRTests.test_structure_chunks_record_budget_metadata_and_split_reasons tests.test_structure_ir.StructureIRTests.test_structure_chunks_protect_structural_relation_under_budget_pressure tests.test_structure_ir.StructureIRTests.test_chunk_boundary_qa_reports_split_and_protected_fragments tests.test_structure_ir.StructureIRTests.test_experiment_metrics_aggregates_quality_evidence`
- `python -m py_compile pdf_translate/chunkers/structure.py pdf_translate/qa/chunk_boundary.py pdf_translate/qa/metrics.py pdf_translate/pipeline.py tests/test_structure_ir.py`
- `python -m unittest discover -s tests`：41 个测试通过。

### 仍需后续
- 当前 token 仍是字符到 token 的粗估，后续需要接入真实 tokenizer 或按后端能力注册表读取上下文预算。
- 段落、表格、图注级非相邻精细合并和真实论文样本人工评分仍需继续推进。

## 结构化错误码核心闭环（2026-06-29）

### 已完成
- 新增 `pdf_translate/error_codes.py`，定义 `ErrorInfo`、`PdfTranslateError`、稳定错误码集合和异常分类入口。
- `call_with_http_retry()` 的捕获事件新增 `error_code`、`error_category`、`error_retryable`；HTTP 429、超时、鉴权失败、客户端/服务端错误、网络错误可映射为结构化错误。
- 翻译工厂对缺失 API Key 和未知后端抛出结构化配置错误；OpenAI 兼容响应结构异常映射为 `API_RESPONSE_INVALID`。
- Web 任务状态和 `web_status.json` 新增 `error_code`、`error_category`、`error_retryable`、`error_next_step`、`error_source`、`error_http_status`，同时保留旧 `error` 文本字段。
- `run_metrics.json` 与 `experiment_metrics.json` 新增 `error_code_counts` 和 `error_category_counts`，用于批量实验统计失败类型。
- 前端 `UserView.vue` 已兼容 FastAPI `detail` 对象和扁平错误字段，提交、取消、下载失败提示以及任务卡片可展示用户消息、下一步建议、错误码和 HTTP 状态。
- 已重新构建 `pdf_translate/server/static/` 前端静态产物，使 Web 交付物包含结构化错误提示闭环。
- 开发者文档、更新规划和测试验收指南已同步 ERR-01 当前状态。

### 验证
- `python -m compileall pdf_translate`
- `python -m unittest tests.test_structure_ir.StructureIRTests.test_http_retry_capture_records_retry_attempts tests.test_structure_ir.StructureIRTests.test_http_retry_exhaustion_raises_structured_rate_limit_error tests.test_structure_ir.StructureIRTests.test_http_retry_timeout_maps_to_structured_error tests.test_structure_ir.StructureIRTests.test_build_translator_missing_api_key_raises_structured_error tests.test_structure_ir.StructureIRTests.test_pdf_parse_exception_maps_to_structured_error tests.test_structure_ir.StructureIRTests.test_run_metrics_aggregates_stage_chunk_and_token_evidence tests.test_structure_ir.StructureIRTests.test_experiment_metrics_aggregates_quality_evidence`
- `npm run build`
- `python -m unittest discover -s tests`：39 个测试通过。
- `git diff --check`：无空白错误；Windows 环境下仅出现 Git 换行符提示。

### 仍需后续
- JOB-01 尚未完成，任务状态的内存、`web_status.json`、SQLite 与管线 state 仍需继续收敛。
- 历史任务列表摘要还未承载完整错误态字段；后续随 JOB-01 收敛任务状态主源时统一处理。

## 续表链可控合并重建（2026-06-29）

### 已完成

- `table_reconstruction.json` 新增 `continued_table_groups`，把 `structure_qa.table_continuations` 中的跨页表格边界组织为续表链候选。
- 续表链合并前会执行兼容性检查，记录列数、行数、表头相似度、warning 和 reject reasons；安全链输出 `merge_status=merged`、`chain_confidence`、合并后行列视图、重复表头跳过数和 `merged_row_gain`，不安全链输出 `merge_status=rejected` 并保留拒绝原因。
- `build_table_translation_hints()` 会在命中续表链成员时输出链级结构提示；被拒绝的续表候选会提示“未安全合并”，避免强行把不同表格拼接。
- `experiment_metrics.json` 新增 `table_chain_candidate_count`、`table_chain_merged_count`、`table_chain_reject_count`、`table_chain_row_gain`、`table_chain_warning_count`、`table_chain_merge_rate` 和 `table_chain_reject_rate`，用于批量实验比较分页续表恢复效果。
- 开发者文档、更新规划、专利优化方向和测试验收指南已同步说明续表链合并重建证据。

### 验证

- `python -m unittest tests.test_structure_ir.StructureIRTests.test_table_reconstruction_builds_continued_table_groups tests.test_structure_ir.StructureIRTests.test_table_reconstruction_rejects_incompatible_continued_table_groups tests.test_structure_ir.StructureIRTests.test_experiment_metrics_aggregates_quality_evidence`
- `python -m unittest discover -s tests`：35 个测试通过。
- `python -m py_compile pdf_translate\qa\table_reconstruction.py pdf_translate\qa\metrics.py tests\test_structure_ir.py`

### 仍需后续

- 需要在真实表格密集论文上统计续表链合并率、拒绝率、误合并样例和人工可读性评分。
- 高精度合并单元格识别、跨多页复杂续表、真实样本拒绝原因准确率评估和 PDF 译文重排仍需后续推进。

## 批量专利实验入口基础（2026-06-29）

### 已完成

- 新增 `pdf_translate.experiments`，提供批量实验矩阵运行器，可把多篇 PDF 与多个策略组合成专利实验批次。
- CLI 新增 `pdf-translate experiment`，支持 `--variants page,structure`、`structure+ocr`、`structure+repair` 等策略规格，并复用现有 `init -> split -> translate` 单篇管线。
- CLI 新增 `--sample-manifest`，可读取 JSON/CSV/TSV 样本元数据，字段包括 `source_pdf`、`sample_id`、`pdf_type`、`tags`、`notes`，其中 `source_pdf` 相对路径按样本清单文件所在目录解析，缺省时按 PDF 入参顺序补位。
- 每次运行仍生成原有单任务证据链，包括 `experiment_metrics.json`、`run_metrics.json`、`cost_estimate.json`、QA、OCR、修复和双语 HTML 等产物。
- 批量目录会生成 `batch_experiment_manifest.json`、`batch_experiment_summary.json`、`batch_experiment_summary.md` 和 `batch_experiment_review.csv`，记录样本、样本类型标签、策略、单次运行状态、指标文件路径、策略均值、相对基线差值和人工评分入口。
- README、更新规划、专利优化方向和测试验收指南已同步说明批量实验入口。

### 验证

- `python -m unittest tests.test_experiments`
- `python -m unittest discover -s tests`：33 个测试通过。
- `python -m py_compile pdf_translate\experiments.py pdf_translate\cli.py tests\test_experiments.py`
- `git diff --check`：无空白错误；Windows 环境下仅出现 Git 换行符提示。

### 仍需后续

- 需要用真实论文样本批量运行 `page`、`structure`、表格保护、OCR 和 QA 修复等策略，形成可提交给导师和专利代理师的实验数据。
- 需要录入人工可读性评分、校准服务商真实账单和归档失败请求成本。

## 任务状态标注同步（2026-06-29）

### 已完成

- `docs/02-更新规划.md` 的优先级任务表新增“状态”列，按 `已完成`、`部分完成`、`待做` 标注当前进度，并同步更新任务描述。
- `docs/03-专利优化方向.md` 新增“当前原型完成度”，对结构感知翻译控制、表格保护、注释/图注/实体归位、动态记忆、局部修复、图像/OCR 路由和输出形态逐项标注完成度。
- `docs/05-测试与验收指南.md` 的最低自动化测试表新增“当前自动化状态”，区分已覆盖、基础覆盖和待补自动化的测试项。

### 验证

- 本次为文档状态同步，不改变代码执行路径。
- 状态标注依据当前仓库实现、`tests/test_structure_ir.py` 的自动化覆盖范围和近期项目进度审查结果。
- `python -m unittest discover -s tests`：30 个测试通过。
- `git diff --check`：无空白错误；Windows 环境下仅出现 Git 换行符提示。

### 仍需后续

- 后续每完成一项 P0/P1 任务，需要同步更新规划表状态、测试覆盖状态和对应发布历史条目。
- 真实论文样本批量实验完成后，需要把各流程版本的指标结果补入专利优化方向和测试验收文档。

## 当前 MVP 状态 - 文档整理基线（2026-06-29）

### 已完成

- Python 核心包与 CLI：`init`、`split`、`translate`、`links`、`run`。
- PDF 正文/参考文献拆分：支持 `References`、`Bibliography`、`参考文献` 等标题识别，并可用尾部比例兜底。
- 页级分块：支持 1-3 页分块和页级重叠。
- 翻译后端：`echo`、OpenAI 兼容、DeepSeek、Ollama、DeepL、hybrid。
- 记忆目录：`glossary.json`、`entities.json`、`chunk_summaries.json`、`style_notes.yaml`、`pending_review.json`、`running_summary.md`。
- 串联模式：支持前文摘要、上一块译文尾部、段尾顺延协议。
- 并联模式：支持分批并行翻译并按顺序合并。
- 精品翻译雏形：通过硅基流动巡视生成术语草稿和风险标签。
- Web 工作台：多用户登录、管理员配置、任务状态、取消任务、收藏任务、审计日志、Markdown/zip 下载。
- 前端构建：Vue 3 + Vite 构建产物由 FastAPI 静态托管。
- 文档整理：新增 `docs/` 正式文档体系，`plans/` 仅作为本地草稿来源。
- 方向重整：明确算法级翻译流程为专利主线，并补充表格/注释/人名、图像/OCR、结构分段和 HTML/PDF 输出方向。
- 文档命名：将 `docs/` 内主要文档改为中文文件名。

### 验证

- 通过代码盘点确认当前能力边界。
- 文档引用已从本地 `plans/` 收敛到正式 `docs/`。
- 尚未在本次整理中运行自动测试；当前仓库也尚未建立测试目录。

### 仍需后续

- 建立自动化测试集。
- 收敛 Web 后端注册表和翻译工厂能力表。
- 引入结构化错误码。
- 统一任务状态主源。
- 实现 DocumentIR、结构感知分段、表格/图像处理、HTML/PDF 输出和 QA 报告。

## 结构 IR 基础原型（2026-06-29）

### 已完成

- 新增 `pdf_translate/extractors/`，实现轻量 `DocumentIR`、`PageIR`、`BlockIR` 和基础块类型识别。
- 表格块已记录启发式行列、表头、数字 token、confidence 和 warning。
- 新增 `pdf_translate/chunkers/`，实现实验性 `StructureChunk`、Markdown table 渲染和结构分段 manifest。
- 新增 `pdf_translate/qa/`，实现 `structure_qa.json`，记录表格维度、页级风险和结构块统计。
- `pipeline.run_translate()` 会生成 `output/document_ir.json`、`output/structure_chunks_manifest.json` 和 `output/structure_qa.json`。
- CLI `translate` / `run` 增加 `--chunk-strategy page|structure`，默认仍为 `page`。
- 新增 `tests/test_structure_ir.py`，覆盖块分类、表格行列元数据、结构 chunk provenance、结构 QA 和 echo 后端最小管线。

### 验证

- `python -m unittest discover -s tests`
- `python -m compileall pdf_translate tests`

### 仍需后续

- 表格行列结构恢复仍是启发式基础，复杂跨页表、高精度合并单元格识别、跨页/多栏脚注图注和实体归位仍需继续实现；后续条目已补入合并单元格候选证据。
- 真实 OCR/VLM 调用、双语 HTML、PDF 译文输出、术语一致性 QA 和自动局部修复在该阶段尚未实现；后续条目记录了其中已落地的增量。

## 图像/OCR 路由基础（2026-06-29）

### 已完成

- `PageIR.meta` 新增文本字符数、文本区域比例、图像区域比例和块类型计数，作为页面风险判断的本地事实。
- 新增 `pdf_translate/vision/`，基于 DocumentIR 生成页面级 `vision_route.json`。
- `vision_route.json` 会输出 `text_only`、`local_ocr`、`vlm_review`、`skip_blank` 建议、风险等级、风险分数、触发原因和下一步说明。
- 非 `text_only` 页面会渲染 `output/vision_pages/page-0001.png` 这类页面预览，并在 `vision_route.json.pages[].evidence` 中记录预览路径、状态、尺寸和缩放比例。
- 非 `text_only` 页面会按 DocumentIR 中的 `image`、`table`、`caption`、`formula` 块渲染 `output/vision_crops/page-0001/p1-b0000-image.png` 这类候选区域裁剪，并在 `vision_route.json.pages[].evidence.region_crops` 中记录块 ID、块类型、bbox、裁剪路径和尺寸。
- `output/ocr_tasks.json` 会把页面预览和区域裁剪证据转成待执行 OCR 任务，记录任务 ID、输入图、优先级、建议本地 OCR 引擎、VLM fallback、任务状态和 DocumentIR 回写目标。
- `experiment_metrics.json.quality` 新增 `vision_preview_page_count`、`vision_region_crop_count`、`ocr_task_count`、`ocr_region_task_count`、`ocr_ready_task_count` 和 `ocr_vlm_fallback_task_count`，`rates` 新增 `vision_preview_page_rate`、`vision_region_crop_per_routed_page`、`ocr_task_per_routed_page`、`ocr_region_task_rate` 与 `ocr_ready_task_rate`，用于统计图像/OCR 路由证据和调度任务覆盖率。
- zip 资料包会将 `output/vision_pages/` 映射为 `质量/图像OCR页面预览/`，将 `output/vision_crops/` 映射为 `质量/图像OCR区域裁剪/`，将 `output/ocr_tasks.json` 映射为 `质量/OCR调度任务.json`，便于导师评审和专利实验归档。
- `pipeline.run_translate()` 每次翻译都会写出 `output/vision_route.json` 与 `output/ocr_tasks.json`，为后续 OCR/VLM 接入、实验指标和专利证据链提供记录。
- 新增单元测试覆盖低文本图片页路由到 `local_ocr`、路由页预览与区域裁剪渲染、OCR 调度任务生成、实验指标聚合、资料包包含页面预览/区域裁剪/OCR 调度任务，并验证 echo 后端最小管线写出 `vision_route.json` 和 `ocr_tasks.json`。

### 验证

- `python -m unittest tests.test_structure_ir.StructureIRTests.test_vision_route_flags_low_text_image_page_for_local_ocr tests.test_structure_ir.StructureIRTests.test_write_vision_route_renders_routed_page_preview tests.test_structure_ir.StructureIRTests.test_ocr_task_manifest_schedules_region_crops_for_writeback tests.test_structure_ir.StructureIRTests.test_experiment_metrics_aggregates_quality_evidence tests.test_structure_ir.StructureIRTests.test_pipeline_writes_document_ir_and_structure_manifest tests.test_structure_ir.StructureIRTests.test_bundle_includes_structure_qa_and_repair_artifacts`
- `python -m unittest discover -s tests`
- `python -m py_compile pdf_translate/vision/routing.py pdf_translate/vision/ocr_tasks.py pdf_translate/pipeline.py pdf_translate/qa/metrics.py pdf_translate/zip_bundle.py tests/test_structure_ir.py`

### 仍需后续

- 已有可选 tesseract CLI 本地 OCR 执行入口；高精度表格 OCR 引擎和 VLM 调用尚未接入。该阶段 OCR 候选文本尚未进入正式结构分段或翻译链路，后续“OCR 候选 QA 与晋级 IR 基础”已补上晋级副本。
- 路由阈值仍是保守启发式，需要在真实论文样本上统计误报、漏报和图像/扫描页覆盖率。

## OCR 结果回写证据基础（2026-06-29）

### 已完成

- 新增 `pdf_translate.vision.ocr_writeback`，提供 `build_ocr_writeback()` 与 `write_ocr_writeback()`。
- 管线生成 `output/ocr_tasks.json` 后会继续生成 `output/ocr_writeback.json` 与 `output/document_ir_ocr.json`。
- OCR 结果 payload 通过 `task_id` 关联调度任务，并经过任务状态、OCR 结果状态、空文本和最小置信度门禁；通过后只追加到 page/block 的 `meta.ocr_candidates`。
- 当前回写不替换 `DocumentIR.text`、不改写正式 chunk 和译文，避免未经 QA 的 OCR 文本污染翻译链路。
- `ocr_writeback.json` summary 新增任务数、结果数、接受/拒绝数、pending 任务数、未知任务结果数、block/page 回写数、结果状态分布、接受引擎分布和拒绝原因分布。
- `experiment_metrics.json` 新增 `ocr_result_count`、`ocr_accepted_result_count`、`ocr_rejected_result_count`、`ocr_pending_task_count`、`ocr_block_writeback_count`、`ocr_page_writeback_count`、`ocr_task_result_coverage_rate`、`ocr_result_acceptance_rate` 和 `ocr_writeback_apply_rate`。
- zip 资料包会将 `output/ocr_writeback.json` 映射为 `质量/OCR结果回写.json`，将 `output/document_ir_ocr.json` 映射为 `设置/OCR增强文档结构IR.json`。
- 新增单元测试覆盖 OCR 候选回写、拒绝原因、原始 DocumentIR 不被污染、实验指标聚合、echo 后端最小管线写出回写产物，以及资料包包含新文件。

### 验证

- `python -m unittest tests.test_structure_ir.StructureIRTests.test_ocr_writeback_appends_candidates_to_augmented_ir tests.test_structure_ir.StructureIRTests.test_experiment_metrics_aggregates_quality_evidence tests.test_structure_ir.StructureIRTests.test_pipeline_writes_document_ir_and_structure_manifest tests.test_structure_ir.StructureIRTests.test_bundle_includes_structure_qa_and_repair_artifacts`
- `python -m py_compile pdf_translate/vision/ocr_writeback.py pdf_translate/pipeline.py pdf_translate/qa/metrics.py pdf_translate/zip_bundle.py tests/test_structure_ir.py`

### 仍需后续

- VLM fallback 尚未接入；OCR 结果文件输入入口与可选 tesseract CLI 执行器已在后续小节落地，但还没有高精度表格 OCR 适配器。
- 该阶段 OCR 候选文本尚未参与结构分段、译前提示、译后 QA 或正式译文替换；后续“OCR 候选 QA 与晋级 IR 基础”已补上候选 QA 和结构分段输入副本，人工确认和失败回退记录仍需继续完善。

## OCR 结果文件入口基础（2026-06-29）

### 已完成

- `pdf_translate.vision.ocr_writeback` 新增 `load_ocr_results()`、`build_ocr_results_payload()` 和 `write_ocr_results_payload()`，可把外部或后续本地 OCR 执行器返回的 `ocr-results-v1` JSON 规范化为 `output/ocr_results.json`。
- 管线在生成 `output/ocr_tasks.json` 后新增 `ocr_results` 阶段；未传入结果文件时写出空载荷，传入 `ocr_results_path` 时会读取并规范化结果，再交给 `write_ocr_writeback()` 做状态、文本、置信度和未知任务门禁。
- CLI `translate` / `run` 新增 `--ocr-results`，可把外部 OCR 结果文件回灌到当前任务目录，用于本地 OCR 原型、人工实验和专利证据链复现。
- `experiment_metrics.json` 新增 `ocr_result_payload_count`、`ocr_invalid_result_count`、`ocr_result_payload_valid_rate`、`ocr_result_payload_status_counts` 和 `ocr_result_payload_engine_counts`，并在 `evidence_files` 中回链 `output/ocr_results.json`。
- zip 资料包会将 `output/ocr_results.json` 映射为 `质量/OCR识别结果.json`，与 `质量/OCR调度任务.json`、`质量/OCR结果回写.json` 和 `设置/OCR增强文档结构IR.json` 共同构成 OCR 输入、门禁和增强 IR 证据链。
- 新增/扩展单元测试覆盖 OCR 结果载荷规范化、结果文件写入与读取、外部 OCR 结果文件进入最小管线、实验指标聚合和资料包映射。

### 验证

- `python -m unittest tests.test_structure_ir.StructureIRTests.test_ocr_writeback_appends_candidates_to_augmented_ir tests.test_structure_ir.StructureIRTests.test_experiment_metrics_aggregates_quality_evidence tests.test_structure_ir.StructureIRTests.test_pipeline_writes_document_ir_and_structure_manifest tests.test_structure_ir.StructureIRTests.test_bundle_includes_structure_qa_and_repair_artifacts`
- `python -m unittest discover -s tests`
- `python -m py_compile pdf_translate/cli.py pdf_translate/pipeline.py pdf_translate/qa/metrics.py pdf_translate/vision/__init__.py pdf_translate/vision/ocr_writeback.py pdf_translate/zip_bundle.py tests/test_structure_ir.py`

### 仍需后续

- 高精度表格 OCR 执行器和 VLM fallback 仍未接入；当前入口主要负责保存和审计结果载荷。
- OCR 候选文本仍不会直接替换正式源文本、结构分段输入或译文，需要先补结构不变量 QA、人工确认、低置信度回退和批量样本对比。

## 可选本地 OCR 执行器基础（2026-06-29）

### 已完成

- 新增 `pdf_translate.vision.ocr_executor`，可消费 `ocr_tasks.json` 中 `pending_engine` 任务，默认通过本地 `tesseract` CLI 对页面预览或区域裁剪图执行 OCR。
- CLI `translate` / `run` 新增 `--execute-ocr`、`--ocr-engine`、`--ocr-language`、`--ocr-timeout` 和 `--ocr-command`；未传入 `--ocr-results` 时，显式开启 `--execute-ocr` 才会尝试本地 OCR。
- 本地 OCR 执行结果会写入 `output/ocr_results.json` 的 `execution` 区块，记录任务数、尝试数、成功数、失败数、跳过数、引擎可用性、状态分布、执行命令摘要和跳过任务；缺少 tesseract、缺少输入图、命令失败或超时都会被记录为失败/跳过证据。
- 管线在开启 `execute_ocr` 时会忽略旧的默认 `output/ocr_results.json`，重新执行 OCR；显式传入 `--ocr-results` 时仍优先使用外部结果文件。
- `experiment_metrics.json` 新增 `ocr_executor_attempted_task_count`、`ocr_executor_succeeded_task_count`、`ocr_executor_failed_task_count`、`ocr_executor_skipped_task_count`、`ocr_executor_available` 和 `ocr_executor_success_rate`，并记录 `ocr_execution_status_counts`。
- 新增单元测试使用 fake runner 验证本地 OCR 执行器能够解析任务、定位裁剪图、生成 `ocr-results-v1` 载荷和执行摘要，不依赖真实 OCR 安装。

### 验证

- `python -m unittest tests.test_structure_ir.StructureIRTests.test_local_ocr_executor_runs_ready_tasks_into_results_payload tests.test_structure_ir.StructureIRTests.test_experiment_metrics_aggregates_quality_evidence`
- `python -m unittest discover -s tests`
- `python -m py_compile pdf_translate/vision/ocr_executor.py pdf_translate/pipeline.py pdf_translate/cli.py pdf_translate/qa/metrics.py pdf_translate/vision/__init__.py tests/test_structure_ir.py`

### 仍需后续

- tesseract CLI 只作为本地 OCR 适配器雏形，尚不能解决图片型复杂表格、公式和多栏版面；后续仍需接入表格 OCR/版面分析器并记录更细粒度置信度。
- 该阶段 OCR 结果仍只作为候选文本进入增强 DocumentIR，不会直接替换正式源文本、结构分段输入或译文；后续“OCR 候选 QA 与晋级 IR 基础”已补上结构分段输入副本，正式译文替换仍待人工确认策略。

## OCR 候选 QA 与晋级 IR 基础（2026-06-29）

### 已完成

- 新增 `pdf_translate.qa.ocr_candidates`，可从 `document_ir_ocr.json` 中读取 page/block 的 `meta.ocr_candidates`，生成 `output/ocr_candidate_qa.json` 与 `output/ocr_candidate_qa.md`。
- OCR 候选 QA 会把候选分为 `candidate`、`needs_review`、`blocked`，并记录低置信度、重复源文本、表格/公式结构复核、过短文本、低有效字符比例和执行器 warning 等原因。
- 新增 `pdf_translate.vision.ocr_promotion`，可消费 OCR 候选 QA 报告并生成 `output/ocr_candidate_promotion.json`、`output/ocr_candidate_promotion.md` 和 `output/document_ir_promoted.json`。
- 只有 `status=candidate` 的 OCR 文本会晋级；block 级候选写入目标块文本，page 级候选会生成合成 OCR 段落块。原始 `document_ir.json` 与增强 `document_ir_ocr.json` 不被覆盖。
- 结构分段器现在使用 `document_ir_promoted.json` 生成结构 chunks；图片块只有在存在晋级 OCR 文本时才进入翻译块。
- `experiment_metrics.json` 新增 OCR 候选可晋级数、实际晋级数、晋级文本字符数、晋级率、合格晋级率和晋级 skip 原因分布，并回链 `ocr_candidate_promotion.json` 与 `document_ir_promoted.json`。
- zip 资料包会将 OCR 候选 QA、候选晋级报告和晋级 IR 分别映射到 `质量/OCR候选文本QA.*`、`质量/OCR候选文本晋级.*` 和 `设置/OCR晋级文档结构IR.json`。
- 单元测试覆盖 OCR 回写候选进入 QA、通过 QA 的图片 OCR 文本进入晋级 IR 和结构分段、最小管线写出晋级产物、实验指标聚合和资料包映射。

### 验证

- `python -m py_compile pdf_translate\extractors\document_ir.py pdf_translate\vision\ocr_promotion.py pdf_translate\chunkers\structure.py pdf_translate\pipeline.py pdf_translate\qa\metrics.py pdf_translate\zip_bundle.py tests\test_structure_ir.py`
- `python -m unittest tests.test_structure_ir.StructureIRTests.test_ocr_writeback_appends_candidates_to_augmented_ir tests.test_structure_ir.StructureIRTests.test_experiment_metrics_aggregates_quality_evidence tests.test_structure_ir.StructureIRTests.test_pipeline_writes_document_ir_and_structure_manifest tests.test_structure_ir.StructureIRTests.test_bundle_includes_structure_qa_and_repair_artifacts`
- `python -m unittest discover -s tests`

### 仍需后续

- OCR 晋级仍是本地规则门禁，尚未包含人工确认台、表格 OCR 结构化单元格晋级和 VLM fallback。
- 通过晋级的 OCR 文本会参与结构分段输入，但正式译文替换、PDF 重排和人工发布合并策略仍需后续实现。

## 译后规则 QA 基础（2026-06-29）

### 已完成

- 新增 `pdf_translate/qa/translation.py`，对每个翻译块执行本地规则 QA。
- 管线完成翻译后会生成 `output/qa_report.json` 和 `output/qa_report.md`。
- 当前 QA 覆盖数字保留、引用保留、Table/Figure 编号保留、Markdown 表格形状、重复段落和英文残留比例。
- JSON 报告可作为未来 `REPAIR-01` 局部修复的输入；Markdown 报告可用于导师评审和人工复核。
- 新增单元测试覆盖漏数字、漏引用、表格形状变化，以及 echo 后端最小管线写出 QA 报告。

### 验证

- `python -m unittest discover -s tests`
- `python -m compileall pdf_translate tests`

### 仍需后续

- 术语一致性、公式/变量细粒度比对和引用语义一致性仍需继续完善。
- 当前 QA 与修复计划只定位问题和建议修复动作，不自动触发局部重译；后续自动修复器应读取 `repair_plan.json` 执行。

## QA 驱动局部修复计划（2026-06-29）

### 已完成

- 新增 `pdf_translate/qa/repair.py`，可将译后 QA issue 映射为局部修复项。
- 管线完成翻译后会生成 `output/repair_plan.json` 和 `output/repair_plan.md`。
- 修复项记录 `repair_id`、`chunk_id`、页码、优先级、问题类型、修复动作、修复范围、建议执行器、原因和证据。
- 当前支持将漏译块、漏数字、漏引用、图表编号缺失、公式符号缺失、表格形状变化、重复段落和英文残留映射为不同修复动作。
- zip 资料包会包含 DocumentIR、结构分段、结构 QA、图像/OCR 路由、译后 QA 和局部修复计划等质量证据文件。
- 新增单元测试覆盖 QA issue 到修复计划的映射，并验证 echo 后端最小管线写出修复计划。

### 验证

- `python -m unittest discover -s tests`
- `python -m compileall pdf_translate tests`

### 仍需后续

- 当前只生成修复计划，不自动调用翻译后端重译。
- 后续需要实现按块、段落、表格单元格的局部重译执行器，并记录修复前后对比。

## 双语 HTML 阅读版（2026-06-29）

### 已完成

- 新增 `pdf_translate/exporters/`，实现 `bilingual.html` 本地静态导出。
- 管线完成翻译、QA 和修复计划后会生成 `output/bilingual.html`。
- 双语 HTML 按 chunk 同屏展示原文与译文，并附带该块 QA 问题和局部修复建议。
- zip 资料包会将 `output/bilingual.html` 映射为 `译文/双语对照.html`。
- 新增单元测试覆盖 HTML 产物内容、echo 后端最小管线写出 HTML，以及资料包包含 HTML。

### 验证

- `python -m unittest discover -s tests`
- `python -m compileall pdf_translate tests`

### 仍需后续

- 当前 HTML 主要用于评审和对照阅读，不是 PDF 原排版回填。
- PDF 译文输出、术语 hover、块级 QA 交互筛选和修复前后对比仍需继续实现。

## 术语一致性 QA 基础（2026-06-29）

### 已完成

- 译后 QA 会读取当前任务 `memory/glossary.json` 中的 `terms`，统计术语库条目数。
- 当源文块出现英文术语而译文未包含术语库指定中文译名时，报告 `missing_glossary_terms`。
- `qa_report.md` 会展示缺失术语的英文原词和期望中文译名，便于人工复核。
- 局部修复计划会把术语缺失映射为 `rewrite_with_glossary_terms`，提示后续重译时注入并锁定术语。
- 双语 HTML 会展示术语缺失证据，方便在原文/译文对照页定位问题。
- 新增单元测试覆盖术语缺失 QA、修复动作映射和 HTML 展示。

### 验证

- `python -m unittest discover -s tests`
- `python -m compileall pdf_translate tests`

### 仍需后续

- 当前只检查“源文出现英文术语但译文未出现指定中文译名”的基础情况。
- 术语候选/确认/否决状态、同一英文多译冲突、章节作用域、实体名保留和术语 hover 仍需继续实现。

## 页面边界碎片 QA 基础（2026-06-29）

### 已完成

- `pdf_translate.qa.structure` 新增页面边界碎片检测，基于相邻页面的最后/最前结构块判断分页断裂风险。
- `structure_qa.json` 新增 `page_boundary_fragments` 明细，记录页码、块 ID、块类型、风险原因、首尾片段和建议处理方式。
- `structure_qa.json` summary 新增 `page_boundary_fragment_count` 和 `page_boundary_fragment_rate`，用于专利原型实验中的页面交接碎片率统计。
- 当前可识别上一页未完句、下一页小写/连接词接续、相同可续块类型跨页，以及疑似续表。
- 新增单元测试覆盖上一页未完句与下一页接续段落的风险输出。

### 验证

- `python -m unittest discover -s tests`
- `python -m compileall pdf_translate tests`

### 仍需后续

- 该阶段只做风险识别和指标记录，还没有自动改变分块边界。
- 后续条目继续记录结构分段器消费 `page_boundary_fragments` 的增量。

## 边界保护式结构分段基础（2026-06-29）

### 已完成

- 新增 `pdf_translate/structure_boundaries.py`，把页面边界碎片检测抽为 QA 与分段器共享的结构规则。
- `build_structure_chunks()` 会消费页面边界风险；当相邻页面存在高风险断句或续表时，允许该边界跨过普通 `target_chars` 或 `max_pages_per_chunk` flush 条件，优先保留到同一结构翻译块。
- `StructureChunk` 与 `structure_chunks_manifest.json` 新增 `boundary_fragment_ids`，记录该块保护了哪些页面边界。
- 主管线 `output/chunks_manifest.json` 也会保留 `boundary_fragment_ids`，便于实验统计和问题追踪。
- 新增单元测试覆盖页面边界风险触发后的同块保护行为。

### 验证

- `python -m unittest discover -s tests`
- `python -m compileall pdf_translate tests`

### 仍需后续

- 当前保护粒度仍以页面边界为主，只允许基础的相邻页面合并。
- 后续需要进一步做到段落、表格续页、图注续页和脚注归属级别的精细合并，并把保护前后的页面交接碎片率纳入实验对比。

## 图注表注脚注归属基础（2026-06-29）

### 已完成

- `pdf_translate.extractors.document_ir.assign_block_parents()` 新增同页关系归属规则。
- 英文 `Fig. 1` / `Figure 1`、`Table 1` 和中文 `图1` / `表1` 类 caption 会被识别为图注或表注。
- 表注优先归属到后续最近表格，图注优先归属到前序最近图片，脚注归属到前序最近正文、表格或公式块；紧跟表格的脚注会标记为 `footnote_for_table`。
- `DocumentIR` 中 caption/footnote 会写入 `parent_id`、`meta.parent_relation`、`meta.caption_kind`，无法归属时写入 `meta.parent_warning`。
- `structure_qa.json` 新增 `relationships` 明细，并在 summary 中统计 `caption_count`、`caption_linked_count`、`caption_orphan_count`、`footnote_count`、`footnote_linked_count`、`footnote_orphan_count`、`table_footnote_count`、`relationship_count` 和 `relationship_warning_count`。
- 新增单元测试覆盖图注、表注、脚注归属和孤立 caption warning。

### 验证

- `python -m unittest tests.test_structure_ir.StructureIRTests.test_classify_table_caption_and_footnote tests.test_structure_ir.StructureIRTests.test_assign_block_parents_links_captions_and_footnotes tests.test_structure_ir.StructureIRTests.test_pipeline_writes_document_ir_and_structure_manifest`
- `python -m compileall pdf_translate\extractors\document_ir.py pdf_translate\qa\structure.py tests\test_structure_ir.py`

### 仍需后续

- 该阶段仅做同页邻近归属；后续“一页跨页关系归位证据”已补相邻 1 页回退，“表格脚注单元格级绑定证据”已补基础匹配链路；多页、多栏阅读顺序、图像区域裁剪后回填和真实样本准确率核对仍待处理。
- 后续 PDF/HTML 重排应消费 `relationships`，把图注、表注和脚注放回所属结构单元附近。

## 续表与表格脚注 QA 基础（2026-06-29）

### 已完成

- `assign_block_parents()` 将紧跟表格的脚注细化为 `footnote_for_table`，并写入 `meta.table_footnote`。
- `structure_qa.json` summary 新增 `table_footnote_count` 和 `table_continuation_count`。
- `structure_qa.json` 新增 `table_continuations` 明细，记录续表边界、前后表格块 ID、页码、首尾片段和建议处理方式。
- `tables` 条目新增 `continued_from_block_id` 与 `continued_to_block_id`，便于定位表格段之间的跨页关系。
- 结构分段器继续消费同一页面边界风险，遇到续表时会保护相邻页面并在 manifest 中记录 `boundary_fragment_ids`。
- 新增单元测试覆盖表格脚注归属、续表 QA 明细和续表边界保护。

### 验证

- `python -m unittest tests.test_structure_ir.StructureIRTests.test_assign_block_parents_links_captions_and_footnotes tests.test_structure_ir.StructureIRTests.test_structure_qa_reports_table_continuations tests.test_structure_ir.StructureIRTests.test_pipeline_writes_document_ir_and_structure_manifest`
- `python -m compileall pdf_translate\extractors\document_ir.py pdf_translate\qa\structure.py tests\test_structure_ir.py`

### 仍需后续

- 当前只识别“上一页最后结构块和下一页第一结构块均为表格”的强续表信号。
- 后续需要支持跨页表题、复杂续表深度合并、高精度合并单元格识别与真实样本校准、脚注单元格绑定真实样本准确率核对，以及翻译后 PDF 重排。

## 实体候选识别与结构 QA 基础（2026-06-29）

### 已完成

- `pdf_translate.extractors.document_ir.extract_entity_candidates()` 新增本地规则实体候选提取。
- 当前可提取引用作者、人名候选、机构名、模型/数据集名和大写缩写。
- `DocumentIR` 文本块会在 `meta.entities` 中记录实体候选的 `type`、`text`、`source` 和 `confidence`。
- `structure_qa.json` 新增 `entities` 明细，记录实体所在页码、块 ID、块类型、实体类型、候选文本、置信度和规则来源。
- `structure_qa.json` summary 新增 `entity_candidate_count`、`entity_unique_count` 和 `entity_type_counts`，用于专利实验中的实体候选覆盖率统计。
- 新增单元测试覆盖实体候选提取、结构 QA 实体统计，以及 echo 后端最小管线写出实体字段。

### 验证

- `python -m unittest tests.test_structure_ir.StructureIRTests.test_extract_entity_candidates_for_academic_names tests.test_structure_ir.StructureIRTests.test_structure_qa_reports_entity_candidates tests.test_structure_ir.StructureIRTests.test_pipeline_writes_document_ir_and_structure_manifest`
- `python -m compileall pdf_translate\extractors\document_ir.py pdf_translate\extractors\__init__.py pdf_translate\qa\structure.py tests\test_structure_ir.py`

### 仍需后续

- 当前是规则候选，不是完整 NER，也不会自动写入确认术语库。
- 后续需要接入术语确认台、章节作用域、实体别名合并、译文保留/一致性 QA，以及必要时的模型辅助复核。

## 实体候选保留 QA 基础（2026-06-29）

### 已完成

- `pdf_translate.qa.translation` 复用本地实体候选规则，在译后 QA 中检查候选实体是否出现在译文中。
- `qa_report.json` summary 新增 `entity_candidate_count` 和 `missing_entity_token_count`。
- 当模型名、数据集名、机构或缩写等候选实体缺失时，块级 issue 会输出 `missing_entity_tokens` 和实体证据。
- `qa_report.md` 会展示缺失实体文本及类型，便于人工复核。
- `repair_plan.json` / `repair_plan.md` 新增 `rewrite_with_entity_tokens` 修复动作，提示后续重译时锁定实体 token。
- 双语 HTML 会展示实体缺失 QA 和实体锁定重译建议。
- 新增单元测试覆盖实体缺失 QA、修复计划映射和 HTML 展示。

### 验证

- `python -m unittest tests.test_structure_ir.StructureIRTests.test_translation_qa_reports_missing_entity_tokens tests.test_structure_ir.StructureIRTests.test_pipeline_writes_document_ir_and_structure_manifest`
- `python -m compileall pdf_translate\qa\translation.py pdf_translate\qa\repair.py tests\test_structure_ir.py`

### 仍需后续

- 当前只做候选实体的字符串保留检查，不判断中文译名或别名是否正确。
- 后续需要结合术语确认台、实体别名、章节作用域和人工确认状态，升级为实体一致性 QA。

## 术语冲突待审与 QA 基础（2026-06-29）

### 已完成

- `memory_store.merge_glossary_terms_from_survey()` 新增术语时会写入 `status: candidate`。
- 当巡视产出同一英文术语的不同中文译名时，不覆盖原术语，而是写入 `memory/pending_review.json` 的 `glossary_conflict` 待审项。
- 当多个英文术语共享同一中文译名时，会写入 `shared_translation_review` 待审项。
- 译后 QA 会读取 `pending_review.json`，在相关块中输出 `glossary_translation_conflict`，并统计 `glossary_conflict_count`。
- 修复计划会把术语冲突映射为 `review_glossary_conflict`，执行器为 `human_review`。
- 双语 HTML 会展示术语冲突证据和对应修复动作。
- 新增单元测试覆盖待审项写入、QA issue、修复计划和 HTML 展示。

### 验证

- `python -m unittest discover -s tests`
- `python -m compileall pdf_translate tests`

### 仍需后续

- 当前只提供待审记录和 QA 提示，还没有术语确认台 UI。
- 后续需要支持确认/否决/合并术语、术语置信度、章节作用域和确认后自动重译相关块。

## 专利实验指标汇总基础（2026-06-29）

### 已完成

- 新增 `pdf_translate.qa.metrics`，提供 `build_experiment_metrics()` 与 `write_experiment_metrics()`。
- 管线完成结构 QA、OCR/VLM 路由、译后 QA 和局部修复计划后，会生成 `output/experiment_metrics.json`。
- 指标汇总包含页数、块数、表格数、续表数、表格脚注数、图注/脚注孤立数、实体候选数、实体缺失数、页面边界碎片率、OCR 候选页数、QA issue 数、修复项数和英文残留比例。
- 比例指标包含关系 warning 率、图注/脚注孤立率、续表率、实体缺失率、OCR 候选页率、每块修复项和每块 QA issue。
- zip 资料包会将 `output/experiment_metrics.json` 映射为 `质量/实验指标.json`，便于导师评审和专利证据归档。
- 新增单元测试覆盖指标聚合、echo 后端最小管线写出实验指标，以及资料包包含实验指标文件。

### 验证

- `python -m unittest discover -s tests`
- `python -m compileall pdf_translate tests`

### 仍需后续

- 批量实验汇总器已在后续条目补上；还需要补真实论文样本对比、后端账单对照、人工评分录入和修复前后指标对比。

## DocumentIR 表格不变量 QA 基础（2026-06-29）

### 已完成

- `pdf_translate.qa.translation.build_translation_qa()` 新增可选 `document_ir` 参数，可把源 DocumentIR 中的表格行列数作为译后结构不变量。
- 当源 chunk 本身不是 Markdown 表格但 DocumentIR 对应页或结构 chunk `block_ids` 中存在表格块时，译后 QA 会检查译文是否保留 Markdown 表格形状。
- 若译文没有表格或行列数不一致，块级 issue 会输出 `table_shape_mismatch`，证据包含表格 `block_id`、页码、源行列数、目标行列数、表头、数字 token 和原因。
- `qa_report.json` summary 新增 `source_table_count` 和 `table_shape_error_count`，`qa_report.md` 展示源表格数和表格形状异常数。
- `experiment_metrics.json` 新增 `table_shape_error_count` 和 `table_shape_error_rate`，为专利实验中的表格行列保持率提供机器可读证据。
- 新增单元测试覆盖纯文本源 chunk + DocumentIR 表格时的表格形状异常检测，并验证修复计划映射到 `repair_table_shape`。

### 验证

- `python -m unittest discover -s tests`
- `python -m compileall pdf_translate tests`

### 仍需后续

- 当前只检查表格级行列形状，不做单元格级内容对齐和局部重译。
- 后续需要支持高精度合并单元格识别与真实样本校准、跨页续表深度重建、真实样本续表原因准确率评估和修复前后表格指标对比；合并单元格候选证据已在后续条目补入。

## 分段边界 QA 基础（2026-06-29）

### 已完成

- 新增 `pdf_translate.qa.chunk_boundary`，提供 `build_chunk_boundary_qa()` 与 `write_chunk_boundary_qa()`。
- 管线写出 `chunks_manifest.json` 后会生成 `output/chunk_boundary_qa.json`，对照 `structure_qa.json.page_boundary_fragments` 与当前实际 chunks。
- 分段边界 QA 会为每个高风险页面边界记录状态：`protected`、`co_located` 或 `split`，并保留相关 chunk id、页码、风险原因、前后块 id 和建议处理方式。
- summary 新增 `split_boundary_count`、`protected_boundary_count`、`co_located_boundary_count`、`high_risk_split_count`、`split_boundary_rate`、`protected_boundary_rate` 和 `high_risk_split_rate`。
- `experiment_metrics.json` 会聚合边界切开数、保护数、同块覆盖数和对应比例。
- zip 资料包会将 `output/chunk_boundary_qa.json` 映射为 `质量/分段边界QA.json`。
- 新增单元测试覆盖 page chunks 切开高风险边界、structure chunks 保护高风险边界、echo 后端最小管线写出分段边界 QA，以及资料包包含该文件。

### 验证

- `python -m unittest discover -s tests`
- `python -m compileall pdf_translate tests`

### 仍需后续

- 当前分段边界 QA 只统计当前策略结果，不自动重新分段。
- 后续需要做 page 策略与 structure 策略的批量对比实验，并把边界切开率下降与漏译/重复译文率联动分析。

## 分段策略对比基础（2026-06-29）

### 已完成

- `pdf_translate.qa.chunk_boundary` 新增 `build_chunk_strategy_comparison()` 与 `write_chunk_strategy_comparison()`。
- 管线会在同一份 DocumentIR 上同时构建固定页分段 baseline 和结构分段候选，并生成 `output/chunk_strategy_comparison.json`。
- 对比报告记录 active strategy、baseline strategy、每个策略的 chunk 数、边界切开数、保护数、同块覆盖数、切开率、保护率和相对 baseline 的切开边界降幅。
- `experiment_metrics.json` 新增 baseline 切开边界数、active 切开边界数、active 相对 baseline 的切开边界降幅和降幅比例。
- zip 资料包会将 `output/chunk_strategy_comparison.json` 映射为 `质量/分段策略对比.json`。
- 新增单元测试覆盖 page baseline 与 structure strategy 的同文档对比、最小管线写出对比报告，以及资料包包含该文件。

### 验证

- `python -m unittest discover -s tests`
- `python -m compileall pdf_translate tests`

### 仍需后续

- 跨样本批量实验汇总器已在后续条目补上。
- 后续需要实际运行真实论文样本，记录边界切开率、漏译率、重复译文率和人工可读性评分的联动变化。

## 表格重建证据基础（2026-06-29）

### 已完成

- 新增 `pdf_translate.qa.table_reconstruction`，提供 `build_table_reconstruction_report()` 与 `write_table_reconstruction_report()`。
- 管线会在结构 QA 后生成 `output/table_reconstruction.json`，按源表格记录单元格行列坐标、表头/行头上下文、数字 token、单位 token、显著性 token、表注/脚注表级绑定、脚注单元格级绑定证据和续表关系。
- 报告 summary 新增 `reconstructable_table_count`、`cell_count`、`numeric_cell_count`、`numeric_token_count`、`unit_token_count`、`significance_token_count`、`caption_linked_table_count`、`footnote_linked_table_count` 和 `table_reconstruction_ready_rate`。
- `experiment_metrics.json` 新增表格可重建数、单元格数、数字单元格率、表注绑定率、表格脚注绑定率和脚注单元格绑定率等专利实验指标。
- zip 资料包会将 `output/table_reconstruction.json` 映射为 `质量/表格重建证据.json`。
- 新增单元测试覆盖表格单元格上下文构造、实验指标聚合、最小管线写出表格重建报告，以及资料包包含该文件。

### 验证

- `python -m unittest tests.test_structure_ir.StructureIRTests.test_table_reconstruction_report_builds_cell_contexts`
- `python -m unittest tests.test_structure_ir.StructureIRTests.test_experiment_metrics_aggregates_quality_evidence`
- `python -m unittest tests.test_structure_ir.StructureIRTests.test_pipeline_writes_document_ir_and_structure_manifest`
- `python -m unittest tests.test_structure_ir.StructureIRTests.test_bundle_includes_structure_qa_and_repair_artifacts`

### 仍需后续

- 当前表格重建仍基于 PyMuPDF 文本块启发式；该阶段尚未识别合并单元格、跨页重复表头或复杂多栏阅读顺序，后续条目已补入合并单元格候选证据，高精度识别和重排仍待实现。
- 后续需要把 `table_reconstruction.json` 继续接入表格单元格级局部重译、复杂续表合并和 PDF 译文重排。

## 表格结构保护提示注入（2026-06-29）

### 已完成

- `TranslationRequest` 新增 `structure_hints`，用于承载当前 chunk 的结构保护证据。
- OpenAI/DeepSeek 兼容提示词升级到 `v4`，会把 `structure_hints` 注入为 `【结构保护提示】`，要求模型保留表格行列、表头/行头关系、表注/脚注关系和锁定 token。
- `pdf_translate.qa.table_reconstruction.build_table_translation_hints()` 会把 `table_reconstruction.json` 压缩成 chunk 级提示，包含表格形状、表头、行头、锁定 token、表注/脚注、脚注单元格绑定和续表关系。
- 串行与并行翻译路径都会把当前 chunk 的结构提示传入翻译请求，使表格保护从“译后检测”前移到“译前约束”。
- 新增单元测试覆盖 chunk 级提示筛选、OpenAI 兼容用户消息注入，以及最小管线写出结构产物。

### 验证

- `python -m unittest discover -s tests`
- `python -m compileall pdf_translate tests`

### 仍需后续

- 需要在真实论文样本和 DeepSeek/OpenAI 兼容后端上对比提示注入前后的表格行列错误率、锁定 token 保留率和可读性评分。
- 当前提示只做译前约束，不自动执行单元格级重译；后续需要结合真实模型输出评估提示效果。

## 表格单元格锁定 token QA 基础（2026-06-29）

### 已完成

- `pdf_translate.qa.translation.build_translation_qa()` 新增 `table_reconstruction` 输入，可消费 `table_reconstruction.json` 中的表格单元格坐标和锁定 token。
- 译后 QA 会解析目标 Markdown 表格；当对应单元格缺失源表格数字、单位或显著性符号时，输出 `table_cell_token_mismatch`，证据包含表格 ID、页码、行列坐标、表头/行头、源单元格、目标单元格和缺失 token。
- `repair_plan.json` 新增 `repair_table_cell_tokens` 修复动作，范围为 `table_cell`，优先级为 `P0`，用于后续表格单元格级局部重译执行器。
- `experiment_metrics.json` 新增 `source_table_locked_token_count`、`table_cell_token_error_count`、`missing_table_locked_token_count`，以及 `table_cell_token_error_rate` 和 `table_locked_token_missing_rate`。
- 管线会在写出译后 QA 时传入当前任务的 `table_reconstruction`，使表格证据链贯通“译前提示 -> 译后检测 -> 局部修复计划 -> 实验指标”。
- 新增单元测试覆盖表格单元格 token 缺失检测、修复计划映射和实验指标聚合。

### 验证

- `python -m unittest discover -s tests`
- `python -m compileall pdf_translate tests`

### 仍需后续

- 当前只定位单元格 token 缺失并生成修复项，不自动调用翻译后端重译单元格。
- 后续需要把 `repair_table_cell_tokens` 的修复请求接入局部重译执行器，并记录修复前后表格 token 缺失率变化。

## 局部修复请求基础（2026-06-29）

### 已完成

- `pdf_translate.qa.repair` 新增 `build_repair_requests()` 与 `write_repair_requests()`，可把 `repair_plan.json` 转成后端/人工可消费的局部修复请求。
- 管线完成修复计划后会生成 `output/repair_requests.json` 和 `output/repair_requests.md`。
- 每个请求记录 `request_id`、`repair_id`、chunk、页码、优先级、问题类型、修复动作、修复范围、执行器、状态、修复指令、锁定 token、源文片段、当前译文片段、QA 证据和后端 payload。
- 翻译后端请求会标记为 `ready_for_translation_backend`；人工/本地规则项会标记为 `needs_manual_review`，为后续自动局部重译调度提供入口。
- `experiment_metrics.json` 新增 `repair_request_count`、`repair_backend_request_count`、`repair_manual_request_count` 和 `repair_request_ready_rate`。
- zip 资料包会将 `output/repair_requests.json` / `output/repair_requests.md` 映射为 `质量/局部修复请求.json` / `质量/局部修复请求.md`。
- 双语 HTML 的 QA 证据展示补充 `cells` 字段，便于人工查看表格单元格级问题。
- 新增单元测试覆盖修复请求生成、最小管线写出、实验指标聚合和资料包映射。

### 验证

- `python -m unittest discover -s tests`
- `python -m compileall pdf_translate tests`

### 仍需后续

- 当前修复请求已可进入执行阶段，但执行结果不自动合并回正式译文。
- 后续需要实现补丁合并、修复前后 QA 对比和失败回退记录。

## 局部修复执行结果基础（2026-06-29）

### 已完成

- `pdf_translate.qa.repair` 新增 `build_repair_results()` 与 `write_repair_results()`，可对局部修复请求执行或记录跳过原因。
- 管线默认生成 `output/repair_results.json` 和 `output/repair_results.md`；未开启执行时会把后端可执行请求标记为 `skipped_execution_disabled`，不会调用外部 API。
- CLI `translate` / `run` 新增 `--execute-repairs` 和 `--max-repair-requests`；显式开启后会用当前翻译后端执行 `ready_for_translation_backend` 请求，并将候选修复片段写入 `output/repairs/<request_id>.md`。
- 修复结果记录请求 ID、修复项 ID、chunk、页码、问题类型、动作、范围、执行器、状态、失败原因、结果文件和结果预览；不会自动覆盖原 chunk 译文。
- `experiment_metrics.json` 新增 `repair_executed_request_count`、`repair_succeeded_count`、`repair_failed_count`、`repair_skipped_count` 和 `repair_execution_success_rate`。
- zip 资料包会将 `output/repair_results.json` / `output/repair_results.md` 映射为 `质量/局部修复结果.json` / `质量/局部修复结果.md`，并把 `output/repairs/` 下的候选修复片段归档到 `质量/局部修复片段/`。
- 新增单元测试覆盖默认跳过、dummy 后端执行成功、最小管线写出、实验指标聚合和资料包映射。

### 验证

- `python -m unittest discover -s tests`
- `python -m compileall pdf_translate tests`

### 仍需后续

- 当前候选修复片段只落盘，不参与正式译文合并。
- 后续需要实现修复补丁合并、修复前后 QA 对比、失败回退和真实模型样本实验。

## 局部修复验证门禁基础（2026-06-29）

### 已完成

- `pdf_translate.qa.repair` 新增 `build_repair_validation()` 与 `write_repair_validation()`，可对候选局部修复片段做本地不变量验证。
- 管线默认生成 `output/repair_validation.json` 和 `output/repair_validation.md`；即使没有开启修复执行，也会记录验证跳过原因，保证证据链连续。
- 当前验证会读取 `repair_requests.json` 中的锁定 token 和表格形状证据，并读取 `repair_results.json` / `output/repairs/` 中的候选片段，统计通过、失败、未检查和跳过状态。
- `experiment_metrics.json` 新增修复验证通过数、失败数、跳过数、锁定 token 检查数、缺失锁定 token 数、表格形状验证数，以及 `repair_validation_pass_rate`、`repair_locked_token_pass_rate` 和 `repair_table_shape_validation_pass_rate`。
- zip 资料包会将 `output/repair_validation.json` / `output/repair_validation.md` 映射为 `质量/局部修复验证.json` / `质量/局部修复验证.md`。

### 验证

- `python -m unittest tests.test_structure_ir.StructureIRTests.test_translation_qa_reports_table_cell_token_mismatch`
- `python -m unittest tests.test_structure_ir.StructureIRTests.test_experiment_metrics_aggregates_quality_evidence`
- `python -m unittest tests.test_structure_ir.StructureIRTests.test_pipeline_writes_document_ir_and_structure_manifest`
- `python -m unittest tests.test_structure_ir.StructureIRTests.test_bundle_includes_structure_qa_and_repair_artifacts`

### 仍需后续

- 当前验证只覆盖候选片段级门禁，不自动合并正式译文；后续需要实现修复补丁合并、合并后 QA 对比、失败回退和真实模型样本实验。

## 局部修复独立合并副本基础（2026-06-29）

### 已完成

- `pdf_translate.qa.repair` 新增 `build_repair_merge()` 与 `write_repair_merge()`，可把验证通过且可安全定位的候选修复写入独立修复副本。
- 管线默认生成 `output/repair_merge.json`、`output/repair_merge.md`、`output/repaired_chunks/` 和 `output/repaired_full.md`；不会覆盖原始 `output/chunks/` 或 `output/translated_full.md`。
- 当前合并策略支持 chunk 级整体替换，以及表格类修复中的首个 Markdown 表格替换；无法安全定位、同 chunk 冲突或验证未通过的候选会记录为跳过或需人工合并。
- 管线会对 `output/repaired_chunks/` 再执行一轮译后 QA，生成 `output/repair_merge_qa.json` 和 `output/repair_merge_qa.md`，为修复前后 QA 对比提供机器证据。
- `experiment_metrics.json` 新增 `repair_merge_candidate_count`、`repair_merge_applied_count`、`repair_merge_patched_chunk_count`、`repair_merge_manual_required_count`、`post_repair_issue_count`、`post_repair_issue_delta`、`repair_merge_apply_rate` 和 `post_repair_issue_reduction_rate`。
- zip 资料包会归档 `repair_merge`、`repair_merge_qa`、`repaired_full.md` 和 `repaired_chunks/`，便于导师评审修复副本。

### 验证

- `python -m unittest tests.test_structure_ir.StructureIRTests.test_translation_qa_reports_table_cell_token_mismatch`
- `python -m unittest tests.test_structure_ir.StructureIRTests.test_experiment_metrics_aggregates_quality_evidence`
- `python -m unittest tests.test_structure_ir.StructureIRTests.test_pipeline_writes_document_ir_and_structure_manifest`
- `python -m unittest tests.test_structure_ir.StructureIRTests.test_bundle_includes_structure_qa_and_repair_artifacts`

### 仍需后续

- 当前合并仍是独立副本，不进入正式发布译文；后续需要增加人工确认、正式替换、失败回滚、细粒度多表格定位和真实模型样本实验。

## 运行性能指标基础（2026-06-29）

### 已完成

- 新增 `pdf_translate.run_metrics`，提供事件级 `RunMetricsRecorder`、`build_run_metrics()` 和估算 token 统计。
- 管线默认重写 `output/run_log.jsonl`，按事件记录 `run_start`、阶段计时、每块翻译、跳过块、增量合并和最终合并。
- 管线完成后生成 `output/run_metrics.json`，汇总总耗时、阶段耗时、翻译请求次数、请求字符数、上下文字符数、译文字符数、估算请求 token、估算译文 token、跳过块、翻译后端分布和吞吐指标。
- `experiment_metrics.json` 新增 `performance` 区块，回链 `run_metrics.json` 中的耗时、字符数、估算 token 和请求次数；`breakdowns` 新增阶段耗时、阶段次数、翻译后端分布和跳过原因。
- zip 资料包会将 `output/run_metrics.json` 映射为 `质量/运行性能指标.json`，并保留 `output/run_log.jsonl` 为 `设置/运行日志.jsonl`。
- 新增单元测试覆盖运行指标聚合、实验指标性能区块、echo 后端最小管线写出 `run_metrics.json` / `run_log.jsonl`，以及资料包包含运行性能指标。

### 验证

- `python -m unittest tests.test_structure_ir.StructureIRTests.test_run_metrics_aggregates_stage_chunk_and_token_evidence tests.test_structure_ir.StructureIRTests.test_experiment_metrics_aggregates_quality_evidence tests.test_structure_ir.StructureIRTests.test_pipeline_writes_document_ir_and_structure_manifest tests.test_structure_ir.StructureIRTests.test_bundle_includes_structure_qa_and_repair_artifacts`
- `python -m py_compile pdf_translate/run_metrics.py pdf_translate/pipeline.py pdf_translate/qa/metrics.py pdf_translate/zip_bundle.py tests/test_structure_ir.py`

### 仍需后续

- 当前 token 是按字符数粗估，不等同于真实 tokenizer 计数。
- 可配置成本画像与 HTTP 重试尝试数已在后续条目补充；缓存命中、巡视/修复请求成本和账单级失败 token 成本仍需继续进入成本模型。

## 可配置成本估算基础（2026-06-29）

### 已完成

- 新增 `pdf_translate.costing`，提供成本画像解析、后端模型名解析、成本估算和 `cost_estimate.json` 写出能力。
- `AppConfig` 新增 `PDF_TRANSLATE_COST_PROFILE_JSON`、`PDF_TRANSLATE_COST_PROFILE_PATH` 和 `PDF_TRANSLATE_COST_CURRENCY` 配置；Web 有效配置会保留对应 KV/环境变量。
- 管线在 `run_metrics.json` 后生成 `output/cost_estimate.json`，按估算请求 token、估算响应 token、请求字符数、译文字符数和翻译请求次数计算成本。
- 默认仅内置 `echo` 与 `ollama` 的零外部 API 成本画像；DeepSeek、OpenAI、DeepL 等付费后端价格由用户配置，避免把会变化的价格写死进代码。
- `experiment_metrics.json.performance` 新增 `cost_profile_configured`、`cost_profile_key`、`cost_currency`、`input_token_cost`、`output_token_cost`、`input_char_cost`、`output_char_cost`、`request_cost`、`estimated_total_cost` 和 `estimated_cost_per_chunk`。
- zip 资料包会将 `output/cost_estimate.json` 映射为 `质量/成本估算.json`。
- `SETUP_MANUAL.md` 增加成本画像环境变量和 JSON 示例。

### 验证

- `python -m unittest tests.test_structure_ir.StructureIRTests.test_cost_estimate_uses_configured_backend_profile tests.test_structure_ir.StructureIRTests.test_experiment_metrics_aggregates_quality_evidence tests.test_structure_ir.StructureIRTests.test_pipeline_writes_document_ir_and_structure_manifest tests.test_structure_ir.StructureIRTests.test_bundle_includes_structure_qa_and_repair_artifacts`
- `python -m unittest discover -s tests`
- `python -m py_compile pdf_translate/config.py pdf_translate/costing.py pdf_translate/pipeline.py pdf_translate/qa/metrics.py pdf_translate/zip_bundle.py pdf_translate/server/settings_service.py tests/test_structure_ir.py`

### 仍需后续

- 当前成本基于估算 token/字符，不等同于服务商真实 tokenizer 和账单。
- 翻译块级 HTTP 尝试/重试已在后续条目补充；失败请求 token 是否计费、巡视/修复请求成本、缓存命中折扣和批量折扣尚未进入成本模型。
- 真实样本批量实验应把成本画像版本、价格来源日期和后端账单截图或导出记录一起归档。

## HTTP 重试证据基础（2026-06-29）

### 已完成

- `pdf_translate.translators.http_retry` 新增 `capture_http_retry_events()`，可在不改变现有翻译器调用方式的前提下捕获 `call_with_http_retry()` 的每次 HTTP 尝试。
- 捕获事件只包含上下文、尝试序号、最大尝试次数、状态、耗时、是否继续重试、错误类型和 HTTP 状态码，不写入请求正文、API Key 或模型响应正文。
- 串行和并行翻译路径会把每个 chunk 的 HTTP 尝试事件写入 `run_log.jsonl`，并在 `run_metrics.json.summary` 汇总 `http_attempt_count`、`http_retry_count`、`http_failed_attempt_count`、`http_retryable_error_count` 和 `http_fatal_error_count`。
- `cost_estimate.json.usage` 新增 `billable_request_count` 与来源字段；命中成本画像后，固定 `per_request` 成本优先按 HTTP 尝试次数计算，缺失尝试数时退回成功翻译请求次数。
- `experiment_metrics.json.performance` 新增 HTTP 尝试/重试、可计费请求数和重试率相关字段，用于对比结构分段、表格保护、QA 修复等流程的成本稳定性。
- 单元测试补充 HTTP 重试捕获、运行指标聚合、成本估算、实验指标汇总和 echo 后端最小管线验证。

### 验证

- `python -m unittest tests.test_structure_ir.StructureIRTests.test_http_retry_capture_records_retry_attempts tests.test_structure_ir.StructureIRTests.test_run_metrics_aggregates_stage_chunk_and_token_evidence tests.test_structure_ir.StructureIRTests.test_cost_estimate_uses_configured_backend_profile tests.test_structure_ir.StructureIRTests.test_experiment_metrics_aggregates_quality_evidence tests.test_structure_ir.StructureIRTests.test_pipeline_writes_document_ir_and_structure_manifest`
- `python -m py_compile pdf_translate/translators/http_retry.py pdf_translate/run_metrics.py pdf_translate/costing.py pdf_translate/pipeline.py pdf_translate/qa/metrics.py tests/test_structure_ir.py`

### 仍需后续

- 失败 HTTP 尝试的输入/输出 token 是否计费取决于服务商账单规则，当前只把固定 `per_request` 按尝试次数计入。
- 译前巡视、自动修复请求和后续 OCR/VLM 请求还需要进入统一请求/成本口径。
- 真实样本实验应归档服务商账单、价格来源日期和失败请求样例，校准估算值与真实成本偏差。
