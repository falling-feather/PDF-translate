# 更新规划

> **文档定位**：说明下一阶段产品与技术优化目标、优先级、验收标准和暂不做事项。
> **适用范围**：当前 MVP 到专利原型阶段。
> **最后更新**：2026-07-06
> **维护者**：项目负责人
> **关联文档**：[开发者文档](01-开发者文档.md)、[专利优化方向](03-专利优化方向.md)、[测试与验收指南](05-测试与验收指南.md)、[版本更新状态](07-版本更新状态.md)

## 背景与问题

当前工具已经完成 PDF 上传、正文/参考文献拆分、分块翻译、记忆注入、串联/并联、精品翻译、多用户 Web 工作台和资料包下载。后续如果目标是充分做好并准备专利，重点不应只是“多接几个模型”，而应把工具做成可验证的学术翻译系统：

- 解除对外部 API 的全程依赖，支持本地 OCR、结构解析和本地模型的可选闭环。
- 优化固定页数分块，减少段落、表格、公式和图注被切断的问题。
- 引入结构中间表示，让翻译、QA、局部修复基于同一份结构事实。
- 改善表格、注释、人名、图注和脚注在纯文本抽取后失去排版关系的问题。
- 建立图像、扫描页和低文本页的本地 OCR/VLM 路由，替代单一文本数量启发。
- 将输出从纯 Markdown 扩展为结构化 Markdown、双语 HTML 和可评估的 PDF 译文。
- 把“精品翻译”从更贵模型升级为译前计划、术语确认、结构风险识别和 QA 的流程。
- 形成可量化实验数据，为专利交底和论文/软著材料准备证据。

## 阶段目标

1. 稳定当前 MVP，补齐后端注册表、任务状态边界、生产安全项和成本日志。
2. 做出结构化阅读体验：表格保护、注释归位、人名/机构识别、中英对照输出和 QA 报告。
3. 建立结构感知算法原型：DocumentIR、结构单元分段、动态记忆、结构不变量 QA。
4. 增强本地化能力：OCR/版面解析可本地运行，图像页可路由到 OCR/VLM，翻译后端支持本地模型或私有接口。
5. 建立专利证据链：测试集、对比实验、质量指标、成本与耗时日志。

## 当前状态摘要

已完成能力、版本变更、验证记录已迁入 [版本更新状态](07-版本更新状态.md) 和 [发布历史](06-发布历史.md)。本规划只保留未来仍需推进的任务、方向和验收方式。

当前整体处于“可用 MVP -> 专利原型”的中段：基础翻译、结构解析、表格证据、OCR 路由、普通表格 OCR 文本本地结构化推断、OCR 文本短行合并单元格候选推断、合并候选视觉证据分级、合并候选人工确认审计产物、表格合并候选 Web 轻量确认回写、单条 bbox 页面预览、候选筛选与批量确认、确认后的表格结构发布副本、确认候选结构补丁、图注/脚注/实体结构关系提示注入、译后结构关系保真 QA、表格脚注绑定 QA、双语 HTML 确认补丁渲染、结构化 PDF 确认补丁基础渲染、结构补丁进入单元格级修复定位依据、结构化 JSON OCR 执行器入口、结构化表格/公式 OCR canonical 晋级、术语冲突审核写回、术语冲突 Web 轻量确认入口、术语单条可编辑确认、术语批量逐条编辑确认、术语作用域感知注入、术语确认后重译计划、术语确认后候选重译执行、术语候选重译稿显式发布/回滚、QA 修复、结构化 PDF 译文原型和实验指标已经打底；后端注册表、任务运行态主源、产物摘要、历史任务清理、CLI/Web 状态诊断口径、任务生命周期审计和受控恢复重新入队已完成最小收口，真实样本跑批、复杂 PDF 重排、多人审核流转、恢复策略真实部署观测和生产安全仍是下一步重点。

本轮补充：TABLE-01 已完成确认表格结构副本的下游消费闭环，并进一步把人工确认后的合并单元格候选物化为 `table_structure_patches`。`table_reconstruction_confirmed.json` 现在会被后半段 QA、修复后 QA、双语 HTML 和结构化 PDF 导出优先读取；双语 HTML 与结构化 PDF 都已能只在 confirmed 视图下把补丁应用到译文侧 Markdown 表格，输出基础 `rowspan` / `colspan` 并跳过 covered cell。下一步重点不再是“确认候选能否形成结构事实”，而是让复杂 PDF 图文原位重排、正式表格替换和覆盖式回滚策略继续消费结构补丁。
本轮补充：REPAIR-01/TABLE-01 已把 confirmed 表格结构补丁接入单元格级修复定位依据。译后 QA 会把当前异常 cell 匹配到的 `matched_structure_patches` 收窄写入 evidence，修复请求再提升为 `merge_target.structure_patch_context`、`structure_patch_evidence_count` 和 `structure_patch_source_review_ids`，并随验证、合并、补丁审核和实验指标透传。当前又补齐了发布后的回滚演练副本、`formal_full.md` 正式稿层、覆盖前备份、正式回滚快照、列表式局部修复补丁审核台和补丁前后差异预览；剩余重点继续收束为“真实样本验收、复杂 PDF 正式稿重排和高精度结构识别”。
本轮补充：B2 注释、图注与实体归位已从“结构 QA 证据”推进到“翻译控制提示”。`build_structure_translation_hints()` 会把表格结构提示、DocumentIR 中的图注/脚注父子关系、跨页归属和实体候选统一写入 `TranslationRequest.structure_hints`；`structure_hints_manifest.json` 和 `experiment_metrics.json` 会记录结构关系提示数、跨页关系提示数、实体提示数和关系类型分布。下一步重点是用真实样本验证这些提示是否降低图注/脚注混入正文、专名遗漏和跨页关系断裂。
本轮补充：QA-02/B2 已把结构关系提示继续接入译后保真检查。译后 QA 现在会检查图注、表注、脚注等关系锚点是否在译文中保留，并能把表格脚注标记是否仍停留在对应单元格作为 `table_footnote_binding_mismatch` 记录；修复计划会生成 `rewrite_with_structure_relations` 或 `repair_table_footnote_binding` 请求，实验指标和批量实验摘要也能统计结构关系异常率和表格脚注绑定异常率。下一步重点是用真实论文样本校准误报、漏报和不同语言表述下的锚点识别。
本轮补充：REPAIR-01 已补齐 `repair_effectiveness.json/md` 修复效果对比报告，并把 `repair_effectiveness_*` 接入批量实验摘要、相对基线差值、人工评分 CSV 和评分后专利证据摘要。系统会在原始 `qa_report.json` 与修复后 `repair_merge_qa.json` 之间生成独立对比，记录问题减少率、已解决问题、新增问题、持续存在问题、改善 chunk 和回归 chunk；`experiment_metrics.json`、zip 资料包和批量证据链也会回链该报告。下一步重点从“是否有修复前后记录”收束为“真实模型修复跑批是否稳定降低问题数，并且不会引入新的可读性风险”。
本轮 Web 产物收口补充：`repair_effectiveness.md` 已进入 Web 任务产物摘要、用户端下载入口和管理端产物下载入口；任务列表可显示“修复效果”，后端状态字段会带出问题减少率、修复前后问题数、已解决/新增/回归数量和整体状态。下一步重点不再是“能否看到这份报告”，而是用真实样本跑批和人工评分判断修复效果是否可靠。

本轮补充：UX-01 已从 Web 轻量确认入口推进到“单条术语确认可编辑”“批量逐条编辑确认”“术语作用域感知注入”“确认后重译计划”“候选重译执行”和“候选稿显式发布/回滚”。`MemoryStore.apply_glossary_review_decision()` 可在确认候选时接收人工改写后的 `candidate_zh`，保留原候选和编辑候选审计字段；`GlossaryReviewModal` 可在详情区编辑确认译名、章节作用域和置信度，批量采用时会通过 `items[]` 把每条已选术语的当前编辑值一起提交；后端会拒绝空译名、重复处理和顶层统一改写候选译名。`MemoryStore.glossary_snippet_for_pages()` 已能按页码、章节、结构类型和 block id 筛选当前块术语，串行/并行翻译路径都会从结构 chunk 和最近标题推断注入上下文。术语确认后会生成 `glossary_retranslation_plan.json/md`，按已确认术语、旧译名线索和 chunk 作用域列出建议重译的分块；执行器会消费计划生成 `glossary_retranslated_chunks/`、`glossary_retranslated_full.md` 和 `glossary_retranslation_result.json/md`，显式确认后可生成 `glossary_retranslation_published_full.md`，显式回滚演练可生成 `glossary_retranslation_rollback_full.md`，默认不覆盖原译文。下一步重点不再是“能不能修正候选译名、按作用域注入、判断影响 chunk、生成候选重译稿或显式发布/回滚”，而是补齐多人审核流转、真实论文术语表校准和真实样本发布稿验收。

本轮补充：CHUNK-01 已把跨页页面碎片识别从普通半句、续表和页尾断词推进到“页面边界续接判别精修”。`detect_page_boundary_fragments()` 现在不仅会把 `trans-` / 下一页 `lation` 标记为 `hyphenated_word_continuation` 并使用 `hyphen_elision`，也会把 `BERT-` / 下一页 `based` 这类学术复合词标记为保留连字符的续接，使用 `hyphen_preserve`，避免生成 `BERTbased`；`Smith et al.`、`Eq.`、`Fig.`、`Ref.` 等页尾学术缩写也会进入 `academic_abbreviation_continuation`；页尾公式以 `+`、`=` 等运算符结束或下一页公式以 `\lambda`、`\sum` 等数学续接符号开头时，会进入 `formula_continuation` 并用 `newline` 保留推导续行。短标题式 `Related-` / 下一页 `Work` 会被抑制为非续接，减少标题断行误报。`chunk_boundary_qa.json`、`chunk_strategy_comparison.json` 和 `experiment_metrics.json` 均新增连字符保留/去除细分指标、学术缩写续接指标和公式推导续接切开/保护/降幅指标。下一步重点从“典型误报能否抑制”推进为“用真实论文样本校准缩写列表、复合词前缀、公式续行符号和图注/标题断行边界”。

本轮补充：VISION-01/OCR-01 已把图像页路由从“单页触发理由”升级为“可解释、可汇总、可进实验指标”的上下文证据。`vision_route.json` 会同时记录页面触发原因分布、图像上下文页数、图像-图注上下文页数、疑似图片表格页数和公式低文本页数；`ocr_tasks.json` 会保留继承的 `route_reason_counts` 和上下文任务数；`experiment_metrics.json` 会把这些指标纳入 quality、rates 和 breakdowns。下一步重点不再是“能否解释为什么这页触发 OCR”，而是用真实扫描页、图片表格页和公式密集页校准阈值，并接入 VLM fallback。

近期优先推进：

- 恢复重新入队的真实部署观测和生产安全配置收口。
- 基于 `experiment-samples` 预分组和确认后覆盖度审计开展真实论文样本补齐、批量实验、人工确认和评分录入；当前 `--patent-batch-only` 已能在筛选后摘要中暴露覆盖缺口，下一步重点是补齐真实样本而不是继续手工追表。
- 基于普通表格 OCR 文本本地结构化推断、文本级合并候选推断、表格结构确认发布副本、审核台批量决策、确认视图下游消费、确认候选结构补丁、结构补丁修复定位依据、译后结构关系保真 QA、局部修复效果对比报告、局部修复正式稿替换/回滚和结构化 JSON OCR 执行器，继续让复杂 PDF 重排、高精度表格、公式、OCR、图像页和复杂跨页结构处理消费结构证据。
- 术语确认台多人审核流转、真实术语表校准和真实样本发布稿验收；单条/批量逐条候选译名编辑、置信度、章节作用域记录、按章节/结构类型/block id 注入、确认后重译计划、候选重译执行和候选稿显式发布/回滚已具备基础。
- PDF 译文的复杂图表/脚注/页码关系重排、正式稿替换/回滚的真实样本验收和可读性校准。

## 优先级任务

状态口径：`部分完成` 表示已形成原型证据链，但仍缺真实样本验证、人工确认或正式发布策略；`待做` 表示尚未进入稳定实现。已完成任务统一归档到 [版本更新状态](07-版本更新状态.md)。

本轮补充：TABLE-01/OCR-01 已完成“表格 OCR 结构化任务契约”“公式 OCR 结构化任务契约”“结构化 OCR 结果透传”和“表格/公式 OCR 本地 QA 门禁”的最小闭环：`ocr_tasks.json` 的表格任务会记录 `table_context`、`structure_contract` 和 `writeback.subtarget`，公式任务会记录 `formula_context`、公式结构契约和公式回写子目标；外部或后续本地 OCR 结果中的 `structured_cells`、`cell_bboxes`、`merged_cell_candidates`、`table_footnotes`、`formula_latex`、`formula_tokens`、`equation_labels` 和 `formula_confidence` 会进入 OCR 回写、候选 QA、候选晋级和 `experiment_metrics.json`；候选 QA 会检查表格行列坐标、bbox 覆盖、表格 locked token，以及公式 LaTeX、公式 token、公式编号、置信度和公式 locked token。OCR-01 进一步补齐结构化 JSON stdout 执行器：`structured_json_cli`、`local_ocr`、`local_table_ocr` 和 `local_formula_ocr` 可作为本地适配器标识，命令 stdout 可返回 `ocr-results-v1` 载荷、结果列表或单条 JSON 结果，执行器按 `task_id` 选取结果并保留表格/公式结构字段；普通文本 stdout 仍走旧 OCR 文本候选路径。OCR 候选晋级阶段已能把通过结构门禁的表格 OCR 写入 `block.meta.table`，把通过结构门禁的公式 OCR 写入 `block.meta.formula`，并把 canonical 结构晋级数量与比例写入实验指标和批量证据字段。REPAIR-01 已补齐局部修复发布确认副本、回滚演练副本和正式稿替换/回滚层：默认生成 `repair_publish.json` / `repair_publish.md`、`repair_rollback.json` / `repair_rollback.md`、`repair_formal_replace.json` / `repair_formal_replace.md` 和 `repair_formal_rollback.json` / `repair_formal_rollback.md`，不覆盖原译文；显式 `--publish-repairs` 后才生成 `published_full.md`，显式 `--rollback-repairs` 后才生成 `rollback_full.md`，显式 `--formal-replace-repairs` 后才生成或更新 `formal_full.md` 并保留 `formal_full.before_repair.md`，显式 `--formal-rollback-repairs` 后才恢复正式稿并保留 `formal_full.repair_applied.md`。EXP-01 已补齐 OCR 结构门禁、修复效果对比、修复发布确认、回滚演练、正式稿替换和正式稿回滚的批量证据汇总，`batch_experiment_summary.*`、相对基线差值和 `batch_experiment_review.csv` 都能保留表格/公式门禁通过率、canonical 晋级率、复核数、bbox 覆盖率、公式 token/编号回流数、问题分布、`repair_effectiveness_*`、`repair_publish_*`、`repair_rollback_*`、`repair_formal_replace_*` 与 `repair_formal_rollback_*` 状态和报告路径；填写人工评分后还可通过 `pdf-translate experiment-evidence` 导出 `batch_experiment_evidence.json` 与 `batch_experiment_evidence.md`，汇总表格/公式 OCR 门禁、晋级证据、局部修复效果对比、局部修复发布审核、回滚演练、正式替换和正式回滚摘要。`pdf-translate experiment-samples` 已可在真实跑批前按文本量、图片页、表格数字行、公式符号、多栏块分布、图注/脚注线索和实体候选把 PDF 预分组为普通、表格密集、公式密集、多栏复杂、扫描/低文本和注释/实体密集样本，并生成 `summary.coverage`、Markdown 覆盖度报告和可回读的人工确认字段；`experiment --patent-batch-only` 可只运行人工标记纳入的样本。剩余重点从“能否表达、保留并本地判断表格/公式 OCR 结构结果”转为“用结构化 JSON 适配器接入高精度表格/公式 OCR 引擎、自动生成真实结构化单元格与公式结果、把正式稿层放到真实样本里验收”，以及“按覆盖缺口补齐真实样本、基于确认字段跑批并筛出导师认可的证据样本”。

最新补充：TABLE-01/OCR-01 已增加普通表格 OCR 文本的本地结构化推断。表格区域任务如果只返回 Markdown 管道表或与源表格列数匹配的普通分隔文本，回写阶段会生成 `structured_cells` 并按区域 bbox 估算 `cell_bboxes`；候选 QA 会把这两类推断标记当作审计痕迹而不是自动复核原因，仍由表格结构门禁决定是否可晋级。
最新补充：TABLE-01/OCR-01 进一步补齐 OCR 文本合并单元格候选推断。回写阶段会保留管道表原始行宽，遇到单行标题、短行尾部缺列或空单元格时生成 `merged_cell_candidates`；外部 OCR 旧式 `type` / `row` / `cols` 候选也会补齐统一的 `span_type`、行列坐标、span 和覆盖单元格字段。剩余重点进一步收束为高精度表格 OCR、视觉级合并单元格确认、复杂续表识别、真实扫描样本校准和人工确认后的真正正式替换。
最新补充：TABLE-01/OCR-01 已把 OCR 晋级后的 `block.meta.table.merged_cell_candidates` 接入 `table_reconstruction.json`、`structure_hints_manifest.json` 和翻译请求 `structure_hints`。表格重建阶段会兼容规范化旧式与新式候选字段，并与本地行宽/空单元格候选按几何范围合并去重，使“OCR 文本 -> canonical 表格 -> 表格重建 -> 结构提示”的证据链闭合。剩余重点不再是候选能否传递，而是候选是否能被高精度视觉证据和人工确认升级为正式表格结构。
最新补充：TABLE-01 已补齐合并单元格候选视觉证据分级。`merged_cell_candidates` 会显式区分 `candidate_status`、`visual_evidence_level` 和 `bbox_evidence.status`；外部 span bbox 可以标记为视觉支持，本地估算 bbox 只能标记为估算证据，不能自动变成 `human_confirmed`。剩余重点进一步收束为真实高精度 OCR/版面引擎接入、人工确认入口、确认后真正正式替换和覆盖式回滚。
最新补充：TABLE-01 已把合并单元格候选人工确认审计产物接入 Web 轻量确认闭环，并补齐单条 bbox 页面预览、候选筛选和批量确认。管线在 OCR 晋级后重新生成 `table_reconstruction.json`，随后输出 `table_merged_cell_review.json` 与 `table_merged_cell_review.md`，把每个合并候选整理成带 `review_id`、证据等级、默认决策和人工决策占位的复核清单；用户端和管理端现在可下载清单、读取候选，在列表式审核面板中查看当前候选所在 PDF 页和 bbox 框选，按状态/证据/关键词筛选，并按单条或批量 `review_id` 写回 `confirm`、`reject`、`needs_revision` 或 `clear`。剩余重点从“是否能形成并写回确认清单”转为“键盘流转/多人审核、确认候选结构物化和覆盖式回滚”。
最新补充：TABLE-01 已补齐确认后的表格结构发布副本。管线默认生成 `table_structure_publish.json/md` 待确认报告；Web 显式确认且所有合并候选无待复核项时，才生成 `table_reconstruction_confirmed.json`，并把确认状态进入任务摘要、实验指标和资料包。该副本不覆盖原始 `table_reconstruction.json`；审核项后续被退回时，旧确认副本会失效删除。当前确认副本已接入后半段 QA、双语 HTML 和结构化 PDF 消费链，并能生成可回滚的 `table_structure_patches`；剩余重点转为“补丁驱动复杂重排、真正正式替换和覆盖式回滚”。

本轮 Web 产物收口补充：REPAIR-01 的发布确认报告和人工确认修复发布稿已进入 Web 任务产物摘要；用户端可以下载 `repair_publish.md` 和 `published_full.md`，管理端任务表也能按 `repair_publish` / `repair_published_full` 查看对应产物。补丁审核清单已可通过 Web 列表式审核台读取、筛选、查看详情，并写回单条或批量人工决策；确认发布会读取既有人工决策，若仍有补丁审核阻断项则进入 `blocked_patch_review`，不生成 `published_full.md`。补丁审核详情已补齐前后片段和统一 diff 预览。剩余重点从“Web 是否能看到修复发布产物与补丁变化”转为“真实样本正式稿验收和复杂 PDF 重排策略”。

本轮 Web 人工确认补充：用户端和管理端已能对已完成任务执行“确认修复稿”，后端会消费现有 `repair_merge.json` 与 `repaired_full.md`，生成 `published_full.md`，并把 `repair_publish.json/md` 更新为已确认发布状态，同时写入审计日志。后续又补齐了列表式逐补丁审核、补丁差异预览、正式稿替换和正式回滚；剩余重点转为“真实样本正式稿验收和复杂 PDF 重排策略”。

本轮补丁级审核产物补充：管线会在 `repair_merge` 之后生成 `repair_patch_review.json` 与 `repair_patch_review.md`，把每个修复补丁整理为默认审核结论、有效审核结论、风险等级、是否阻断发布、合并目标、结果片段、差异预览和人工决策字段；Web 用户端与管理端已可下载该报告，并可通过列表式审核台查看前后片段与统一 diff，写回单条或批量 `approve`、`reject`、`needs_revision` 或 `clear`。任务状态和实验指标会统计补丁总数、默认可安全合并数、人工审核数、有效安全数、需人工复核数、阻断发布数、默认结论分布、人工结论分布和风险分布。剩余重点从“是否能留下补丁级审计产物并看清补丁变化”转为“用真实样本验证人工决策对正式稿质量的影响”。

本轮回滚演练补充：REPAIR-01 已新增 `repair_rollback.json/md` 与可选 `rollback_full.md`。管线默认记录回滚条件，CLI 显式 `--rollback-repairs` 或 Web 显式确认后，才会把原始 `translated_full.md` 原子复制为回滚演练稿，并记录原始译文、发布稿和回滚稿 SHA256、`rollback_matches_original` 与 `published_preserved`；该动作不覆盖 `published_full.md`。任务状态、下载入口、管理端产物、zip 资料包、实验指标、批量评分表和评分后专利证据摘要已能汇总回滚可用、已确认、已执行、匹配原文、成功率和状态分布。随后已补齐 `formal_full.md` 正式稿替换与正式回滚层、列表式逐补丁审核台和补丁差异预览，剩余重点从“是否能做正式替换/回滚并看清补丁变化”转为“真实样本验收和复杂 PDF 正式稿重排”。

| 编号 | 优先级 | 状态 | 任务 | 说明 | 验收标准 |
| --- | --- | --- | --- | --- | --- |
| JOB-01 | P0 | 部分完成 | 统一任务状态主源 | 运行态主源已收口到 `JobRegistry` / `web_status.json`，SQLite 保留任务归属与列表索引，管线 `state.json` 保留 chunk 断点续跑；任务列表和详情已暴露产物一致性摘要、管线 state 诊断、恢复状态与 warning；管理员历史任务巡检、孤儿清理和恢复报告已落地；CLI 新增 `web-status` 读取同一诊断口径；提交、启动、取消请求、取消终态、完成、失败、启动恢复扫描和恢复重新入队审计已完成最小闭环；重启前的活动任务可在显式策略下按持久化运行参数重新入队，自定义临时 API、取消标记和旧快照缺参会被跳过 | 重启后任务列表、详情和 CLI 可合并运行态字段、产物摘要和管线断点诊断；管理员可巡检/清理数据库与磁盘漂移，可手动恢复符合条件的恢复任务；后续重点转为真实部署观测、恢复失败提示和公网环境回归 |
| PERF-01 | P0 | 部分完成 | 阶段耗时与 token/字符日志 | 基础事件、阶段耗时、估算 token、成本画像和翻译块级 HTTP 尝试/重试回写已落地；下载打包耗时、巡视/修复请求成本和账单级失败 token 成本仍待补齐 | `run_log.jsonl`、`run_metrics.json` 与 `cost_estimate.json` 能支撑性能与成本分析 |
| SEC-01 | P0 | 部分完成 | 收紧生产默认安全项 | 已新增生产安全预检报告、CLI `security-check`、管理端 `/api/admin/security/preflight`、JWT 过期、生产启动门禁、可配置上传大小、上传流式读取、PDF 魔数校验、zip symlink 边界保护、API Key 不回显和管理端 API Key 可选加密落盘；管理员引导密码、CORS、JWT secret、JWT TTL、数据目录、SQLite 密钥明文/密文状态会进入同一份风险清单 | 对公网部署前可跑配置检查；后续仍需部署验收和真实公网环境回归 |
| CHUNK-01 | P0 | 部分完成 | 从分页切块升级为结构分段 | 已支持 `--chunk-strategy structure`、边界碎片 QA、page/structure 策略对比、OCR 晋级 IR 输入、预算驱动 split reason/预算压力记录、结构关系保护、续表跨页边界受控保护、跨页连字符保留/去除判别、学术缩写续接保护、跨页公式推导续接保护和短标题断行误报抑制；结构 chunk 会写入跨页续接提示，实验指标可统计续表、连字符、学术缩写和公式推导边界切开/保护情况；仍需段落/图注级更细边界合并与真实样本验证 | 页面交接碎片率、重复译文率、半句切断率、预算越界率、结构关系保护次数、续表边界保护率、连字符续接保护率、学术缩写续接保护率和公式推导续接保护率可统计 |
| TABLE-01 | P0 | 部分完成 | 表格与注释结构保护 | 已有表格单元格证据、续表风险、续表合并重建候选、脚注绑定、合并单元格候选、合并候选视觉证据分级、合并候选人工确认审计清单、Web 列表式逐项确认面板、状态/证据/关键词筛选、批量决策写回、单条 bbox 页面预览、确认后的表格结构发布副本、确认视图下游消费、确认候选结构补丁、双语 HTML 补丁渲染、结构化 PDF 补丁基础渲染、结构补丁进入单元格级修复定位依据、结构提示清单、图注/脚注父子关系提示、译后结构关系保真 QA、表格脚注绑定 QA、实体候选弱提示、一页跨页归位、续表跨页分段保护、表格 QA、表格 OCR 结构契约、普通表格 OCR 文本本地结构化推断、OCR 文本短行合并候选推断、OCR 晋级合并候选接入表格重建与结构提示、结构化 OCR 结果透传、本地结构门禁、结构化 JSON OCR 适配入口和结构化表格 OCR canonical 晋级；键盘流转/多人审核、高精度表格 OCR、真实样本校准、复杂续表深度合并、复杂 PDF 原位重排/跨页分页联动、真正正式替换和覆盖式回滚仍待实现 | 表格密集论文输出可读，行列保持率、结构提示覆盖率、结构关系提示覆盖率、结构关系异常率、实体提示覆盖率、脚注绑定率、脚注绑定异常率、续表边界保护率、续表拒绝类别分布、OCR 表格结构晋级率、合并候选证据等级分布、人工确认清单覆盖率、bbox 预览可用率、人工确认写回率、批量审核成功率、表格结构发布率、结构补丁应用率和跨页关系归位率可统计 |
| UX-01 | P1 | 部分完成 | 译前术语确认台 | 已有巡视术语写入、术语冲突待审、QA 标记、后端审核写回和 Web 轻量确认入口；可按待审 key 或 review_id 确认/否决候选译名，支持用户端/管理端筛选、详情、备注、单条和批量写回；单条确认可编辑候选译名、章节作用域和置信度，批量采用可按 `items[]` 为每条术语携带独立候选译名、章节作用域、置信度和备注，并保留原候选/编辑候选审计字段；翻译请求前的术语片段已能按页码、章节、结构类型和 block id 选择性注入；确认术语后可生成 `glossary_retranslation_plan.json/md`，列出命中已确认术语且建议重译的 chunk；已能执行候选重译并生成候选分块、候选全文和执行报告，显式确认后可生成术语重译发布稿，显式回滚演练可恢复原始译文副本，默认不覆盖原译文；尚无多人审核流转和真实论文术语表校准 | 用户可在界面中编辑、确认、否决术语；已确认术语能按结构上下文进入对应 chunk 的提示词，能给出需重译 chunk 清单，并可生成、发布和回滚候选重译稿供人工验收 |
| UX-02 | P1 | 部分完成 | 中英对照输出 | `bilingual.html` 已落地，按块展示原文、译文、QA 问题和修复建议；译文侧 Markdown 表格可消费 confirmed `structure_patches` 输出合并单元格；页码/术语 hover 与 PDF 对照仍待完善 | 译文可对照阅读，页码与术语索引可见 |
| QA-02 | P1 | 部分完成 | 翻译 QA 报告 | 数字、引用、图表编号、Markdown/DocumentIR 表格形状、表格单元格 token、图注/脚注关系锚点、表格脚注绑定、术语一致/冲突、实体候选、公式符号、公式编号、变量/统计表达不变量、重复段落和英文残留已落地；复杂 LaTeX/跨行公式语法树、公式 OCR、真实样本结构关系误报校准与人工确认仍待补齐 | 每次完成后生成 `output/qa_report.md`；公式缺失会进入 `formula_mismatch`，结构关系缺失会进入 `caption_or_footnote_relation_mismatch`，表格脚注错位会进入 `table_footnote_binding_mismatch`，并形成对应局部修复建议；复杂术语确认流程可跟踪 |
| VISION-01 | P1 | 部分完成 | 图像页与扫描页处理 | 已有页面级 OCR/VLM 路由、页面预览、候选区域裁剪、图像/图注/表格/公式上下文 reason 汇总、OCR 调度、结构化 JSON OCR 执行器、结果载荷、回写、候选 QA、晋级 IR 和表格/公式 canonical 结构晋级；VLM fallback 与真实样本阈值校准待做 | 低文本页能说明是否 OCR、是否 VLM、是否跳过，并能汇总触发原因和上下文类型 |
| OCR-01 | P1 | 部分完成 | 本地 OCR/版面解析适配 | 已有可选 tesseract CLI 执行入口、`structured_json_cli` / `local_ocr` / `local_table_ocr` / `local_formula_ocr` 适配入口、`ocr-results-v1` 回灌、表格 OCR 结构化任务契约、公式 OCR 结构化任务契约、路由原因与上下文任务汇总、普通表格 OCR 文本本地结构化推断、OCR 文本合并候选推断、OCR 晋级合并候选进入表格重建、结构提示、人工确认审计清单、Web 轻量确认回写、候选筛选/批量确认、单条 bbox 页面预览和表格结构确认发布副本、结构化结果透传、结构化表格 OCR 本地 QA 门禁、结构化公式 OCR 本地 QA 门禁和通过门禁后的 canonical 结构晋级；高精度表格/公式 OCR、版面分析器、复杂表格结构生成和正式替换仍待接入 | 扫描 PDF 可产出文本层、结构层或 OCR 风险报告，并可解释任务来自哪些页面上下文 |
| OUT-01 | P1 | 部分完成 | 输出形态升级 | 结构化 Markdown、双语 HTML 和结构化 PDF 译文原型已具备基础；双语 HTML 和结构化 PDF 已接入 confirmed 表格结构补丁渲染；当前会生成 `output/translated_full.pdf` 与 `output/translated_pdf_report.json`，并进入实验指标、zip 资料包、下载接口和前端显式入口；PDF 报告已记录 caption/footnote、表格脚注单元格绑定、续表组、结构关系输出证据和结构补丁渲染数量；复杂图表原位重排、跨页合并单元格分页联动和真实样本可读性校准仍待完善 | 表格、图注、脚注和页码关系在阅读产物中清晰 |
| MEM-01 | P2 | 待做 | 术语库导入导出 | 支持 TSV/TBX/TMX 或简化格式导入导出 | 同一用户/领域可复用术语资产 |
| INTEG-01 | P2 | 待做 | 学术工作流集成 | arXiv URL、Obsidian Markdown、批量文件夹、Zotero 轻集成 | 小规模文献项目可以批量翻译和归档 |
| REPAIR-01 | P2 | 部分完成 | QA 驱动局部修复 | 已形成修复计划、请求、执行结果、验证门禁、独立合并副本、补丁级审核产物、补丁前后差异预览、修复效果对比报告、人工决策写回、批量决策写回、发布门禁、合并后 QA、显式发布确认副本、回滚演练副本、正式稿替换和正式稿回滚层；表格单元格 token 修复请求可携带 confirmed 结构补丁上下文并进入验证、合并、补丁审核和实验指标；默认生成 `repair_patch_review.json/md`、`repair_effectiveness.json/md`、`repair_publish.json/md`、`repair_rollback.json/md`、`repair_formal_replace.json/md` 与 `repair_formal_rollback.json/md`，`--publish-repairs` 或 Web 显式确认只有在补丁审核无阻断项时才生成 `published_full.md`，`--rollback-repairs` 或 Web 显式确认才生成 `rollback_full.md`，`--formal-replace-repairs` 或 Web 显式确认才生成 `formal_full.md` 与 `formal_full.before_repair.md`，`--formal-rollback-repairs` 或 Web 显式确认才恢复正式稿并保留 `formal_full.repair_applied.md`；批量实验评分表和证据摘要已能汇总补丁审核统计、修复效果对比、发布确认状态、开放合并问题数、回滚可用/已执行/匹配原文状态、正式替换/正式回滚状态、补丁审核报告路径、修复效果报告路径、发布报告路径、回滚报告路径和相关稿件路径；Web 任务产物摘要、用户端下载、管理端下载、列表式逐补丁审核台、确认发布动作、回滚演练动作、正式替换动作和正式回滚动作已能暴露补丁审核报告、修复效果报告、修复报告、发布稿、正式稿、备份稿、回滚报告与回滚演练稿，并可查看源文摘要、当前译文、修复候选、合并后片段和统一 diff；真实模型跑批效果对比、真实样本验收和复杂 PDF 正式稿重排仍待做 | 成本低于全文重译，修复前后有独立对比记录，表格修复可统计按证据定位的补丁数量与比例，结构补丁支撑修复数量、修复效果减少率、已解决/新增/回归问题、补丁默认结论/人工结论/有效结论/风险分布、发布门禁状态、Web 确认动作、批量证据摘要、正式替换状态、正式回滚状态、回滚演练状态与回滚基线可审计 |

## 几大核心方向

### 方向一：算法级翻译流程

这是最适合作为专利主线的方向。核心不是某个模型，而是流程控制算法：

```text
本地结构识别
  -> 结构单元分段
  -> 译前术语与风险计划
  -> 动态记忆注入
  -> 分层翻译
  -> 结构不变量 QA
  -> 局部修复
```

验收重点是每一步都有中间结果、指标和可复现对比。

### 方向二：表格、注释、人名和图注处理

当前纯文本抽取会把表格行列、脚注、图注、人名机构信息打散，导致 Markdown 可读性差。该方向要解决“文本属于哪里”的问题：

- 表格保留行列、表头、单位、脚注、显著性符号。
- 图注和表注保留与图表编号的关系。
- 人名、机构名、数据集名、模型名进入实体识别和术语记忆。
- 页脚注释、括注、参考引用不能混入正文段落。

验收重点是结构保持率和阅读产物可读性。

### 方向三：图像、扫描页和 OCR/VLM 路由

现有“按每页文字数量低于平均值来预估是否调用图像模型”的方法只能作为粗略启发。后续应综合页面文本量、文本区域面积、图片区域、页面截图 OCR 置信度、图注和表格截图等信息，决定是否本地 OCR、是否 VLM、是否只保留图像占位。

验收重点是低文本页不漏处理，且不会对普通文本页过度调用昂贵模型。

### 方向四：分段替代分页

分页切块会制造页面交接碎片，尤其是跨页段落、表格续页、公式推导和图注。后续分块策略应从“页”转向“结构单元”：

- 段落、标题和章节优先。
- 表格、公式、图注不可切分。
- 跨页未完段落用顺延机制接续。
- 根据上下文预算动态调整块大小。

验收重点是半句切断率、重复译文率、漏译率和块间衔接评分。

### 方向五：输出形态从 Markdown 扩展到 HTML/PDF

Markdown 适合轻量资料包，但复杂论文需要更强排版表达：

- 结构化 Markdown：保留标题、表格、图注、脚注。
- 双语 HTML：适合对照阅读、术语 hover、块级定位、QA 标记。
- PDF 译文：适合导师评审和正式传播，当前已有结构化重排原型，后续重点是复杂图表、脚注和页码关系的可读性评审，不先追求完全原坐标回填。

## 本地化 OCR 与 API 依赖解除路线

本地化不是一次性替换所有模型 API，而是分层降低依赖：

1. **本地解析**：优先把 PDF 文本、表格、注释、图注、公式边界在本地抽取，外部 API 只负责必要翻译。
2. **本地 OCR**：对扫描页、低文本页、图片型表格使用本地 OCR，输出 OCR 置信度与风险标记。
3. **本地结构 QA**：数字、公式、引用、表格维度、图表编号等 QA 规则本地运行，不调用模型。
4. **本地排版输出**：HTML/PDF 生成尽量本地完成，减少把排版恢复交给模型。
5. **本地翻译后端**：Ollama/私有 OpenAI 兼容接口作为可选翻译端，先保证接口一致性。
6. **混合路由**：普通文本走本地/低成本模型，高风险表格、图像页或复杂段落再调用强模型。

验收重点不是“完全无 API”，而是可证明 OCR、结构解析、QA、排版输出已经本地化，翻译执行器可替换。

## 分段式翻译算法优化路线

当前固定页数滑窗简单可靠，但不能感知论文结构。建议分四步升级：

1. **段落边界保护**：先从纯文本层识别段落、标题、公式编号和引用编号，避免明显半句切块。
2. **跨页续段识别**：识别页面末尾未完句、表格续页、图注续页和公式推导续行，交给顺延机制处理。
3. **结构原子分块**：引入 DocumentIR 后，把表格、图注、公式环境、注释和脚注作为不可切分对象。
4. **预算驱动分块**：按模型上下文预算、术语数量、前文摘要长度和结构风险动态决定块大小。

核心验收指标：

- 重复译文率下降。
- 块间断裂/漏译率下降。
- 术语一致率提升。
- 页面交接碎片率下降。
- 单块失败可恢复，且局部重译不破坏全局合并。

## 依赖与风险

- OCR/版面解析会引入额外模型或原生依赖，Windows 部署复杂度会上升。
- 表格结构识别可能需要多工具组合，不能指望单一库覆盖所有论文。
- 本地大模型翻译质量和速度受显卡、内存限制，不能作为唯一默认方案。
- 专利方向需要新颖性检索，工程创新点不直接等同于可授权权利要求。
- 如果过早追求 PDF 原排版回写，项目会被拖入重型排版系统，偏离当前轻量优势。

## 不做事项

- 本阶段不整体重写为 C++。
- 本阶段不优先做完整 PDF 原排版回写。
- 本阶段不把“精品翻译”仅定义为更贵模型。
- 本阶段不盲目接入大量翻译后端，先统一注册表和质量闭环。
- 本阶段不承诺完全离线翻译，先做到解析、OCR、QA 的本地化可选能力。

## 验收方式

- 自动测试：核心纯函数和管线边界必须有不依赖外部 API 的测试。
- 人工评审：准备普通论文、表格密集论文、公式密集论文、多栏复杂论文样本。
- 指标记录：单次任务优先读取 `output/experiment_metrics.json`、`output/run_metrics.json` 和 `output/cost_estimate.json`。
- 文档同步：功能完成后，把完成事实写入 [版本更新状态](07-版本更新状态.md) 和 [发布历史](06-发布历史.md)。

## 待确认问题

- 本地 OCR 首选引擎是 PaddleOCR、Tesseract、RapidOCR，还是通过 MinerU/Marker/Docling 统一接入。
- PDF 译文复杂排版重排和可读性评审优先级如何安排。
- 术语确认台是否作为所有模式前置步骤，还是只绑定精品翻译。
- 专利原型的最小实验集规模和导师验收口径。
