# 更新规划

> **文档定位**：说明下一阶段产品与技术优化目标、优先级、验收标准和暂不做事项。
> **适用范围**：当前 MVP 到专利原型阶段。
> **最后更新**：2026-07-06
> **维护者**：项目负责人
> **关联文档**：[开发者文档](01-开发者文档.md)、[专利优化方向](03-专利优化方向.md)、[测试与验收指南](05-测试与验收指南.md)、[版本更新状态](07-版本更新状态.md)

## 背景与问题

当前工具已经完成 PDF 上传、正文/参考文献拆分、分块翻译、记忆注入、串联/并联、精品翻译、多用户 Web 工作台和资料包下载。后续如果目标是充分做好并准备专利，重点不应只是“多接几个模型”，而应把工具做成可验证的学术翻译系统：

- 解除对外部 API 的全程依赖，支持本地 OCR、结构解析和本地模型的可选闭环。
- 优化固定页数分块，减少段落、表格、公式和图注被切断的问题。
- 引入结构中间表示，让翻译、QA、局部修复基于同一份结构事实。
- 改善表格、注释、人名、图注和脚注在纯文本抽取后失去排版关系的问题。
- 建立图像、扫描页和低文本页的本地 OCR/VLM 路由，替代单一文本数量启发。
- 将输出从纯 Markdown 扩展为结构化 Markdown、双语 HTML 和可评估的 PDF 译文。
- 把“精品翻译”从更贵模型升级为译前计划、术语确认、结构风险识别和 QA 的流程。
- 形成可量化实验数据，为专利交底和论文/软著材料准备证据。

## 阶段目标

1. 稳定当前 MVP，补齐后端注册表、任务状态边界、生产安全项和成本日志。
2. 做出结构化阅读体验：表格保护、注释归位、人名/机构识别、中英对照输出和 QA 报告。
3. 建立结构感知算法原型：DocumentIR、结构单元分段、动态记忆、结构不变量 QA。
4. 增强本地化能力：OCR/版面解析可本地运行，图像页可路由到 OCR/VLM，翻译后端支持本地模型或私有接口。
5. 建立专利证据链：测试集、对比实验、质量指标、成本与耗时日志。

## 当前状态摘要

已完成能力、版本变更、验证记录已迁入 [版本更新状态](07-版本更新状态.md) 和 [发布历史](06-发布历史.md)。本规划只保留未来仍需推进的任务、方向和验收方式。

当前整体处于“可用 MVP -> 专利原型”的中段：基础翻译、结构解析、表格证据、OCR 路由、普通表格 OCR 文本本地结构化推断、OCR 文本短行合并单元格候选推断、结构化 JSON OCR 执行器入口、结构化表格/公式 OCR canonical 晋级、QA 修复、结构化 PDF 译文原型和实验指标已经打底；后端注册表、任务运行态主源、产物摘要、历史任务清理、CLI/Web 状态诊断口径已完成最小收口，真实样本跑批、复杂 PDF 重排、术语确认台、端到端恢复审计和生产安全仍是下一步重点。

近期优先推进：

- 端到端任务恢复审计和生产安全配置收口。
- 基于 `experiment-samples` 预分组和覆盖度缺口开展真实论文样本补齐、批量实验、人工确认和评分录入。
- 基于普通表格 OCR 文本本地结构化推断、文本级合并候选推断和结构化 JSON OCR 执行器，继续接入高精度表格、公式、OCR、图像页和复杂跨页结构处理。
- 术语确认台、置信度管理和章节作用域。
- PDF 译文的复杂图表/脚注/页码关系重排、修复发布确认 UI、正式替换策略和失败回滚策略。

## 优先级任务

状态口径：`部分完成` 表示已形成原型证据链，但仍缺真实样本验证、人工确认或正式发布策略；`待做` 表示尚未进入稳定实现。已完成任务统一归档到 [版本更新状态](07-版本更新状态.md)。

本轮补充：TABLE-01/OCR-01 已完成“表格 OCR 结构化任务契约”“公式 OCR 结构化任务契约”“结构化 OCR 结果透传”和“表格/公式 OCR 本地 QA 门禁”的最小闭环：`ocr_tasks.json` 的表格任务会记录 `table_context`、`structure_contract` 和 `writeback.subtarget`，公式任务会记录 `formula_context`、公式结构契约和公式回写子目标；外部或后续本地 OCR 结果中的 `structured_cells`、`cell_bboxes`、`merged_cell_candidates`、`table_footnotes`、`formula_latex`、`formula_tokens`、`equation_labels` 和 `formula_confidence` 会进入 OCR 回写、候选 QA、候选晋级和 `experiment_metrics.json`；候选 QA 会检查表格行列坐标、bbox 覆盖、表格 locked token，以及公式 LaTeX、公式 token、公式编号、置信度和公式 locked token。OCR-01 进一步补齐结构化 JSON stdout 执行器：`structured_json_cli`、`local_ocr`、`local_table_ocr` 和 `local_formula_ocr` 可作为本地适配器标识，命令 stdout 可返回 `ocr-results-v1` 载荷、结果列表或单条 JSON 结果，执行器按 `task_id` 选取结果并保留表格/公式结构字段；普通文本 stdout 仍走旧 OCR 文本候选路径。OCR 候选晋级阶段已能把通过结构门禁的表格 OCR 写入 `block.meta.table`，把通过结构门禁的公式 OCR 写入 `block.meta.formula`，并把 canonical 结构晋级数量与比例写入实验指标和批量证据字段。REPAIR-01 已补齐局部修复发布确认副本：默认生成 `repair_publish.json` / `repair_publish.md` 记录待确认状态，不覆盖原译文；显式 `--publish-repairs` 后才生成 `published_full.md`，并保留 `translated_full.md` 作为回滚基线。EXP-01 已补齐 OCR 结构门禁和修复发布确认的批量证据汇总，`batch_experiment_summary.*`、相对基线差值和 `batch_experiment_review.csv` 都能保留表格/公式门禁通过率、canonical 晋级率、复核数、bbox 覆盖率、公式 token/编号回流数、问题分布、`repair_publish_*` 发布确认状态、开放合并问题数和修复发布报告路径；填写人工评分后还可通过 `pdf-translate experiment-evidence` 导出 `batch_experiment_evidence.json` 与 `batch_experiment_evidence.md`，汇总表格/公式 OCR 门禁、晋级证据和局部修复发布审核摘要。`pdf-translate experiment-samples` 已可在真实跑批前按文本量、图片页、表格数字行、公式符号、多栏块分布、图注/脚注线索和实体候选把 PDF 预分组为普通、表格密集、公式密集、多栏复杂、扫描/低文本和注释/实体密集样本，并生成 `summary.coverage`、Markdown 覆盖度报告和可回读的人工确认字段；`experiment --patent-batch-only` 可只运行人工标记纳入的样本。剩余重点从“能否表达、保留并本地判断表格/公式 OCR 结构结果”转为“用结构化 JSON 适配器接入高精度表格/公式 OCR 引擎、自动生成真实结构化单元格与公式结果、把修复发布确认做成 Web 可审阅 UI 和正式替换/失败回滚策略”，以及“按覆盖缺口补齐真实样本、基于确认字段跑批并筛出导师认可的证据样本”。

最新补充：TABLE-01/OCR-01 已增加普通表格 OCR 文本的本地结构化推断。表格区域任务如果只返回 Markdown 管道表或与源表格列数匹配的普通分隔文本，回写阶段会生成 `structured_cells` 并按区域 bbox 估算 `cell_bboxes`；候选 QA 会把这两类推断标记当作审计痕迹而不是自动复核原因，仍由表格结构门禁决定是否可晋级。
最新补充：TABLE-01/OCR-01 进一步补齐 OCR 文本合并单元格候选推断。回写阶段会保留管道表原始行宽，遇到单行标题、短行尾部缺列或空单元格时生成 `merged_cell_candidates`；外部 OCR 旧式 `type` / `row` / `cols` 候选也会补齐统一的 `span_type`、行列坐标、span 和覆盖单元格字段。剩余重点进一步收束为高精度表格 OCR、视觉级合并单元格确认、复杂续表识别、真实扫描样本校准和人工确认后的正式替换。

本轮 Web 产物收口补充：REPAIR-01 的发布确认报告和人工确认修复发布稿已进入 Web 任务产物摘要；用户端可以下载 `repair_publish.md` 和 `published_full.md`，管理端任务表也能按 `repair_publish` / `repair_published_full` 查看对应产物。补丁审核清单已可通过 Web 读取并写回单条人工决策，确认发布会读取既有人工决策，若仍有补丁审核阻断项则进入 `blocked_patch_review`，不生成 `published_full.md`。剩余重点从“Web 是否能看到修复发布产物”转为“做出更完整的逐补丁审核台、正式替换策略和失败回滚演练”。

本轮 Web 人工确认补充：用户端和管理端已能对已完成任务执行“确认修复稿”，后端会消费现有 `repair_merge.json` 与 `repaired_full.md`，生成 `published_full.md`，并把 `repair_publish.json/md` 更新为已确认发布状态，同时写入审计日志。剩余重点从“能否在 Web 里发起确认”转为“审核前逐项查看修复补丁、按问题拒绝/通过、正式替换策略和失败回滚演练”。

本轮补丁级审核产物补充：管线会在 `repair_merge` 之后生成 `repair_patch_review.json` 与 `repair_patch_review.md`，把每个修复补丁整理为默认审核结论、有效审核结论、风险等级、是否阻断发布、合并目标、结果片段和人工决策字段；Web 用户端与管理端已可下载该报告，并可通过轻量审核入口写回 `approve`、`reject`、`needs_revision` 或 `clear`。任务状态和实验指标会统计补丁总数、默认可安全合并数、人工审核数、有效安全数、需人工复核数、阻断发布数、默认结论分布、人工结论分布和风险分布。剩余重点从“是否能留下补丁级审计产物”转为“做成更直观的逐补丁批准/拒绝审核台、把人工决策接入正式替换策略，并完成失败回滚演练”。

| 编号 | 优先级 | 状态 | 任务 | 说明 | 验收标准 |
| --- | --- | --- | --- | --- | --- |
| JOB-01 | P0 | 部分完成 | 统一任务状态主源 | 运行态主源已收口到 `JobRegistry` / `web_status.json`，SQLite 保留任务归属与列表索引，管线 `state.json` 保留 chunk 断点续跑；任务列表和详情已暴露产物一致性摘要、管线 state 诊断、恢复状态与 warning；管理员历史任务巡检、孤儿清理和恢复报告已落地；CLI 新增 `web-status` 读取同一诊断口径；端到端创建/取消/完成/失败审计仍待补齐 | 重启后任务列表、详情和 CLI 可合并运行态字段、产物摘要和管线断点诊断；管理员可巡检/清理数据库与磁盘漂移；后续补齐端到端恢复审计 |
| PERF-01 | P0 | 部分完成 | 阶段耗时与 token/字符日志 | 基础事件、阶段耗时、估算 token、成本画像和翻译块级 HTTP 尝试/重试回写已落地；下载打包耗时、巡视/修复请求成本和账单级失败 token 成本仍待补齐 | `run_log.jsonl`、`run_metrics.json` 与 `cost_estimate.json` 能支撑性能与成本分析 |
| SEC-01 | P0 | 部分完成 | 收紧生产默认安全项 | 已新增生产安全预检报告、CLI `security-check`、管理端 `/api/admin/security/preflight`、JWT 过期、生产启动门禁、可配置上传大小、上传流式读取、PDF 魔数校验、zip symlink 边界保护、API Key 不回显和管理端 API Key 可选加密落盘；管理员引导密码、CORS、JWT secret、JWT TTL、数据目录、SQLite 密钥明文/密文状态会进入同一份风险清单 | 对公网部署前可跑配置检查；后续仍需部署验收和真实公网环境回归 |
| CHUNK-01 | P0 | 部分完成 | 从分页切块升级为结构分段 | 已支持 `--chunk-strategy structure`、边界碎片 QA、page/structure 策略对比、OCR 晋级 IR 输入、预算驱动 split reason/预算压力记录、结构关系保护和续表跨页边界受控保护；结构 chunk 会写入跨页续接提示，实验指标可统计续表边界切开/保护情况；仍需段落/图注级更细边界合并与真实样本验证 | 页面交接碎片率、重复译文率、半句切断率、预算越界率、结构关系保护次数和续表边界保护率可统计 |
| TABLE-01 | P0 | 部分完成 | 表格与注释结构保护 | 已有表格单元格证据、续表风险、续表合并重建候选、脚注绑定、合并单元格候选、结构提示清单、一页跨页归位、续表跨页分段保护、表格 QA、表格 OCR 结构契约、普通表格 OCR 文本本地结构化推断、OCR 文本短行合并候选推断、结构化 OCR 结果透传、本地结构门禁、结构化 JSON OCR 适配入口和结构化表格 OCR canonical 晋级；高精度合并单元格视觉确认、真实样本校准、高精度表格 OCR、复杂续表深度合并和人工确认后的正式单元格级修复仍待实现 | 表格密集论文输出可读，行列保持率、结构提示覆盖率、脚注绑定率、续表边界保护率、续表拒绝类别分布、OCR 表格结构晋级率和跨页关系归位率可统计 |
| UX-01 | P1 | 部分完成 | 译前术语确认台 | 已有巡视术语写入、术语冲突待审和 QA 标记；尚无可编辑确认台、置信度管理和章节作用域 | 用户可编辑、确认、否决术语并启动翻译 |
| UX-02 | P1 | 部分完成 | 中英对照输出 | `bilingual.html` 已落地，按块展示原文、译文、QA 问题和修复建议；页码/术语 hover 与 PDF 对照仍待完善 | 译文可对照阅读，页码与术语索引可见 |
| QA-02 | P1 | 部分完成 | 翻译 QA 报告 | 数字、引用、图表编号、Markdown/DocumentIR 表格形状、表格单元格 token、术语一致/冲突、实体候选、公式符号、公式编号、变量/统计表达不变量、重复段落和英文残留已落地；复杂 LaTeX/跨行公式语法树、公式 OCR 与人工确认仍待补齐 | 每次完成后生成 `output/qa_report.md`；公式缺失会进入 `formula_mismatch` 和公式邻近段落修复建议；复杂术语确认流程可跟踪 |
| VISION-01 | P1 | 部分完成 | 图像页与扫描页处理 | 已有页面级 OCR/VLM 路由、页面预览、候选区域裁剪、OCR 调度、结构化 JSON OCR 执行器、结果载荷、回写、候选 QA、晋级 IR 和表格/公式 canonical 结构晋级；VLM fallback 与真实样本阈值校准待做 | 低文本页能说明是否 OCR、是否 VLM、是否跳过 |
| OCR-01 | P1 | 部分完成 | 本地 OCR/版面解析适配 | 已有可选 tesseract CLI 执行入口、`structured_json_cli` / `local_ocr` / `local_table_ocr` / `local_formula_ocr` 适配入口、`ocr-results-v1` 回灌、表格 OCR 结构化任务契约、公式 OCR 结构化任务契约、普通表格 OCR 文本本地结构化推断、OCR 文本合并候选推断、结构化结果透传、结构化表格 OCR 本地 QA 门禁、结构化公式 OCR 本地 QA 门禁和通过门禁后的 canonical 结构晋级；高精度表格/公式 OCR、版面分析器、复杂表格结构生成和人工确认后正式替换仍待接入 | 扫描 PDF 可产出文本层、结构层或 OCR 风险报告 |
| OUT-01 | P1 | 部分完成 | 输出形态升级 | 结构化 Markdown、双语 HTML 和结构化 PDF 译文原型已具备基础；当前会生成 `output/translated_full.pdf` 与 `output/translated_pdf_report.json`，并进入实验指标、zip 资料包、下载接口和前端显式入口；PDF 报告已记录 caption/footnote、表格脚注单元格绑定、续表组和结构关系输出证据；复杂图表原位重排和真实样本可读性校准仍待完善 | 表格、图注、脚注和页码关系在阅读产物中清晰 |
| MEM-01 | P2 | 待做 | 术语库导入导出 | 支持 TSV/TBX/TMX 或简化格式导入导出 | 同一用户/领域可复用术语资产 |
| INTEG-01 | P2 | 待做 | 学术工作流集成 | arXiv URL、Obsidian Markdown、批量文件夹、Zotero 轻集成 | 小规模文献项目可以批量翻译和归档 |
| REPAIR-01 | P2 | 部分完成 | QA 驱动局部修复 | 已形成修复计划、请求、执行结果、验证门禁、独立合并副本、补丁级审核产物、人工决策写回、发布门禁、合并后 QA 和显式发布确认副本；默认生成 `repair_patch_review.json/md` 与 `repair_publish.json/md`，`--publish-repairs` 或 Web 显式确认只有在补丁审核无阻断项时才生成 `published_full.md`，不覆盖原译文；批量实验评分表和证据摘要已能汇总补丁审核统计、发布确认状态、开放合并问题数、补丁审核报告路径、发布报告路径和发布稿路径；Web 任务产物摘要、用户端下载、管理端下载、轻量审核入口和确认发布动作已能暴露补丁审核报告、修复报告与发布稿；更完整的逐补丁审核台、真正正式替换、失败回滚演练和真实模型效果对比仍待做 | 成本低于全文重译，修复前后有对比记录，表格修复可统计按证据定位的补丁数量与比例，补丁默认结论/人工结论/有效结论/风险分布、发布门禁状态、Web 确认动作、批量证据摘要与回滚基线可审计 |

## 几大核心方向

### 方向一：算法级翻译流程

这是最适合作为专利主线的方向。核心不是某个模型，而是流程控制算法：

```text
本地结构识别
  -> 结构单元分段
  -> 译前术语与风险计划
  -> 动态记忆注入
  -> 分层翻译
  -> 结构不变量 QA
  -> 局部修复
```

验收重点是每一步都有中间结果、指标和可复现对比。

### 方向二：表格、注释、人名和图注处理

当前纯文本抽取会把表格行列、脚注、图注、人名机构信息打散，导致 Markdown 可读性差。该方向要解决“文本属于哪里”的问题：

- 表格保留行列、表头、单位、脚注、显著性符号。
- 图注和表注保留与图表编号的关系。
- 人名、机构名、数据集名、模型名进入实体识别和术语记忆。
- 页脚注释、括注、参考引用不能混入正文段落。

验收重点是结构保持率和阅读产物可读性。

### 方向三：图像、扫描页和 OCR/VLM 路由

现有“按每页文字数量低于平均值来预估是否调用图像模型”的方法只能作为粗略启发。后续应综合页面文本量、文本区域面积、图片区域、页面截图 OCR 置信度、图注和表格截图等信息，决定是否本地 OCR、是否 VLM、是否只保留图像占位。

验收重点是低文本页不漏处理，且不会对普通文本页过度调用昂贵模型。

### 方向四：分段替代分页

分页切块会制造页面交接碎片，尤其是跨页段落、表格续页、公式推导和图注。后续分块策略应从“页”转向“结构单元”：

- 段落、标题和章节优先。
- 表格、公式、图注不可切分。
- 跨页未完段落用顺延机制接续。
- 根据上下文预算动态调整块大小。

验收重点是半句切断率、重复译文率、漏译率和块间衔接评分。

### 方向五：输出形态从 Markdown 扩展到 HTML/PDF

Markdown 适合轻量资料包，但复杂论文需要更强排版表达：

- 结构化 Markdown：保留标题、表格、图注、脚注。
- 双语 HTML：适合对照阅读、术语 hover、块级定位、QA 标记。
- PDF 译文：适合导师评审和正式传播，当前已有结构化重排原型，后续重点是复杂图表、脚注和页码关系的可读性评审，不先追求完全原坐标回填。

## 本地化 OCR 与 API 依赖解除路线

本地化不是一次性替换所有模型 API，而是分层降低依赖：

1. **本地解析**：优先把 PDF 文本、表格、注释、图注、公式边界在本地抽取，外部 API 只负责必要翻译。
2. **本地 OCR**：对扫描页、低文本页、图片型表格使用本地 OCR，输出 OCR 置信度与风险标记。
3. **本地结构 QA**：数字、公式、引用、表格维度、图表编号等 QA 规则本地运行，不调用模型。
4. **本地排版输出**：HTML/PDF 生成尽量本地完成，减少把排版恢复交给模型。
5. **本地翻译后端**：Ollama/私有 OpenAI 兼容接口作为可选翻译端，先保证接口一致性。
6. **混合路由**：普通文本走本地/低成本模型，高风险表格、图像页或复杂段落再调用强模型。

验收重点不是“完全无 API”，而是可证明 OCR、结构解析、QA、排版输出已经本地化，翻译执行器可替换。

## 分段式翻译算法优化路线

当前固定页数滑窗简单可靠，但不能感知论文结构。建议分四步升级：

1. **段落边界保护**：先从纯文本层识别段落、标题、公式编号和引用编号，避免明显半句切块。
2. **跨页续段识别**：识别页面末尾未完句、表格续页、图注续页，交给顺延机制处理。
3. **结构原子分块**：引入 DocumentIR 后，把表格、图注、公式环境、注释和脚注作为不可切分对象。
4. **预算驱动分块**：按模型上下文预算、术语数量、前文摘要长度和结构风险动态决定块大小。

核心验收指标：

- 重复译文率下降。
- 块间断裂/漏译率下降。
- 术语一致率提升。
- 页面交接碎片率下降。
- 单块失败可恢复，且局部重译不破坏全局合并。

## 依赖与风险

- OCR/版面解析会引入额外模型或原生依赖，Windows 部署复杂度会上升。
- 表格结构识别可能需要多工具组合，不能指望单一库覆盖所有论文。
- 本地大模型翻译质量和速度受显卡、内存限制，不能作为唯一默认方案。
- 专利方向需要新颖性检索，工程创新点不直接等同于可授权权利要求。
- 如果过早追求 PDF 原排版回写，项目会被拖入重型排版系统，偏离当前轻量优势。

## 不做事项

- 本阶段不整体重写为 C++。
- 本阶段不优先做完整 PDF 原排版回写。
- 本阶段不把“精品翻译”仅定义为更贵模型。
- 本阶段不盲目接入大量翻译后端，先统一注册表和质量闭环。
- 本阶段不承诺完全离线翻译，先做到解析、OCR、QA 的本地化可选能力。

## 验收方式

- 自动测试：核心纯函数和管线边界必须有不依赖外部 API 的测试。
- 人工评审：准备普通论文、表格密集论文、公式密集论文、多栏复杂论文样本。
- 指标记录：单次任务优先读取 `output/experiment_metrics.json`、`output/run_metrics.json` 和 `output/cost_estimate.json`。
- 文档同步：功能完成后，把完成事实写入 [版本更新状态](07-版本更新状态.md) 和 [发布历史](06-发布历史.md)。

## 待确认问题

- 本地 OCR 首选引擎是 PaddleOCR、Tesseract、RapidOCR，还是通过 MinerU/Marker/Docling 统一接入。
- PDF 译文复杂排版重排和可读性评审优先级如何安排。
- 术语确认台是否作为所有模式前置步骤，还是只绑定精品翻译。
- 专利原型的最小实验集规模和导师验收口径。
