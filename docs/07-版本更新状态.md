# 版本更新状态

> **文档定位**：集中记录当前版本状态、已完成任务、可验证能力和下一步缺口。
> **适用范围**：当前 MVP 到专利原型阶段的版本进展追踪。
> **最后更新**：2026-07-06
> **维护者**：项目负责人
> **关联文档**：[更新规划](02-更新规划.md)、[发布历史](06-发布历史.md)、[测试与验收指南](05-测试与验收指南.md)

## 当前总体状态

整体完成度约 **97%**。

基础产品闭环已经成型：PDF 上传、拆分、翻译、资料包下载、Web 工作台、结构分析、QA 报告、公式不变量检查、实验指标和结构化 PDF 译文都有可运行基础。生产安全预检、JWT 过期、生产启动门禁、上传上限配置、上传流式校验、PDF 魔数校验、zip symlink 边界、API Key 不回显和管理端 API Key 加密落盘基础已经补入。表格 OCR 结构化任务契约、公式 OCR 结构化任务契约、普通表格 OCR 文本本地结构化推断、OCR 文本短行合并单元格候选推断、OCR 晋级合并候选接入表格重建与结构提示、图注/脚注父子关系提示注入、译后结构关系保真 QA、表格脚注绑定 QA、实体候选弱提示、合并候选视觉证据分级、合并候选人工确认审计清单、表格合并候选 Web 列表式确认面板、单条 bbox 页面预览、候选筛选与批量决策、确认后的表格结构发布副本、确认视图下游消费、确认候选结构补丁物化、双语 HTML 确认补丁渲染、结构化 PDF 确认补丁基础渲染、结构补丁进入单元格级修复定位依据、结构化结果透传、本地结构门禁、结构化 JSON OCR 执行器入口和通过门禁后的 canonical 表格/公式结构晋级已经形成最小闭环，并已进入实验指标、批量实验摘要、相对基线差值、人工评审 CSV、zip 资料包和评分后专利证据摘要；局部修复已补到补丁级审核产物、列表式逐补丁审核台、补丁前后差异预览、修复效果对比、单条/批量人工决策写回、发布门禁、显式发布确认副本、回滚演练副本、正式稿替换和正式稿回滚，默认不覆盖原译文，显式 `--publish-repairs` 或 Web 显式确认只有在补丁审核无阻断项时才生成 `published_full.md`，显式 `--rollback-repairs` 或 Web 显式确认才生成 `rollback_full.md`，显式 `--formal-replace-repairs` 或 Web 显式确认才生成 `formal_full.md` 与 `formal_full.before_repair.md`，显式 `--formal-rollback-repairs` 或 Web 显式确认才恢复正式稿并保留 `formal_full.repair_applied.md`；批量实验评分表和专利证据摘要也能汇总补丁审核统计、修复效果对比、结构补丁支撑修复统计、结构关系提示覆盖、结构关系异常率、表格脚注绑定异常率、实体提示覆盖、修复发布状态、回滚演练状态、正式替换/正式回滚状态、开放合并问题和报告路径；Web 任务状态、用户端和管理端已能显示、下载补丁审核报告、修复效果对比报告、表格合并候选确认清单、表格结构发布报告、确认后的表格重建副本、修复发布报告、发布稿、回滚报告、回滚演练稿、正式稿、正式替换报告、正式回滚报告和修复前备份，并通过列表式审核入口查看候选页图、bbox 框选、筛选候选和批量写回人工决策，局部修复补丁也可通过列表式审核台按阻断状态、风险和关键词筛选，查看源文摘要、当前译文、修复候选、合并后片段、统一 diff、文件路径与定位信息，逐条或批量通过/拒绝/复查/清空；术语冲突也已具备用户端和管理端 Web 轻量确认入口，可查看待审术语、筛选、填写备注并单条或批量确认/否决，单条确认和批量采用时都可人工修正每条候选译名、章节作用域和置信度，翻译请求前的术语片段也能按页码、章节、结构类型和 block id 选择性注入，确认后还能生成 `glossary_retranslation_plan.json/md`，指出命中已确认术语并建议重译的 chunk。显式确认表格结构后还能在已有 chunk 产物基础上刷新结构化 PDF 译文报告；任务生命周期审计已覆盖提交、启动、取消请求、取消终态、完成、失败、启动恢复扫描和恢复重新入队，重启前活动任务可在显式策略下按持久化参数受控重新入队。批量实验前的样本清单、覆盖度审计、Markdown 人工确认报告、确认字段贯通和确认样本过滤跑批已经补齐，可把论文目录预分组为普通、表格密集、公式密集、多栏复杂、扫描/低文本和注释/实体密集样本，并允许导师确认后的类型/标签和纳入标记直接控制批量实验，降低真实跑批前的整理成本。面向专利原型还差真实样本验证、复杂 PDF 图表/脚注/页码重排、术语确认后自动重译执行、恢复真实部署观测、正式稿真实样本验收，以及更高精度的表格/公式/OCR/图像处理。

本轮新增状态：`table_reconstruction_confirmed.json` 已从“可生成、可下载、可统计”推进到“可被后半段 QA、修复后 QA、双语 HTML 和结构化 PDF 导出消费”，并进一步把人工确认后的合并单元格候选物化为 `table_structure_patches`。双语 HTML 和结构化 PDF 现在都会在 confirmed 视图下把补丁应用到译文侧 Markdown 表格，输出基础 `rowspan` / `colspan` 并跳过 covered cell；表格单元格 token QA 也会把当前异常 cell 匹配到的 confirmed patch 提升为修复请求、合并和补丁审核的定位证据。局部修复已新增 `repair_rollback.json/md`、`rollback_full.md`、`repair_formal_replace.json/md`、`formal_full.md`、`formal_full.before_repair.md`、`repair_formal_rollback.json/md`、`formal_full.repair_applied.md`、列表式逐补丁审核台和补丁前后差异预览，剩余缺口从“能否正式替换/回滚/逐项审核/查看差异”收敛为“真实样本验收和复杂 PDF 正式稿重排”。
本轮新增状态：QA-02/B2 已补齐译后结构关系保真 QA。当前可检查图注/表注/脚注关系锚点是否被译文保留，也能检查表格脚注标记是否仍留在对应目标单元格；异常会进入 `caption_or_footnote_relation_mismatch` 或 `table_footnote_binding_mismatch`，并继续进入修复计划、修复请求、实验指标和批量实验相对基线差值。剩余缺口从“结构关系能否提示模型”推进为“真实样本里这些提示和 QA 是否稳定降低错位”。
本轮新增状态：JOB-01 已补齐任务生命周期审计和受控恢复重新入队最小闭环。`job_submit`、`job_started`、`job_cancel_requested`、`job_cancelled`、`job_done`、`job_error`、`job_hydration_report` 和 `job_recovery_requeued` 会把任务状态、阶段、耗时、错误码、翻译模式、并发数、原始文件名、关键产物路径、恢复扫描摘要和重新入队原因写入审计日志；重启前仍处于 `queued` / `running` 的任务可在显式策略下用 `web_status.json` 中的持久化运行参数重新入队，用户临时自定义 API、取消标记和旧快照缺参会被跳过；剩余缺口收敛为“真实部署观测和服务重启演练”。
本轮新增状态：EXP-01 已补齐确认后样本覆盖度审计。`--patent-batch-only` 完成人工纳入样本筛选后，manifest、summary 和评分后 evidence 会同步写入 `selected_coverage` / `patent_batch_coverage`，直接说明这批已选样本在普通、表格、公式、多栏、扫描和注释/实体 6 类上的数量与缺口；剩余缺口从“筛选后是否还要手工追覆盖度”收敛为“补齐真实论文样本并录入导师评分”。

本轮新增状态：UX-01 已补齐术语确认单条可编辑译名入口、批量逐条编辑确认、术语作用域注入和确认后重译计划。`pending_review.json` 中的 `glossary_conflict` 可按 `dedupe_key` 或 Web `review_id` 确认/否决，确认时可人工覆盖候选译名，并在待审项和活动术语中保留 `original_candidate_zh` / `edited_candidate_zh`；审核人、时间、备注、置信度和章节作用域会进入待审项与已确认术语，译后 QA 可在缺失术语证据中透传这些信息。用户端和管理端已能读取术语审核报告、筛选待审项、查看详情、填写备注，单条确认时编辑确认译名、章节作用域和置信度；批量采用会把选中术语的逐条编辑值通过 `items[]` 写回，后端仍拒绝顶层统一改写候选译名，并在审计中记录 `candidate_zh_changed_count`。翻译请求前的术语片段已能按页码、章节、结构类型和 block id 选择性注入，结构 chunk 会从 `block_ids`、`block_types` 和最近标题推断上下文。确认术语后会生成 `glossary_retranslation_plan.json/md`，按确认译名、旧译名线索和 chunk 作用域统计命中分块、建议重译分块和无需重译分块，用户端、管理端和 ZIP 资料包均可下载。剩余缺口收敛为自动重译执行、多人审核流转和真实术语表校准。

粗略拆分如下：

| 模块 | 完成度 | 说明 |
| --- | --- | --- |
| 基础 PDF 翻译 MVP | 约 88% | 主流程可用，任务运行态主源、产物一致性摘要、CLI/Web 状态诊断、恢复重新入队、管理员孤儿清理、JWT 过期和生产安全预检已最小收口 |
| 专利算法原型 | 约 92% | DocumentIR、结构分段、续表跨页边界保护、QA、译后结构关系保真 QA、表格脚注绑定 QA、公式不变量检查、术语作用域感知注入、术语确认后重译计划、局部修复、补丁级审核产物、列表式逐补丁审核台、补丁差异预览、修复效果对比、单条/批量人工决策写回、发布门禁、修复发布确认副本、修复回滚演练副本、正式稿替换/回滚层、结构化 PDF 译文、PDF 结构关系输出审计、表格/公式 OCR 结构契约、普通表格 OCR 文本本地结构化推断、OCR 文本合并候选推断、OCR 晋级合并候选接入表格重建与结构提示、合并候选视觉证据分级、合并候选人工确认审计清单、Web 列表式确认面板、状态/证据/关键词筛选、批量决策写回、单条 bbox 页面预览、表格结构确认发布副本、确认候选结构补丁、双语 HTML 和结构化 PDF 补丁渲染、结构补丁支撑单元格级修复定位、结构化结果透传、本地结构门禁、结构化 JSON OCR 执行器、canonical 结构晋级、实验指标、批量证据摘要、修复效果对比摘要、修复发布审核摘要、回滚演练摘要、正式替换摘要和正式回滚摘要已打底 |
| 复杂结构处理 | 约 75% | 表格、续表、脚注、跨页关系、表格/公式 OCR 结构契约、普通表格 OCR 文本本地结构化推断、OCR 文本短行合并候选推断、OCR 晋级合并候选进入表格重建、结构提示、人工确认审计清单、Web 轻量确认回写、候选筛选/批量确认、单条 bbox 页面预览、确认发布副本和结构补丁、双语 HTML 与结构化 PDF 合并单元格基础渲染、结构补丁支撑单元格级修复定位、合并候选视觉证据分级、结构化结果透传、本地结构门禁、结构化 OCR 接入口和表格/公式 canonical 结构晋级已有证据链，高精度处理仍需加强 |
| OCR/图像页处理 | 约 64% | 路由、预览、任务、结构化表格/公式任务契约、普通表格 OCR 文本本地结构化推断、OCR 文本合并候选推断、OCR 晋级合并候选进入表格重建、结构提示、人工确认审计清单和 Web 轻量确认回写、单条 bbox 页面预览、合并候选视觉证据分级、结构化 OCR 结果透传、结构化 JSON stdout 执行器、本地结构门禁、回写链路和 canonical 结构晋级已有，真实 OCR/VLM 效果仍需校准 |
| 正式发布能力 | 约 83% | 后端注册表、任务运行态主源、产物摘要、任务存储巡检、恢复诊断、恢复重新入队、PDF 译文下载接口、表格确认清单下载/轻量写回、候选筛选/批量确认、单条 bbox 页面预览、表格结构确认发布副本、补丁审核报告、补丁列表式审核台、补丁差异预览、补丁单条/批量人工决策写回、修复发布确认副本、修复回滚演练副本、正式稿替换/回滚报告、正式稿/备份稿/回滚前修复稿 Web 下载入口、Web 确认发布门禁、Web 回滚演练动作、Web 正式替换动作、Web 正式回滚动作、批量修复发布审核、回滚演练、正式替换和正式回滚证据、JWT 过期、生产门禁、上传/资料包边界、安全预检和管理端密钥加密已有基础，仍需部署验收、复杂 PDF 正式稿重排和真实公网环境回归 |

## 版本状态总表

| 方向 | 状态 | 当前能力 |
| --- | --- | --- |
| 基础 PDF 翻译 | 已完成基础闭环 | 支持上传、正文/参考文献拆分、分块翻译、记忆注入、串行/并行翻译、精品翻译和资料包下载 |
| 多后端与任务恢复 | 部分完成 | 已支持 OpenAI 兼容、DeepSeek、Ollama、DeepL、echo 等路径；后端统一注册表已落地，任务运行态主源、产物摘要、管线 state 诊断、CLI/Web 同口径状态查询、生命周期审计和恢复重新入队已完成最小收口 |
| 结构化解析与分段 | 部分完成 | 已有 DocumentIR、结构分段、边界 QA、续表跨页边界受控保护、page/structure 对比和预算驱动记录；真实样本边界合并仍需验证 |
| 表格与注释保护 | 部分完成 | 已有表格单元格证据、续表候选、续表边界保护、脚注绑定、合并单元格候选、合并候选视觉证据分级、合并候选人工确认审计清单、Web 轻量逐项确认回写、状态/证据/关键词筛选、批量决策写回、单条 bbox 页面预览、表格结构确认发布副本、确认候选结构补丁、双语 HTML 和结构化 PDF 补丁渲染、结构补丁进入单元格级修复定位依据、结构提示清单、单元格 token QA、表格 OCR 结构契约、普通表格 OCR 文本本地结构化推断、OCR 文本短行合并候选推断、OCR 晋级合并候选接入表格重建与结构提示、结构化结果透传、本地结构门禁、结构化 JSON OCR 接入口和表格 canonical 结构晋级 |
| OCR 与图像页 | 部分完成 | 已有 OCR/VLM 路由、页面预览、候选裁剪、OCR 任务、表格 OCR 任务结构契约、公式 OCR 任务结构契约、普通表格 OCR 文本本地结构化推断、OCR 文本合并候选推断、OCR 晋级合并候选进入表格重建、结构提示、人工确认审计清单、Web 轻量确认回写、候选筛选/批量确认、单条 bbox 页面预览和表格结构确认发布副本、结构化 OCR 结果透传、结构化 JSON stdout 执行器、本地结构门禁、结果回写、候选 QA、晋级 IR 和表格/公式 canonical 结构晋级 |
| 翻译 QA 与局部修复 | 部分完成 | 已有 QA 报告、译后结构关系保真检查、表格脚注绑定检查、公式符号/编号/变量不变量检查、修复计划、修复请求、执行结果、验证门禁、修复合并副本、修复效果对比、结构补丁支撑的表格单元格修复定位、补丁级审核产物、列表式逐补丁审核台、人工单条/批量决策写回、发布确认门禁、发布确认副本、回滚演练副本、正式稿替换、正式稿回滚、Web 修复补丁审核/修复效果/发布/回滚/正式替换/正式回滚产物下载、Web 显式确认生成发布稿、回滚稿、正式稿和备份稿、批量补丁审核、修复效果对比摘要、修复发布审核、回滚演练摘要、正式替换/正式回滚摘要和合并后 QA |
| 专利实验证据链 | 部分完成 | 已有 `experiment_metrics.json`、批量实验入口、样本清单与覆盖度审计、样本 Markdown 人工确认报告、样本确认字段贯通、确认样本过滤跑批、确认后样本覆盖度审计、多维人工评分表、表格/公式 OCR 结构门禁批量汇总、表格结构补丁统计、补丁审核统计、修复效果对比汇总、修复发布审核汇总、回滚演练汇总和评分后证据摘要导出；真实论文跑批、评分录入和导师验收样本仍待补齐 |
| 输出阅读形态 | 部分完成 | 结构化 Markdown、`bilingual.html` 和 `translated_full.pdf` 已具备基础，`bilingual.html` 与结构化 PDF 已能消费 confirmed 表格结构补丁合并显示译文表格单元格，用户端/管理端已补译文 PDF 下载入口；PDF 报告已记录图注、脚注、续表、结构关系输出证据和结构补丁渲染数量；复杂图表原位重排、跨页合并单元格分页联动与页码关系精修仍待完善 |
| Web、部署与安全 | 部分完成 | 已有 `security-check`、管理端安全预检、JWT 过期、生产启动门禁、API Key 不回显、管理端 API Key 可选加密落盘、上传大小配置、上传流式校验、PDF 魔数校验、zip symlink 边界保护、任务生命周期审计和受控恢复重新入队；部署验收、真实公网回归和恢复观测仍待补 |

## 进行中任务进展

| 编号 | 状态 | 当前进展 | 剩余缺口 |
| --- | --- | --- | --- |
| JOB-01 | 部分完成 | `JobRegistry` / `web_status.json` 已作为 Web 运行态主源；用户任务列表、管理员任务列表、任务详情和 CLI `web-status` 会合并运行状态、产物摘要、管线 state 诊断和恢复 warning；管理员可巡检/清理数据库与磁盘漂移，并查看启动恢复报告；状态快照新增 schema 版本和原子写入；恢复时不信任快照内 `work_dir` 或不匹配的 `job_id`；提交、启动、取消请求、取消终态、完成、失败、启动恢复扫描和恢复重新入队审计已落地；重启前活动任务可在显式策略下按持久化运行参数重新入队，自定义临时 API、取消标记和旧快照缺参会被跳过 | 仍需补齐真实部署观测、恢复失败提示和服务重启演练 |
| SEC-01 | 部分完成 | 已完成生产安全预检、JWT 过期、生产模式启动门禁、上传上限配置、API Key 不回显、管理端 API Key 可选加密落盘、上传流式读取、PDF 魔数校验、超限残片清理和 zip symlink 边界保护；新增认证、安全预检、密钥存储、上传与资料包边界测试 | 仍需补齐部署验收和真实公网环境回归 |

## 已完成任务归档

以下任务已从 [更新规划](02-更新规划.md) 中移出，后续只在本文件和 [发布历史](06-发布历史.md) 中追踪：

| 编号 | 状态 | 完成内容 | 验证方式 |
| --- | --- | --- | --- |
| QA-01 | 已完成 | 增加核心单元测试，覆盖 DocumentIR、结构分段、表格重建、OCR 候选、QA、局部修复、运行指标、成本估算和 echo 最小管线 | `python -m unittest discover -s tests` |
| ERR-01 | 已完成 | 建立结构化错误码核心闭环，接入 HTTP retry、翻译工厂、Web 任务状态、运行指标和前端错误展示 | 单元测试与 Web 状态字段验证 |
| EXP-01 | 已完成 | 增加批量专利实验入口，可批量运行多 PDF、多策略实验矩阵；样本清单与覆盖度审计可在跑批前自动预分组 PDF，识别注释/实体密集样本，并生成 Markdown 人工确认报告、确认字段、确认样本过滤跑批、确认后覆盖度审计、摘要和多维人工评分表 | `samples.csv`、`samples.json`、`samples.md`、`summary.coverage`、`confirmed_pdf_type`、`--patent-batch-only`、`sample_filter.selected_coverage`、`patent_batch_coverage`、`batch_experiment_manifest.json`、`batch_experiment_summary.*`、`batch_experiment_review.csv`、`batch_experiment_evidence.*` |
| IR-01 | 已完成 | 建立轻量 DocumentIR，记录页面、块、类型、文本、bbox、锁定 token、父子关系、实体候选、页面风险和 OCR 增强副本 | 管线可从 IR 生成结构 chunks |
| ARCH-01 | 已完成 | 建立统一后端能力注册表，CLI、Web、管理端、翻译工厂、成本模型名和段尾顺延能力共用同一份后端定义 | `tests/test_backend_registry.py` 与全量单元测试 |

## 已完成里程碑

### 2026-06-29

- 完成结构层基础原型：DocumentIR、结构分段器、结构 QA、表格重建、边界碎片检测和实验指标基础。
- 完成 OCR/VLM 路由基础：页面预览、候选裁剪、OCR 任务、OCR 结果载荷、结果回写、候选 QA 和晋级 IR。
- 完成 QA 驱动局部修复基础：修复计划、修复请求、候选结果、验证门禁、修复合并副本和合并后 QA。
- 完成批量专利实验入口：多 PDF、多策略运行，输出实验清单、摘要和人工评分 CSV。
- 完成预算驱动结构分段和一页跨页关系归位，开始减少分页切块带来的页面交接碎片。
- 完成结构化错误码核心闭环，错误信息可以进入日志、任务状态、指标和前端提示。

### 2026-06-30

- 完成表格脚注单元格级绑定证据，脚注可以尝试归属到具体表格单元格。
- 完成续表拒绝原因分布聚合，可以解释续表为什么合并或拒绝合并。
- 完成合并单元格候选证据和提示注入，把疑似合并风险传入翻译提示。
- 完成 `structure_hints_manifest.json`，让每个 chunk 实际使用的结构提示可审计。
- 完成表格修复目标定位证据，局部修复可以按 QA 证据定位目标 Markdown 表格。

### 2026-07-01

- 完成公式不变量 QA 基础：`qa_report` 可检查公式符号、公式编号、带上下标变量、常见指标名和统计表达缺失，`formula_mismatch` 会进入公式邻近段落修复请求，实验指标和批量评审 CSV 会保留公式缺失数量与缺失率。
- 完成统一后端能力注册表：新增 `pdf_translate/translators/registry.py`，集中声明后端 id、展示名称、配置字段、默认启用、自定义 API 支持和段尾顺延能力。
- CLI、Web 用户端、管理端、翻译工厂、成本估算和管线能力判断已接入同一份 registry。
- 用户自定义 API 后端从固定 DeepSeek 扩展为 registry 中允许的 OpenAI 兼容、DeepSeek、Ollama 和 DeepL。
- 完成任务运行态主源最小收口：`web_status.json` schema、原子写入、重启恢复、路径边界和任务列表状态合并已落地。
- 任务产物一致性摘要已进入任务列表和任务详情，管理端会显示产物状态并禁用不可下载按钮。
- 管理员任务存储巡检与孤儿清理已落地，可识别数据库缺目录和目录缺数据库索引，并跳过运行中任务。
- 完成 CLI/Web 状态诊断最小收口：任务列表、详情和 `pdf-translate web-status` 共用运行态、产物摘要、管线 state 诊断和恢复 warning。
- 新增 `tests/test_job_status.py`，覆盖任务状态快照、重启恢复、恢复报告、列表合并、缺失状态快照、完成态缺失译文的产物异常、管线 state 诊断、CLI `web-status`、存储漂移识别和路径穿越清理保护。
- 完成生产安全预检基础：新增 `pdf-translate security-check`、`/api/admin/security/preflight`、`security-preflight-v1` 报告、可配置上传上限和 API Key 不回显。
- 新增 `tests/test_security_preflight.py`，覆盖默认风险报告、加固环境、非法上传上限、本地密钥存储提示、管理端设置快照不泄露密钥和 CLI 同口径输出。
- 完成上传与资料包边界加固：Web 上传改为流式读取，增加 PDF 魔数校验、超限残片清理、上传审计字节数记录和 zip symlink 外链跳过。
- 新增 `tests/test_web_upload_security.py` 与 `tests/test_zip_bundle_security.py`，覆盖上传安全边界和资料包 symlink 边界。
- 完成 JWT 过期与生产启动门禁：token 新增 `iat`/`exp`，安全预检新增 JWT TTL 摘要与非法配置检查，生产模式下 Web app 会阻断高/中风险配置。
- 新增 `tests/test_auth_deps.py`，扩展 `tests/test_security_preflight.py`，覆盖 token 过期、缺 `exp` 拒绝、TTL 配置和生产门禁。
- 完成管理端 API Key 加密存储基础：设置 `PDF_TRANSLATE_SECRET_KEY` 或 `PDF_TRANSLATE_SECRET_KEY_FILE` 后，新保存的 API Key 会以 `enc:v1:` 密文落盘；旧明文保持兼容读取，安全预检会区分明文、密文、可解密和不可解密状态。
- 新增 `tests/test_secret_store.py`，扩展 `tests/test_security_preflight.py`，覆盖密钥加密写入、旧明文兼容、缺失/错误加密密钥预警和报告不泄露密钥值。
- 完成 CHUNK-01/TABLE-01 续表跨页边界受控保护：`structure_qa.json` 记录续接类型和续接动作，结构 chunk 会注入跨页续接提示并标记 `protected_table_continuation`，`chunk_boundary_qa.json` / `experiment_metrics.json` 可统计续表边界切开、保护和相对固定页分段的降幅。
- 完成 OUT-01 结构化 PDF 译文输出原型：管线生成 `output/translated_full.pdf` 与 `output/translated_pdf_report.json`，并接入实验指标、批量实验记录、zip 资料包、Web 下载接口和任务产物摘要。
- 完成 OUT-01 前端显式下载入口：用户任务卡片和管理端任务表均可展示译文 PDF 可用状态，并直接下载结构化 PDF 译文。
- 完成 OUT-01 结构关系输出审计：`translated_pdf_report.json` 与 PDF 正文会记录 chunk 关联的表格、图注、脚注、续表组和受保护结构关系，并进入 `experiment_metrics.json`。
- 完成 EXP-01 多维人工评分表：`batch_experiment_review.csv` 可记录 Markdown/HTML/PDF 可读性、表格可读性、图文脚注排版、术语一致性、结构连贯性和专利证据采纳结论。
- 完成 TABLE-01/OCR-01 表格 OCR 结构化任务契约：`ocr_tasks.json` 表格区域任务写入 `table_context`、`structure_contract` 和 `writeback.subtarget`，OCR 回写、候选 QA、候选晋级和实验指标可透传或统计表格结构上下文。
- 完成 TABLE-01/OCR-01 结构化 OCR 结果透传：外部或后续本地 OCR 结果中的 `structured_cells`、`cell_bboxes`、`merged_cell_candidates` 和 `table_footnotes` 会进入 OCR 回写、候选 QA、候选晋级和实验指标。
- 完成 TABLE-01/OCR-01 结构化表格 OCR 本地门禁：候选 QA 会检查结构化单元格行列坐标、行列数、bbox 覆盖和 locked token，完整结构化表格候选可通过门禁，缺关键证据的候选进入复核或阻断。
- 完成 OCR-01 公式 OCR 结构化任务契约与本地门禁：`ocr_tasks.json` 公式区域任务写入 `formula_context`、公式结构契约和 `writeback.subtarget`，OCR 回写、候选 QA、候选晋级和实验指标会透传 `formula_latex`、`formula_tokens`、`equation_labels` 与 `formula_confidence`；候选 QA 会检查公式 LaTeX、公式 token、公式编号、置信度和 locked token。
- 完成 OCR-01 本地结构化 JSON 执行器适配：`structured_json_cli`、`local_ocr`、`local_table_ocr` 和 `local_formula_ocr` 可作为本地适配器入口，命令 stdout 可返回普通文本或结构化 JSON，结构化结果会按 `task_id` 匹配并保留表格/公式字段。
- 完成 OCR-01 结构化 OCR canonical 晋级：通过表格门禁的 `structured_cells` 会写入晋级 IR 的 `block.meta.table`，通过公式门禁的 `formula_latex` / `formula_tokens` / `equation_labels` 会写入 `block.meta.formula`，并进入实验指标、批量摘要、人工评审 CSV 和评分后证据摘要。
- 完成 EXP-01/OCR 结构门禁批量证据汇总：`batch_experiment_summary.json`、`batch_experiment_summary.md`、相对基线差值和 `batch_experiment_review.csv` 会保留结构化表格 OCR 门禁通过率、复核数、bbox 覆盖率、结构化公式 OCR 门禁通过率、公式 token/编号回流数和问题分布，便于后续真实样本进入专利证据筛选。
- 完成 EXP-01 人工评分证据摘要导出：填写 `batch_experiment_review.csv` 后可通过 `pdf-translate experiment-evidence` 生成 `batch_experiment_evidence.json` 与 `batch_experiment_evidence.md`，汇总入选证据、评分均值、表格/公式 OCR 结构门禁、失败运行和产物路径。
- 完成 EXP-01 样本清单生成入口：`pdf-translate experiment-samples` 可扫描 PDF 文件或目录，输出 `samples.csv` 和样本分析 JSON，按启发式指标预分组普通、表格密集、公式密集、多栏复杂和扫描/低文本样本。
- 完成 EXP-01 样本覆盖度审计：样本分析新增注释/实体密集分类和 `summary.coverage`，可提示申请前测试集在普通、表格、公式、多栏、扫描、注释/实体 6 类上的数量缺口。
- 完成 EXP-01 样本覆盖度 Markdown 报告：`experiment-samples` 默认生成 `samples.md`，用于人工确认覆盖缺口、样本类型和关键指标。
- 完成 EXP-01 样本人工确认字段贯通：`samples.csv` 可填写 `confirmed_pdf_type`、`confirmed_tags`、`include_in_patent_batch`、`reviewer` 和 `review_notes`，批量实验会优先采用确认类型/标签并把确认信息写入 manifest、记录和评分表。
- 完成 EXP-01 确认样本过滤跑批：`pdf-translate experiment --patent-batch-only` 会只运行人工标记纳入的样本，并在批量 JSON/Markdown 摘要中记录输入、选中和跳过数量。

### 2026-07-05

- 完成 REPAIR-01 局部修复补丁审核产物：管线生成 `repair_patch_review.json/md`，记录补丁默认审核结论、风险等级、是否阻断发布、合并目标、结果片段和人工决策占位；Web 用户端/管理端可下载该报告，批量实验和证据摘要会汇总补丁审核统计。
- 完成 REPAIR-01 局部修复补丁人工决策门禁：Web 可读取补丁审核清单并写回单条 `approve` / `reject` / `needs_revision` / `clear` 决策；发布确认会保留既有人工决策，存在补丁审核阻断项时写入 `blocked_patch_review`，不生成 `published_full.md`。
- 完成 REPAIR-01 局部修复发布确认副本：管线默认生成 `repair_publish.json` / `repair_publish.md` 待确认报告，CLI 显式 `--publish-repairs` 后才生成 `published_full.md`，原始 `translated_full.md` 保留为回滚基线；发布确认请求状态、开放合并问题和发布率进入实验指标与资料包。
- 完成 EXP-01/REPAIR-01 批量实验修复发布审核证据汇总：`batch_experiment_review.csv`、`batch_experiment_summary.json` 和 `batch_experiment_evidence.json/md` 会保留 `repair_publish_*` 状态、开放合并问题、发布报告路径和发布状态分布，便于真实样本跑批后筛选可进入专利交底的修复证据。
- 完成 Web 修复发布产物状态与下载入口：任务产物摘要会暴露修复发布确认报告、发布请求状态、开放合并问题、回滚可用性和人工确认发布稿；用户端和管理端均可下载 `repair_publish.md` 与 `published_full.md`。
- 完成 Web 修复稿人工确认发布：用户端和管理端可对已完成任务显式确认，后端生成 `published_full.md`、更新 `repair_publish.json/md` 并写入审计日志，同时保留 `translated_full.md` 作为回滚基线。

### 2026-07-06

- 完成 UX-01 术语冲突审核写回基础：`MemoryStore.apply_glossary_review_decision()` 可确认或否决待审术语冲突，确认后更新活动术语译名，否决后保留原术语；审核人、时间、备注、置信度和章节作用域会写入 `glossary.json` / `pending_review.json`，译后 QA 可透传已确认术语元数据。
- 完成 UX-01 术语冲突 Web 轻量确认入口：`MemoryStore.build_glossary_review_report()` 汇总术语表与待审项，Web 接口支持读取、单条确认/否决和批量确认/否决；用户端和管理端共用 `GlossaryReviewModal`，可筛选、查看详情、填写备注并写回人工决策。
- 完成 UX-01 术语确认单条可编辑译名：单条确认可覆盖候选译名并保留原候选/编辑候选审计字段，Web 详情区可编辑确认译名、章节作用域和置信度；后端拒绝空译名、重复处理和批量统一改写候选译名。
- 完成 UX-01 术语确认批量逐条编辑：批量接口支持 `items[]` 为每条术语携带独立候选译名、章节作用域、置信度和备注，前端批量采用会提交当前选中术语的编辑态；后端在写入前统一校验空译名、置信度和重复 review_id，避免明显输入错误造成半批写入。
- 完成 UX-01 术语作用域感知注入：`glossary_snippet_for_pages()` 可按页码、章节、结构类型和 block id 过滤术语；串行/并行翻译路径会从结构 chunk 与最近标题推断上下文，`chunks_manifest.json` 记录 `section_scopes`。
- 完成 UX-01 术语确认后重译计划：术语单条或批量确认后会生成 `glossary_retranslation_plan.json/md`，按已确认术语、旧译名线索、chunk 作用域和译文现状标记 `retranslate_chunk` / `keep_translation`；任务状态、用户端/管理端下载、ZIP 资料包和审计路径均已接入。
- 完成 JOB-01 任务生命周期审计增强：任务提交、启动、取消请求、取消终态、完成、失败和服务启动恢复扫描会写入审计日志，终态审计携带状态、阶段、耗时、错误码、翻译模式、并发数、原始文件名和关键产物路径。
- 完成 JOB-01 受控恢复重新入队策略：`web_status.json` 持久化恢复所需运行参数，启动恢复默认关闭，显式策略下只重新入队符合条件的恢复任务，并跳过自定义临时 API、取消标记和旧快照缺参任务；管理端可手动重新入队单个恢复任务。
- 完成 REPAIR-01 局部修复效果对比报告：管线在 `repair_merge_qa` 后生成 `repair_effectiveness.json/md`，对比修复前后 QA，统计问题减少率、已解决/新增/持续存在问题、改善 chunk 和回归 chunk；`experiment_metrics.json`、测试覆盖和 zip 资料包均已接入。
- 完成 EXP-01/REPAIR-01 批量实验修复效果对比证据汇总：`batch_experiment_summary.json`、相对基线差值、`batch_experiment_review.csv` 和评分后 `batch_experiment_evidence.json/md` 会汇总 `repair_effectiveness_*` 指标、问题减少率、已解决/新增/回归数量、状态分布和报告路径。
- 完成 EXP-01 确认后样本覆盖度审计：`--patent-batch-only` 筛选后的样本集会在 `sample_filter.selected_coverage` 和 `patent_batch_coverage` 中记录 6 类覆盖数量、最低建议数量、缺口和是否满足专利跑批覆盖。
- 完成 Web 修复效果对比报告产物收口：任务状态新增 `repair_effectiveness_report_ready`、问题减少率、修复前后问题数、已解决/新增/回归数量和整体状态字段；用户端新增 `repair-effectiveness.md` 下载入口，管理端新增 `kind=repair_effectiveness` 产物下载，任务完成审计详情记录 `repair_effectiveness` 路径。
- 完成 TABLE-01 确认表格结构下游消费：确认副本现在会被后半段 QA、修复后 QA 和结构化 PDF 导出优先读取；Web 确认发布后可刷新译文 PDF 和 PDF 报告，并在报告中记录 confirmed 来源和确认候选引用数。
- 完成 TABLE-01 确认候选结构补丁物化：人工确认后的合并单元格候选会生成 `table_structure_patches`，确认副本中的 cells 会保留可回滚补丁元数据，表格发布报告、Web 任务状态、PDF 报告和实验指标都会记录结构补丁数量、覆盖单元格和 PDF 引用数。
- 完成 OUT-01/TABLE-01 双语 HTML 表格结构补丁渲染：`bilingual.html` 在 confirmed 视图下会按 `structure_patches` 给译文 Markdown 表格输出 `rowspan` / `colspan`，并保留 `data-structure-patch-id` 供人工审阅；未确认 source 视图不改变阅读产物。
- 完成 OUT-01/TABLE-01 结构化 PDF 表格结构补丁渲染：`translated_full.pdf` 在 confirmed 视图下会按 `structure_patches` 基础绘制 `rowspan` / `colspan`，跳过 covered cell，并把 `table_structure_patch_rendered_count` 写入 PDF 报告和实验指标；未确认 source 视图不改变阅读产物。
- 完成 REPAIR-01/TABLE-01 表格结构补丁进入单元格级修复定位：译后 QA 会生成当前 cell 相关的 `matched_structure_patches`，修复请求会把其提升为 `merge_target.structure_patch_context` 和 `structure_patch_source_review_ids`，验证、合并、补丁审核和实验指标都会保留结构补丁支撑修复数量。
- 完成 REPAIR-01 局部修复回滚演练链路：默认生成 `repair_rollback.json/md`，显式 `--rollback-repairs` 或 Web 显式确认后才生成 `rollback_full.md`；任务产物摘要、用户端/管理端下载、zip 资料包、实验指标、批量评分表和评分后证据摘要均已接入回滚可用、已执行、匹配原文和状态分布。
- 完成 REPAIR-01 局部修复正式稿替换与正式回滚：默认生成 `repair_formal_replace.json/md` 与 `repair_formal_rollback.json/md`；显式 `--formal-replace-repairs` 或 Web 显式确认后才生成 `formal_full.md` 与 `formal_full.before_repair.md`，显式 `--formal-rollback-repairs` 或 Web 显式确认后才恢复正式稿并保留 `formal_full.repair_applied.md`；任务产物摘要、用户端/管理端下载、zip 资料包、实验指标、批量评分表和评分后证据摘要均已接入正式替换/正式回滚状态。
- 完成 REPAIR-01 局部修复列表式逐补丁审核台：用户端和管理端共用 `RepairPatchReviewModal`，可按发布阻断、人工处理状态、风险等级和关键词筛选补丁，查看 chunk、页码、合并目标、风险、默认/有效结论、候选片段和备注，并逐条或批量写回 `approve`、`reject`、`needs_revision` 或 `clear`；后端批量接口会先校验所有 `review_id`，避免半更新，Markdown 报告也会写出人工决策、审核人、时间和备注。
- 完成 REPAIR-01 局部修复补丁差异预览：`repair_merge.json` 和 `repair_patch_review.json` 会保留源文摘要、当前译文、修复候选、合并后片段和统一 diff；`repair_merge.md`、`repair_patch_review.md` 与 Web 审核台均可直接查看前后变化。
- 完成 B2 结构关系提示注入：`structure_hints` 已从表格保护扩展到图注/脚注父子关系、跨页关系和实体候选弱提示；`structure_hints_manifest.json`、`experiment_metrics.json` 和批量实验摘要会记录结构关系提示数、跨页关系提示数、实体提示数和关系类型分布。
- 完成 QA-02/B2 译后结构关系保真 QA：`qa_report.json/md` 可报告 `caption_or_footnote_relation_mismatch` 和 `table_footnote_binding_mismatch`，修复计划可生成 `rewrite_with_structure_relations` 与 `repair_table_footnote_binding`，实验指标和批量实验摘要可统计结构关系异常率与表格脚注绑定异常率。
- 完成 TABLE-01/OCR-01 普通表格 OCR 文本本地结构化推断：表格 OCR 候选即使只返回 Markdown 管道表或列数稳定的普通文本，也可在回写阶段生成 `structured_cells` 和估算 `cell_bboxes`。
- 完成 TABLE-01/OCR-01 表格 OCR 文本合并单元格候选推断：回写阶段保留管道表原始行宽，可把单行标题、短行尾部缺列和空单元格转换为 `merged_cell_candidates`，并随候选晋级保留到 canonical `block.meta.table`。
- 完成 TABLE-01/OCR-01 OCR 晋级合并候选接入表格重建：canonical `block.meta.table.merged_cell_candidates` 会进入 `table_reconstruction.json`、`structure_hints_manifest.json` 和翻译请求 `structure_hints`，并与本地候选按几何范围去重。
- 完成 TABLE-01 合并候选视觉证据分级：`candidate_status`、`visual_evidence_level` 和 `bbox_evidence.status` 可区分文本启发式、估算 bbox、OCR cell bbox、外部 span bbox 和未来人工确认状态，避免把估算 bbox 当作视觉确认。
- 完成 TABLE-01 合并候选人工确认审计产物：管线在 OCR 晋级后重新生成 `table_reconstruction.json`，并新增 `table_merged_cell_review.json/md`，按 `review_id` 整理候选、证据等级、默认决策和人工决策占位，进入实验指标和 zip 资料包。
- 完成 TABLE-01 合并候选 Web 人工确认回写：用户端和管理端可下载 `table_merged_cell_review.md`，读取候选清单，并按单条 `review_id` 写回 `confirm`、`reject`、`needs_revision` 或 `clear`；任务状态会同步统计待确认、已复核、已确认、已拒绝和需修改数量。
- 完成 TABLE-01 表格结构确认发布副本：管线默认生成 `table_structure_publish.json/md` 待确认报告；Web 显式确认且无待复核候选时生成 `table_reconstruction_confirmed.json`，阻断时删除旧确认副本；任务状态、实验指标、用户端/管理端下载和 zip 资料包均已接入该产物。
- 完成 TABLE-01 合并候选列表式审核面板：用户端和管理端共用 `TableMergedCellReviewModal`，可直接查看候选位置、跨度、证据、文本、备注和状态，并逐条写回确认/拒绝/复查/清空决策。
- 完成 TABLE-01 合并候选 bbox 页面预览：审核面板可按单条候选拉取带认证的 PNG 预览，后端只从任务 `input.pdf` 渲染对应页并叠加 `bbox_evidence` 框选，帮助人工确认时同时查看文本和页面证据。
- 完成 TABLE-01 合并候选筛选与批量审核：审核面板可按状态、证据和关键词筛选候选，勾选当前筛选结果后批量写回确认/拒绝/复查/清空；后端批量接口会先校验所有 `review_id`，避免半更新。
- 完成合并候选字段兼容：外部 OCR 的旧式 `type` / `row` / `cols` 候选会补齐 `span_type`、`row_index`、`column_index`、span 和 `covered_cells`，不破坏原字段。
- 完成本地推断 warning 的 QA 口径收口：`structured_table_inferred_from_text`、`cell_bboxes_estimated_from_region` 与 `merged_cell_candidates_inferred_from_text` 作为审计标记保留，不再单独阻断候选晋级；真正是否可晋级仍由结构门禁、locked token、bbox 和置信度判断。
- 新增 `test_table_ocr_plain_text_can_be_locally_structured` 与 `test_table_ocr_plain_text_infers_merged_cell_candidates`，覆盖普通表格 OCR 文本从回写、候选 QA、candidate promotion 到 canonical 表格结构的闭环。

## 仍需继续推进

- 表格确认副本已经接入后半段 QA/PDF 消费链，已确认合并候选也已物化为可回滚结构补丁，双语 HTML 与结构化 PDF 已能按补丁合并显示译文表格单元格，图注/脚注父子关系和实体候选也已进入翻译控制提示，译后结构关系保真 QA 也已能检查关系锚点和表格脚注绑定是否保留，局部修复能按 confirmed 结构补丁记录定位依据，并已具备副本级回滚演练、正式稿替换、正式稿回滚、列表式逐补丁审核台和补丁差异预览；下一步应聚焦复杂 PDF 图文原位重排、跨页分页联动和真实样本验收。
- 用真实论文样本按确认后的 `samples.csv` 和 `--patent-batch-only` 跑批，录入评分并补齐导师可读批量实验报告。
- 在已具备表格/公式 OCR 结构契约、普通表格 OCR 文本本地结构化推断、文本级合并候选推断、合并候选证据分级、人工确认审计清单、Web 列表式确认面板、候选筛选/批量确认、单条 bbox 页面预览、表格结构确认发布副本、确认视图下游消费、确认候选结构补丁、结构补丁支撑单元格级修复定位、局部修复回滚演练、正式稿替换/回滚、结构化结果透传、本地结构门禁、结构化 JSON 执行器入口和 canonical 结构晋级的基础上，加强高精度表格行列识别、复杂 `structured_cells` / `cell_bboxes` / `formula_latex` / `formula_tokens` 自动生成、复杂续表合并、表格/公式 OCR 引擎接入和复杂 PDF 正式稿重排。
- 在现有 Web 术语确认入口上补齐多人审核流转和确认后自动重译执行；候选译名单条/批量逐条编辑与确认后重译计划已具备基础。
- 完善 PDF 译文的复杂图表原位重排、页码关系精修和可读性评审入口。
- 补齐真实部署观测、恢复失败提示和服务重启演练。
- 继续收紧生产部署安全项，重点补部署验收和真实公网环境回归。
- 把局部修复从“补丁审核产物 + 列表式逐补丁审核台 + 补丁差异预览 + Web 整稿确认发布门禁 + 副本级回滚演练 + 正式稿替换/回滚”推进到“真实模型修复效果对比和真实样本正式稿验收”。

## 验证入口

- 自动测试：`python -m unittest discover -s tests`
- 最近完整校验（2026-07-06）：`python -m unittest discover -s tests` 通过，138 个测试通过、1 个 symlink 权限相关用例按预期跳过；`git diff --check` 通过。
- 单次实验指标：`output/experiment_metrics.json`
- 批量实验摘要：`batch_experiment_summary.json`、`batch_experiment_summary.md`
- 跑批样本清单：`samples.csv`、`samples.json`、`samples.md`，重点查看 `summary.coverage` 与 `confirmed_pdf_type`
- 人工评分表：`batch_experiment_review.csv`
- 评分后证据摘要：`batch_experiment_evidence.json`、`batch_experiment_evidence.md`
- 详细变更记录：[发布历史](06-发布历史.md)
